# home

초기 세팅 상태를 보여주는 자리 표시 화면이다. 디자인 확정 후 실제 홈 화면으로 교체된다.

- **라우트**: `/` — `src/app/page.tsx`
- **조립**: `features/toggle-theme` (다크 모드 데모 토글)
- **상태**: 없음
- **참고**: 색은 전부 시맨틱 토큰이라 다크 모드가 토큰 전환만으로 동작한다
