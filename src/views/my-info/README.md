# views/my-info

내 정보. 회원 정보와 배송지를 모아 보여주고 각 항목을 수정 화면으로 잇는다.

- **라우트**: `/mypage/info` — `src/app/mypage/info/page.tsx`
- **조립**: `entities/member`·`entities/pet`·`entities/address`의 조회 훅, `shared/ui`의 `page-header` · `badge` · `icon` · `skeleton`
- **상태**: 회원 정보·아이 목록·배송지 셋 다 서버 상태(TanStack Query)
- **참고**: UI 시안 기준(`mypa_011`, `1482-27384`)

| 파일 | 설명 |
| --- | --- |
| `ui/my-info-view.tsx` | 내 정보 |
| `ui/my-info-view.test.tsx` | 서버 값 표시, 아직 없는 값 처리, 불러오는 중 |
| `index.ts` | 공개 API |

## 짚어둘 것

**이름·생년월일은 화살표가 없다.** 시안은 다섯 줄 모두 화살표인데 고칠 화면이 없다(#64). 화면이 생기면 링크로 바꾼다.

**"내 아이들" 값은 아이 이름을 쉼표로 잇는다.** 시안 자리에는 닉네임 더미가 적혀 있어 PD 확인 대상이다(#194).

**배송지 항목은 한 화면이 새 배송지와 수정을 맡는다.** 어느 곳인지는 `?place=` 쿼리로 넘긴다. 배송지 입력 화면 자체는 #187에서 시안으로 옮긴다.

**값이 셋으로 나뉘어 온다.** 회원 정보는 `GET /members/me`, 아이 이름은 `GET /members/me/pets`, 배송지는 `GET /members/me/addresses`다(#266). 한 화면이지만 조회 셋이라 각자 도착한다 — 값 자리만 `Skeleton`으로 잡고 줄과 레이블은 바로 그린다.

**배송지 아이콘은 지은 이름으로 고른다.** 시안(`mypa_011`)이 집·회사에만 그림을 두는데 서버는 이름을 자유롭게 받는다. `집`·`회사`만 짝을 지어 두고 나머지는 그림 없이 둔다.

## 아직 없는 것

**이름·생년월일은 서버가 주지만 늘 비어 온다.** 회원가입도 온보딩도 묻지 않아 받는 자리가 없다. 비어 있을 때는 `등록 전이에요`로 알린다 — 빈 칸으로 두면 고장으로 읽힌다.
