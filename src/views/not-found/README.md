# views/not-found

없는 주소로 들어왔을 때 보이는 화면. `src/app/not-found.tsx`가 이 슬라이스를 부른다.

| 파일 | 설명 |
| --- | --- |
| `ui/not-found-view.tsx` | 그림·안내 문구·홈으로 가는 버튼. 시안 `404`(2115:171082) |

**헤더와 하단 바가 없다.** 길을 잃은 자리라 다른 데로 가는 길을 버튼 하나로만 준다.

그림은 이 화면에서만 쓰는 102px 한 장이라 `shared/ui/icon` 세트(24px)에 넣지 않고 여기에 뒀다.
