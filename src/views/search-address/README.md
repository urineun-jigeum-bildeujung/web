# views/search-address

주소 검색 화면. 와이어프레임 `mypa_312 계열 3장`에 대응한다.

| 파일 | 설명 |
| --- | --- |
| `ui/search-address-view.tsx` | 주소 검색 |
| `index.ts` | 공개 API |

## 라우트

`/mypage/address/search` — **경로는 임시다.** 페이지·라우터 구조는 다른 팀원이 작업 중이며 그 결과로 교체한다.

## 아직 없는 것

API 연동. 화면 안의 값은 확인용 목 데이터이며 백엔드 계약이 정해지면 교체한다.
