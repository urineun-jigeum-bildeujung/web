// 비교할 상품 고르기 라우트. 경로는 임시이며 라우터 구조 확정 시 교체한다.
import { SelectCompareProductView } from "@/views/select-compare-product";

export default function SelectComparePage() {
  return <SelectCompareProductView />;
}
