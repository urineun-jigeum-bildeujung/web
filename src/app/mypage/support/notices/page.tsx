// /mypage/support/notices 라우트. 화면 조립은 views/support-notices에 있다.

import { SupportNoticesView } from "@/views/support-notices";

export default function SupportNoticesPage() {
  return <SupportNoticesView />;
}
