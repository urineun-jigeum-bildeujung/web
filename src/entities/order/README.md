# entities/order

주문을 표현하는 것들.

| 파일 | 설명 |
| --- | --- |
| `ui/order-status-badge.tsx` | 주문 상태 뱃지. 결제완료·상품준비·배송중·배송완료·구매확정 |
| `index.ts` | 공개 API |

상태 값은 IA의 "주문/배송 내역" 단위기능을 따른다. **색만으로 구분하지 않도록 문구가 항상 함께 나온다.**

조회 훅(`api/`)과 주문 타입(`model/`)은 백엔드 API 계약이 정해진 뒤에 만든다.
