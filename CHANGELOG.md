# 변경 이력

이 프로젝트에서 무엇이 언제 달라졌는지 기록합니다. **날짜 단위로 쌓고, 각 항목에 이슈 번호를 답니다.**

- 이 문서는 **시간축**을 다룹니다. 프로젝트가 지금 어떤 상태인지는 [README](./README.md)를 봅니다.
- 릴리스 시점에는 해당 날짜 위에 버전 제목(`## v0.1.0`)을 얹습니다.

작성 규칙은 [git-convention](./docs/conventions/git-convention.md)의 "변경 이력 기록" 절을 봅니다.

---

## 2026-09-23

### 추가

- **헤더의 종이 읽지 않은 알림을 알리고, 열어 둔 화면에 새 알림이 토스트로 뜬다.** 그전에는 종이 알림 데이터와 연결되지 않은 그림이라 새 알림이 와도 표시가 없었고, 앱(웹뷰)은 웹 푸시를 받지 못해 열어 둔 채로도 새 알림을 몰랐다. 홈·상품 상세·찜·비교·마이페이지의 종을 하나로 모아 읽지 않은 알림이 있으면 오른쪽 위에 주황 점을 붙이고, 알림함에서 읽으면 바로 꺼진다. 로그인한 채 화면이 보이는 동안 30초마다 알림 목록을 다시 받아 새로 온 것만 토스트로 알린다 — 처음 들어올 때의 옛 알림은 알리지 않고, 한꺼번에 많이 오면 최신 셋만 띄운다. 웹 푸시를 켠 브라우저는 푸시가 오는 즉시 목록을 다시 받아 더 빨리 뜨고, 토스트는 한 곳에서만 띄워 같은 알림이 두 번 뜨지 않는다. 팀 결정대로 OS 알림 대신 웹 안에서 알리므로 앱 변경이 없다 (#395)
- 메인 화면의 카테고리 탭(사료·간식·영양제) 상품 격자와 "전체" 탭 타임딜 미리보기가 목데이터 대신 실제 API로 채워진다. 정렬 드롭다운을 백엔드가 지원하는 5종(추천순·인기순·낮은 가격순·높은 가격순·리뷰 많은순)으로 바꿨다 — 기존 최신순·별점순은 실제 시안 근거가 없던 값이었다. "더 보기"로 다음 페이지를 이어 받는다. 타임딜 미리보기는 진행 중인 딜 묶음이 여러 개면 가장 먼저 끝나는 것부터 보여주다가 끝나면 다음 묶음으로 넘어간다. "AI가 골라주는 맞춤 상품" 캐러셀과 `/recommendations`는 백엔드가 `petId`를 실제 조회에 반영하지 않고 적합도 값도 주지 않아(개인화 계약 미비) 이번엔 연동하지 않았다 (#289)
- 좋아요 화면의 찜 탭이 목데이터·로컬 상태 대신 실제 API로 동작한다. 카테고리(전체·사료·간식·영양제)로 거르면 서버가 그 조건으로 다시 조회하고, 하트를 눌러 찜을 풀면 확인창 없이 즉시 목록에서 빠지며 실패하면 되돌아온다. 찜 응답엔 상품의 카테고리가 없어 "찜 전체가 비었음"과 "고른 카테고리에만 없음"을 가르려고 전체 조회를 함께 둔다. 할인 중인 상품은 정가를 취소선으로, 할인율을 함께 보여준다 (#390)

### 변경

- **주문·배송 확인과 주문 상세가 PD팀이 오늘 정리한 새 시안을 따른다.** 목록은 "주문내역"·"취소·반품·교환" 두 탭으로 나뉘고, 주문을 한 줄로 접지 않고 **결제일로 묶은 뒤 결제 시각마다** 상품별 상태 뱃지(상태별 색)·상품 줄·버튼을 세운다 — 같은 날 여러 번 결제해도 날짜 머리는 하나이고, 날짜 사이에만 구분선이 들어간다. "자세히 보기" 버튼 자리는 주문마다 "주문 상세" 링크가 대신하고, 주문 취소 확인 버튼은 빨간색이 됐다. 주문 상세는 "주문 내역" 제목 아래 결제일·주문 상품·결제상세·배송지 네 카드와 1:1 문의 안내로 바뀌었고, 반품·교환 버튼은 맨 아래 진한 버튼으로 옮겼다. **서버에 없어 임시로 둔 것이 넷이다** — 목록의 상품별 금액은 줄을 비우고, "장바구니 담기"는 주문 응답에 상품 ID가 없어 누르면 준비 중이라고 알리고, "취소·반품·교환" 탭은 신청 건을 모아 주는 API가 없어 준비 중 화면을 두고, 상품마다 있는 "주문 취소"는 서버가 주문 전체만 취소해 어느 줄에서 눌러도 주문 전체가 취소된다(PD팀은 상품별 취소가 맞다고 답했다). 상세의 상태 뱃지와 반품·교환 신청 상태 뱃지(#334)는 시안에서 빠져 걷었다. 결제수단 줄이 위 금액과 12px 떨어지는 것은 주문 완료 화면에도 같이 들어간다 — 두 화면의 UI 시안이 같다 (#405)
- **리뷰를 쓸 때 아이를 여러 마리 고를 수 있다.** 한 상품을 두 아이에게 함께 먹이는 일이 흔한데 리뷰는 회원+상품당 한 건이라 한 마리만 실을 수 있었다. 작성 화면의 아이 줄이 여러 마리를 고르는 체크박스가 됐고, 작성한 리뷰 상세는 아이마다 칩을 보인다. 별점은 이제 4.5점이 5점으로 반올림되지 않고 그대로 보인다 (#391)
- **고양이는 등록·수정에서 체구를 묻지 않는다.** 체구는 강아지에게만 뜻이 있어 고양이 보호자에게는 답할 수 없는 질문이었다. 강아지 체구 안내는 초소형견 4kg 미만 / 소형견 4~10kg / 중형견 10~25kg / 대형견 25kg 이상 네 줄로 바뀌었다 (#391)

### 수정

- **주문에 아이를 함께 보낸다.** 백엔드가 오늘 새벽 주문 생성 요청에 `petId`를 필수로 넣어, 그대로 두면 배포되는 순간 **주문 생성이 400으로 막혀 결제가 통째로 멈춘다.** 여러 아이 중 고르는 자리는 시안에 없고 대표 아이를 둘지도 아직 정해지지 않아, 우선 기본 아이를 싣는다. 아이 목록을 알기 전에는 결제 버튼을 열지 않고, 만들어 둔 주문을 다시 쓰는 판정(#361)에도 아이를 넣었다 — 기본 아이를 바꾸고 돌아왔는데 옛 주문으로 결제하면 그 주문이 다른 아이 몫으로 남는다 (#393)

- **결제 준비가 막혔을 때 들고 있던 주문을 놓아준다.** 만들어 둔 주문을 다시 쓰려고 들고 있는데(#367), 서버가 그것을 거절할 때 보는 코드가 하나뿐이었다. 서버는 셋으로 거른다 — 못 찾거나(404), 남의 것이거나(403), 결제할 수 있는 상태가 아니거나(409). **앞의 둘이 오면 저장소를 비우지 않아 다시 눌러도 같은 자리에서 막혔다.** 문구는 뜨지만 그 안내대로 해도 빠져나갈 길이 없고, 탭을 닫아야 풀린다는 것을 사용자가 알 방법이 없다. 셋을 모두 받아 비운다. **상태 코드로 뭉뚱그리지 않았다** — 429처럼 잠깐 막힌 것까지 버리면 다시 누를 때 주문이 하나 더 생기고, 그게 #361에서 고친 문제다. 너무 좁게도 넓게도 잡지 않았는지 양쪽으로 되돌려 확인했다 (#388)

### 개발 환경

- **인프라가 수집하는 `/metrics`에 서버 지표를 내보낸다.** 인프라가 web 파드의 `/metrics`를 Prometheus로 읽도록 이미 켜 두었는데 우리 쪽에 그 경로가 없어 404로 헛수집하고 있었다. 이제 메모리·CPU·이벤트 루프 지연 같은 Node.js 기본 지표를 표준 이름으로 낸다. **그대로 열면 누구나 `leechs.shop/metrics`로 서버 내부 지표를 보므로 막았다** — Prometheus는 파드 IP로 직접 부르고 바깥 요청은 로드밸런서를 거쳐 도메인으로 오므로, Host가 IP·localhost일 때만 답한다. `x-forwarded-for`로는 가를 수 없었다 — Next.js가 모든 요청에 채워 넣는다 (#401)

- **화면에서 난 오류와 성능을 인프라팀 Grafana로 모은다.** 그동안 배포된 화면에서 오류가 나도, 느려도 알 방법이 없었다. 인프라팀이 준비한 수집 서버로 Grafana Faro가 오류·웹 바이탈·콘솔 오류를 보내고, 같은 출처 API 요청에는 추적 헤더(`traceparent`)를 실어 백엔드 로그와 잇는다. **수집 주소와 키가 있는 환경에서만 켜진다** — 로컬·CI는 지금과 같다. SDK와 OpenTelemetry가 gzip 71KB라 첫 화면이 뜬 뒤에 따로 불러오고, 첫 화면 번들은 0.5KB 늘었다. 민감정보는 수집 서버가 거르고, 우리가 직접 남기는 오류는 이미 요약만 남기는 `reportError`를 그대로 탄다 (#396)

### 문서

- **반품·교환 신청 화면 문서를 오늘 상태에 맞췄다.** 백엔드가 클레임 사진 업로드 주소(`POST /orders/images/presigned-url`)를 머지했는데 문서는 "엔드포인트가 없다"로, 신청 화면 시안은 "Figma에 없다"로 남아 있었다. 같은 날 신청 화면 시안도 사진 칸(최대 3장)을 넣어 완성돼, 다음 작업에서 교체한다고 고쳐 적었다. 동작 변경은 없다 (#405)
- **거래 도메인 주석과 README를 백엔드·PD의 지금 상태에 맞췄다.** 백엔드가 반품·교환 기간 판정 메서드를 `isClaimableForReturn`으로 바꿨는데 우리 주석·README 아홉 곳이 옛 이름(`isClaimable`)으로, 한 곳이 옛 줄 번호로 인용하고 있어 찾아가면 없는 메서드였다. 결제 README에는 PD가 9/18에 답한 본문 글자색 건이 여전히 "확인 대상"으로, 장바구니 README에는 옵션 없음·배송비 고정으로 이미 정리된 요청 필드가 "아직 응답에 없다"로 남아 있었다. 동작 변경은 없다 (#399)

- **주석이 설명하는 심볼에 붙도록 고쳤다.** 닫힌 JSDoc 바로 다음 줄에 또 JSDoc이 오면 앞의 것은 어디에도 붙지 않는데, 어제 PR이 여러 번 겹치면서 그런 곳이 넷 생겼다. **제일 아까운 것은 `OrderDetailItem`이었다** — `unitPrice`가 낱개 값이라는 설명이 클레임 타입에 붙어 있어 hover로 볼 수 없었다. 주문 상세가 `unitPrice * quantity`로 줄 금액을 내는 근거라, 모르고 그대로 쓰면 2개짜리 금액이 절반으로 나온다. 같은 뜻을 두 번 적어 둔 곳 둘도 합쳤다 (#386)

- **주문 상태 뱃지가 넷인데 문서는 다섯이라고 적고 있었다.** #297에서 `PAID`·`PREPARING`을 `배송준비중` 하나로 묶었는데 README와 컴포넌트 주석이 따라오지 않아, **없는 뱃지인 `결제완료`·`상품준비`를 있다고 적어 두고 있었다.** 세 곳을 실제와 맞췄다 (#386)

- **폴더 README의 파일 표를 실제 파일과 맞췄다.** 거래 도메인 열한 폴더를 대조해 아홉 줄이 빠져 있었다. 어제 만든 `views/checkout/model/pending-order.ts`도 올라가 있지 않았다. 동작 변경은 없다 (#386)

## 2026-09-22

### 추가

- **작성한 리뷰 상세가 실제 리뷰를 보여준다.** 그전에는 "디자인 확정 전 자리 표시 화면입니다"와 번호만 있어 자기가 쓴 후기를 다시 볼 수 없었다. 사진 리뷰 상세 시안대로 사진을 크게 보이고(여러 장이면 밀어서 넘김) 그 아래 닉네임·별점·작성일, 아이 프로필·사용 기간·반응 칩, 후기 글을 보이며, 하단에서 그 상품으로 갈 수 있다. 지운 리뷰는 찾을 수 없다고 알린다 (#363)
- **설정의 알림 스위치가 이 기기의 푸시 허용이 됐고, 알림함이 실제 알림을 보여준다.** 그전에는 스위치가 아무것도 저장하지 않아 새로고침하면 켜짐으로 돌아왔고, 알림 목록은 목데이터 여덟 줄이었다. 켜면 브라우저 권한을 묻고 푸시 토큰을 서버에 등록하며 끄면 지운다. 탭이 보이는 동안 온 푸시는 화면 아래 토스트로 알리고, 알림함은 서버 목록을 최신순으로 그리며 열어 본 알림은 읽음으로 알린다. 배송·상품·타임딜 알림은 상세에서 그 화면으로 이어진다. 푸시를 받을 수 없는 환경에서는 스위치가 잠기고 그 까닭을 보인다 (#354)
- **나의 상품 후기의 "작성 가능한 리뷰" 탭이 실제로 구매확정하고 아직 후기를 안 쓴 상품을 보여준다.** 그전에는 목데이터 셋을 늘 보여서 "후기 남기기"를 눌러도 가짜 상품으로 갔다. 후기를 등록하면 그 상품이 목록에서 빠진다. 응답에 없는 것 둘은 시안과 다르게 둔다 — 구매일 자리에는 구매확정 시각을 "구매확정일"로 보이고, "후기 작성 N일 남음"은 백엔드에 기한 개념이 없어 그리지 않는다 (#349)
- 아이 관리의 "아이 제품 관리" 탭에서 남긴 반응이 서버에 저장된다. 그전에는 목데이터 위에서 화면만 완료로 바꿨다. 목록은 서버가 주는 "지금 반응을 남길 수 있는 구매"(구매확정 뒤 사료·간식 7일, 영양제 30일)이고, 잘 맞았어요·그냥 그랬어요·안 맞았어요는 등록, "아직 일러요"는 보류로 나가 7일 뒤 다시 뜬다. 등록되면 카드가 목록에서 빠지고, 이미 남겼거나 아직 기간 전이면 그 까닭을 알린다. 시트 제목의 아이는 항목의 아이, 서버가 아직 안 주면 대표 아이다. 답한 항목을 주는 API가 없어 거르기 칩(전체·미입력·입력)과 구매일·구매 횟수 배지는 뺐다 (#345)
- **주문 상세가 반품·교환 신청 상태를 보여준다.** 접수하고 돌아오면 화면이 그대로여서 됐는지 알 길이 없었고, 반품하기를 또 누르면 신청 화면이 "신청 진행 중"으로 되돌려 보내 막다른 길이었다. 신청이 걸린 상품 줄에 "반품 수거 중"처럼 붙이고, 신청할 수 있는 상품이 하나도 없으면 반품·교환 버튼을 감춘다 — **끝난 신청만 있으면 다시 신청할 수 있으므로 버튼은 그대로 둔다.** 시안이 없어 새 모양을 만들지 않고 주문 상태 뱃지와 같은 것을 썼다 (#334)
- **마이페이지의 배송지 관리가 등록해 둔 배송지를 보여준다.** 그전에는 "디자인 확정 전 자리 표시 화면입니다"라는 개발용 문구와 추가 링크뿐이라, 넣어 둔 배송지를 볼 수도 고칠 수도 없었다. 같은 목록을 그리는 결제의 배송지 설정(`paym_011`)이 이미 완성돼 있어 그 목록을 `entities/address`로 내리고 두 화면이 함께 쓴다. **지우기는 넣지 않았다** — 되돌릴 수 없는 동작이라 확인창 문구와 자리가 시안에 있어야 한다 (#329)
- **배송이 끝난 주문에서 반품·교환을 신청할 수 있다.** 그전에는 주문 상세의 "반품 접수하기"가 준비 중 안내만 있는 화면으로 데려가 막다른 길이었다. 상품과 수량을 고르고 사유를 남겨 접수하면 주문 상세로 돌아간다. **서버가 거는 조건을 화면이 먼저 막는다** — 배송완료가 아니면 폼을 열지 않고, 진행 중인 신청이 걸린 상품은 목록에서 뺀다(품목 하나만 걸려도 요청 전체가 거절된다). 시안이 아직 없어 주문 상세와 같은 골격으로 맞췄고, 기능명세서의 사유 라디오는 서버가 자유 문자열 하나만 받아 여러 줄 입력으로, 사진은 클레임용 업로드 주소를 받을 곳이 없어 빼고 간다 (#327)

### 제거

- **한 번도 쓰이지 않던 런타임 의존성 셋을 걷어냈다 — `motion` · `recharts` · `date-fns`.** 기술 스택 표에 올라 있고 설치도 돼 있었지만 저장소 어디에서도 import한 적이 없었다. **번들에는 안 실린다** — 쓰지 않으면 묶이지 않는다. 비용은 설치 시간과 lockfile, 취약점 점검 대상, 그리고 "이건 왜 있지"를 매번 다시 확인하게 되는 쪽이다.

  **`motion`** — UI 시안에 애니메이션이 없다. **`recharts`** — 차트를 그리는 화면이 없고 shadcn 차트 컴포넌트도 받은 적이 없다. 소비 리포트가 생기면 그때 다시 본다.

  **`date-fns`는 쓸 자리가 있었는데도 뺐다.** 서버가 주는 날짜는 주문 일자·결제일시·후기 작성일·알림 받은 날·타임딜 시작 시각 여섯 자리에 나오고, 전부 `shared/lib/date/display-date.ts` 한 곳을 거친다. 그 유틸이 date-fns가 아니라 `Intl.DateTimeFormat`을 쓰는 이유는 **기준 시간대를 한국으로 못 박아야 하기 때문**이다 — 브라우저 시간대를 따라가면 자정 근처 주문이 기기마다 다른 날짜로 보인다(#295). date-fns v4로 시간대를 고정하려면 `@date-fns/tz`를 하나 더 들여야 하는데 플랫폼이 이미 하는 일이라, **패키지를 늘리는 대신 지금 방식을 유지하고 date-fns를 뺐다.**

  **`zustand`는 남긴다.** 아직 쓰는 곳이 없지만 서버와 무관한 UI 상태가 나올 자리라 팀장 판단으로 둔다.

  왜 안 쓰기로 했는지는 `library-convention`의 "겪은 사례"에 적어 두었다. 검색하면 제일 먼저 나오는 것들이라 적어 두지 않으면 다음에 같은 후보를 다시 들고 오게 된다 (#340)

### 수정

- **배송지 등록이 끝까지 도는지를 CI가 확인한다.** 등록은 배송지 관리 → 폼 → 주소 검색 → 폼으로 세 화면을 건너가는데, E2E는 세 화면이 각각 뜨는지만 보고 건너가는 길은 한 번도 지나지 않았다. **오늘 고친 버그 둘이 정확히 그 틈에서 났다** — 저장하면 방금 떠난 주소 검색 화면으로 돌아가던 것(#369)과 검색을 다녀오면 먼저 적어 둔 이름·연락처가 사라지던 것(#370)이다. 둘 다 새 배송지 등록은 전부 해당했는데 몇 달 동안 아무도 보지 못했고, 단위 테스트는 라우트를 건널 수 없어 앞으로도 못 본다. 이름을 먼저 적고 주소를 나중에 고르는 순서로 한 번에 밟고, 등록 요청에 화면에 칸이 없는 우편번호까지 실려 나가는지 본다. **고친 둘을 각각 되돌려 테스트가 깨지는 것까지 확인했다** (#382)


- **시연에서 누를 동작이 서버까지 가는지를 CI가 확인한다.** 장바구니는 조회 목이 아예 없어 **빈 화면이 통과**하고 있었고, 구매확정은 시트를 열어 폭만 쟀고, 주문취소는 확인창이 뜨는지만 봤다. 셋 다 요청이 한 번도 나가지 않았다. 담아 둔 줄이 있는 장바구니에서 고르고 수량을 바꾸고 빼고 결제로 넘어가는 길, 구매확정과 주문취소가 각각 `confirm`·`cancel`까지 가고 알림이 뜨는 길을 E2E로 고정했다. **고른 줄만 `?items=`에 실리는지**와 **못 사는 줄이 전체선택 분모에서 빠지는지**도 함께 본다 — 둘 다 어긋나면 고르지 않은 상품이 주문된다. **구매확정을 보내는 중에 시트가 닫히지 않는지도 단위 테스트에 더했다** — 그전에는 버튼이 잠기는 것까지만 보고, 바깥을 누르거나 `Escape`로 닫는 길은 아무도 보지 않았다. 그리로 닫히면 어느 주문을 확정하는지 잃은 채 요청만 남는다 (#379)

- **반품 접수가 끝까지 도는지를 CI가 확인한다.** E2E 목이 바뀐 주문 응답을 따라가지 못해 `deliveredAt`과 남은 수량이 빠진 옛 모양을 고정하고 있었다. 그 목으로는 **반품·교환 버튼이 아예 안 보이는 주문 상세가 통과했다.** 목을 지금 응답에 맞추고, 주문 상세에서 반품하기를 눌러 확인창을 지나 상품과 수량을 고르고 사유를 적어 접수하고 상세로 돌아오는 데까지 밟는 E2E를 넣었다. 7일이 지난 주문에 버튼이 없는 것도 함께 본다. **이 경로는 지금 브라우저로 볼 길이 없다** — 배송완료 주문을 만들 방법이 없어서다. 배송완료 시각은 고정 날짜로 박지 않고 오늘에서 거슬러 잡는다. 7일 규칙이라 그날이 지나면 CI가 저절로 깨진다 (#377)

- **주소 검색을 다녀와도 먼저 적어 둔 이름·연락처가 남는다.** 주소 검색이 별도 화면이라 거기로 넘어가는 순간 폼이 통째로 사라졌다. 고른 주소는 주소창에 실려 돌아오는데 **이름·연락처·상세주소·요청사항은 아무 말 없이 빈 칸이 됐고**, 새 배송지는 반드시 검색을 거치므로 주소보다 이름을 먼저 적은 사람은 매번 다시 적어야 했다. 검색하러 갈 때 적던 값을 탭 저장소에 적어 두고 돌아오면 되살린다. **그냥 다시 들어온 경우는 되살리지 않는다** — 지웠다고 생각한 값이 돌아오면 그대로 저장된다. 고치던 대상이 같을 때만 쓰고, 저장에 성공하면 비운다. 이름·연락처는 개인정보라 주소창이 아니라 저장소에 둔다 (#370)

- **반품·교환 신청에서 서버가 거절할 것을 화면이 먼저 막는다.** 백엔드가 주문 상세 응답에 배송완료 시각과 남은 수량을 더해 줘서, 그전에 "응답에 없어 못 한다"고 적어 둔 셋을 붙였다. **하나, 신청 수량 상한이 주문 수량이었다** — 2개 산 상품을 1개 반품한 뒤에도 2개를 고를 수 있었고 사유까지 다 적은 뒤에 거절당했다. 남은 수량(`effectiveQuantity`)을 상한으로 두고 남은 것이 없는 줄은 목록에서 뺀다. **둘, 반품·교환 버튼이 배송완료 주문 전체에 떴다** — 기능명세서와 서버 `Order.isClaimable`은 배송완료 뒤 7일까지인데 화면은 상태만 보고 있었다. 이제 같은 규칙으로 막는다. **셋, 주문 완료 화면이 숫자 주문 id를 주소창으로 날랐다** — `?order=`를 바꾸면 이 결제와 상관없는 주문으로 갔다. 승인 응답이 주는 id를 쓴다 (#374)

- **배송지를 저장하면 들어온 화면으로 돌아간다.** 그전에는 한 칸 되돌리기라 방금 떠난 주소 검색 화면으로 갔다 — 주소는 거기서만 고르므로 **새 배송지 등록은 전부 그랬다.** 검색 화면은 쪽을 넘길 때마다 history를 쌓아 몇 칸을 되돌릴지 셀 수도 없다. 들어온 화면이 자기 경로를 `from`에 실어 보내고, 검색 화면이 `place`처럼 그것도 그대로 돌려주게 했다. 배송지 관리에서 들어왔으면 배송지 관리로, 결제 배송지 설정에서 들어왔으면 거기로, 결제 화면에서 배송지를 등록하러 갔으면 **고른 상품을 들고** 결제 화면으로 돌아온다. `?from=https://…`처럼 바깥을 가리키는 값은 쓰지 않는다 — 주소창에 실려 오는 값이라 손으로 고치면 저장을 마친 사용자를 남의 사이트로 보내게 된다 (#369)

- **결제창에서 취소하고 돌아와 다시 눌러도 주문이 하나 더 만들어지지 않는다.** 만들어 둔 주문을 컴포넌트 상태로 들고 있어서, `failUrl`로 돌아오며 화면이 다시 서는 순간 사라졌다. **사용자가 가장 많이 밟는 길이 그쪽**이라 앞서 막은 것(#361)은 결제창이 열리기 전에 막히는 경우만 덮고 있었다. 탭 안에서만 사는 `sessionStorage`로 옮겨 리다이렉트를 건너 재사용한다 — 탭을 닫으면 사라져야 해서 `localStorage`는 쓰지 않고, 주소창에도 싣지 않는다(사용자가 바꿔 남의 주문으로 시도할 수 있다). 들고 있던 주문을 서버가 `PAYMENT_409_ORDER_NOT_PAYABLE`로 거절하면 비워 다음에 새로 만들고, 결제창을 통과했으면 주문 완료 화면이 비운다. **저장소가 막힌 환경(사생활 보호 모드)에서는 조용히 그전 동작으로 떨어진다** — 거기서 던지면 결제 자체가 막힌다 (#367)
- 리뷰를 다 쓰고 "확인"을 누른 뒤나 반려동물 등록을 마치고 홈으로 간 뒤 뒤로가기를 누르면 방금 끝낸 폼(빈 리뷰 작성 화면, 온보딩 완료 단계)으로 돌아오던 것을 고쳤다. 완료 뒤 이동은 히스토리를 바꿔 끼워 그 화면이 남지 않는다 (#371)
- **결제가 실패하거나 취소되어 돌아와도 고른 상품이 그대로다.** 결제창에 넘기던 실패 복귀 주소가 `` `${origin}/payment` `` 한 줄이라 **고른 것을 싣는 `items`가 통째로 빠졌다.** 그러면 없는 것으로 읽혀 살 수 있는 줄 전부가 잡힌다 — 장바구니에서 하나만 골라 결제하다 취소하면 돌아왔을 때 **셋이 다 잡히고 금액도 셋 몫**이었고, 실패 안내를 보고 다시 누른 사용자가 고르지 않은 상품까지 주문하게 됐다. `toFailUrl`을 `toSuccessUrl` 옆에 두고 **우리가 실은 값만** 되돌려 싣는다. **토스가 붙인 `code`·`message`·`orderId`는 옮기지 않는다** — 실어 돌면 실패 안내가 옛 값으로 다시 뜨고 주소가 회차마다 길어진다. 쿼리 이름은 `ITEMS_PARAM` 상수로 모았다. 오타 하나에 주문이 장바구니 전체로 넓어지는 자리다 (#364)

- **반품·교환 접수가 실패해도 처리되지 않은 Promise 거부가 나지 않는다.** 접수 함수가 `mutateAsync`라 실패하면 던지는데 `onClick`에 그대로 넘기고 있어 받아 줄 곳이 없었다. 서버가 막는 경우가 셋이나 실재한다 — 진행 중인 신청·수량 초과·기간 경과. 특히 **수량 초과는 밟기 쉽다**: 이미 신청한 몫을 뺀 수량이 응답에 없어 상한을 주문 수량으로 잡고 있어서, 2개 산 상품을 1개 반품한 뒤에도 2개를 고를 수 있다. 사용자가 보는 것은 달라지지 않는다 — 문구는 전역 토스트가 이미 띄우고 있었다. **실패 경로 테스트가 하나도 없어 함께 넣었다**: 실패해도 적어 둔 사유가 남고 다시 낼 수 있는지 본다. 배송지·결제 화면이 쓰던 것과 같은 방식이다 (#358)
- **결제가 실패한 뒤 다시 눌러도 주문이 하나 더 만들어지지 않는다.** 주문 생성 → 결제 준비 → 결제창 세 걸음 중 뒤 둘에서 막히면 다시 누를 때 처음부터 갔다. 세 번 실패하면 `PENDING` 주문이 셋 남고, 그것들이 주문 내역에 "결제 대기" 줄로 쌓여 **한 번 사려던 것이 목록에 여러 줄로 보였다.** 만든 주문을 들고 있다가 보내려는 본문(배송지·상품·수량·요청사항)이 그대로면 결제 준비부터 다시 한다. **한 글자라도 다르면 새로 만든다** — 배송지를 바꿨는데 옛 주문으로 결제하면 엉뚱한 곳으로 간다. 같은 주문으로 결제 준비를 다시 부르는 것은 서버가 이미 받아 준다(`RequestPaymentService`가 중복 저장에서 기존 결제를 찾아 같은 `tossOrderId`를 돌려준다). 붙이기 전에 그 코드를 확인했다 (#361)

- 결제상세의 이름·값 목록이 스크린 리더에 짝으로 읽힌다. 세부 항목을 붙이려고 `<div>`로 한 겹 더 감싸고 있었는데, 그러면 그 안의 항목이 목록 소속으로 읽히지 않는다. **Lighthouse 접근성이 `/payment`에서 두 항목을 잡아 92점이었다** — 이 프로젝트 목표는 95점이다. 보이는 간격은 그대로다 (#341)
- 휴대폰 인증에서 통신사를 골라도 React 경고가 뜨지 않는다. `Select`가 값 없이 떠 있다가 고르는 순간 제어 컴포넌트로 바뀌고 있었다. 보이는 것은 달라지지 않는다 — 빈 값도 "고른 것 없음"이라 안내 문구가 그대로 뜬다. **고르는 E2E가 콘솔까지 보게 했다** — 화면을 열기만 하는 검사로는 값이 바뀔 때 나는 것을 못 잡고, Radix가 이것을 `console.warn`으로 내서 오류만 세던 검사에도 안 걸렸다 (#336)
- 배송지 두 화면이 줄을 실제로 그리는지 E2E가 겨눈다. `screens.spec.ts`가 옛 응답 모양(`{ addresses: [...] }`)의 스텁을 들고 있었는데, 같은 경로를 회원 픽스처가 나중에 등록해 **한 번도 쓰이지 않았다.** 조회가 실패해도 화면이 오류를 직접 그려 콘솔이 깨끗하므로 "오류 없이 그려진다"만으로는 잡히지 않는다 (#329)
- 결제수단 로고를 이미지 최적화 없이 원본 그대로 내보낸다. 83×16짜리 6KB PNG라 WebP로 바꿔 얻을 것이 없는데 `next/image`가 그 한 장을 받으려고 `/_next/image`를 한 번 더 왕복했다. **CI에서 그 왕복이 끝나지 않아** 주문 상세를 여는 E2E가 되풀이 실패했고, 같은 커밋을 재실행하면 통과해 원인을 못 찾고 있었다 — 실패한 회차의 Playwright 트레이스에 응답 없는 요청이 정확히 그것 하나였다. 최적화를 다시 켜면 걸리도록 회귀 테스트를 뒀다 (#324)

### 제거

- **결제 수단 관리 화면(`/mypage/payment`)을 뺐다.** 목 카드 세 장을 사용자에게 보여주고 있었는데, 붙일 API가 없고 앞으로도 없다. 확정 UI 시안에 `mypa_05` 계열이 한 장도 없고(와이어프레임에만 여섯), 결제수단 기억은 토스페이먼츠가 `customerKey`로 맡아 우리가 보관할 것이 없다. 카드번호·CVC를 직접 받는 형태라 PCI DSS 확인 대기로 남아 있던 항목도 함께 사라진다. **마이페이지 시안(`mypa_001`)에는 그 줄이 아직 그려져 있어** 진입점을 뺀 것은 의도된 시안 이탈이며, PD에 시안 수정을 요청해야 한다 (#348)

### 개발 환경

- **배송지 폼이 `react-hook-form`과 `zod`를 쓴다.** `AGENTS.md` 기술 스택 표가 폼 도구로 둘을 적어 두었는데 **저장소 이력에서 한 번도 import된 적이 없었다** — 폼이 전부 `useState`였다. 규칙과 코드가 어긋난 채 폼만 늘고 있어 우리 담당인 이 화면부터 옮겼다. 검증 규칙은 `model/address-form-schema.ts` 한 곳에 모았고 **서버 `AddressRegisterRequest`의 제약을 그대로 옮긴 것**이라, 저장 버튼 잠금 조건을 손으로 나열하다 상세주소를 빠뜨려 400이 나던 종류(#314)가 다시 생기지 않는다. 화면 동작은 그대로다 — 기존 테스트 21개를 고치지 않고 통과한다. **잠금 판정에 `formState.isValid`를 쓰지 않는다**: resolver 검증이 비동기라 마지막 칸을 채운 직후에도 버튼이 한 박자 늦게 풀려, 같은 스키마로 `useWatch` 값을 그 자리에서 `safeParse`한다 (#355)

- `shadcn`을 `devDependencies`로 옮겼다. CLI인데 `dependencies`에 있어 **배포 의존성 트리에 express와 MCP SDK가 딸려 들어와 있었다.** 런타임 코드가 `shadcn`을 import하지 않고 CI도 부르지 않는다 — `npx shadcn add`는 `devDependencies`에서도 그대로 동작한다. 배포 의존성의 취약점이 4건에서 1건으로 줄었다. `overrides` 두 항목이 그대로인지, `npm ci`가 깨끗하게 끝나는지 확인했다 (#332)

## 2026-09-21

### 추가

- **장바구니 담기가 실제로 서버에 간다.** 그전에는 "장바구니에 담겼어요" 안내만 뜨고 아무것도 보내지 않아, 담고 장바구니로 가면 비어 있었다. 상품 상세와 타임딜 두 화면에 붙였고 담는 동안 버튼에 대기 표시가 돈다. **낙관적 갱신을 걸지 않는다** — 수량 변경과 달리 담기는 서버가 줄을 만들어야 짝이 확정되고, 먼저 그렸다가 되돌리면 방금 담은 것이 사라지는 장면이 된다. 상품 비교 화면은 상품 자체가 아직 목이라 붙이지 않았다 (#316)

- 리뷰 작성이 실제로 등록된다. 그전에는 "등록하기"가 아무것도 보내지 않고 완료 화면으로만 넘어갔다. 답한 반응 문항만 서버에 실리고, 사진은 먼저 S3에 올린 뒤 그 주소를 함께 보낸다. 아이 목록·보호자 닉네임·상품 줄도 상수 대신 서버 값이다. 이미 쓴 상품과 구매 확정 전 상품은 왜 안 되는지 알린다. 서버가 반응 문항을 하나 이상 요구해 그때까지 등록 버튼이 하나는 받도록 막는다. 나의 상품 후기의 "작성한 리뷰" 탭도 서버 목록을 보인다 — 응답에 구매일이 없어 작성일을 대신 보인다. "작성 가능한 리뷰" 탭은 목록 API가 아직 없어 목데이터 그대로다 상품 정보나 아이 목록을 못 받으면 스켈레톤으로 덮어 두지 않고 못 받았다고 알리며 다시 시도할 수 있다 (#291 리뷰) (#291)
- 온보딩과 아이 정보 수정에서 고른 사진이 실제로 올라간다. 그전에는 사진을 골라도 파일을 들고만 있다가 버렸다. 백엔드가 파일을 받지 않고 서명된 S3 주소만 주므로 프론트가 그 주소로 직접 올린 뒤 받은 주소를 등록·수정 요청에 싣는다. 올리는 동안 "작성 완료"·"수정완료" 버튼에 대기 표시가 돌고, S3가 거절하거나 서버가 사진 형식을 거절하면 무엇을 다시 해야 하는지 알린다. 내 프로필 사진은 바꾸는 화면이 아직 없어 이번에 붙이지 않았다 (#269)
- 주문 상세가 실제 주문을 보여준다. 목 데이터 네 벌을 들고 있어 어느 주문을 눌러도 값이 같았다. 배송비는 응답에 필드가 없어 결제 금액에서 상품 금액을 빼 만들고, 도로명과 상세 주소는 한 줄로 합쳐 보여준다. 상품이 여럿이면 모두 그린다 — 목록은 첫 줄만 세우고 나머지를 수로 접지만 상세는 무엇을 샀는지 다 보여야 한다. **결제 전 주문은 결제 정보가 비어 오므로 결제상세 카드를 세우지 않는다** (#288)
- 주문 목록이 스크롤 끝에서 다음 쪽을 이어 가져온다. 한 번에 오는 것이 기본 열 건이라 첫 쪽만 그리면 열한 번째 주문부터 볼 길이 없었다. 시안에 "더보기" 버튼 자리가 없어 목록 끝이 화면에 들어오면 자동으로 잇는다. **둘째 쪽부터 실패하면 이미 받은 주문을 그대로 두고 다시 시도 버튼만 붙인다** — 보고 있던 목록이 오류 화면으로 덮이면 스크롤하던 자리를 잃는다. 서버가 방금 보낸 커서를 그대로 돌려주는 경우도 멈춘다 (#288, #288 리뷰)
- 주문·배송 확인이 실제 주문을 보여준다. 그전에는 목 데이터 네 건을 들고 있어 무엇을 샀는지 이 화면에서 알 수 없었다. 구매 확정과 주문 취소도 서버를 부르고 끝난 뒤 목록을 다시 받는다 — 전에는 화면 안의 배열만 바꿔서 새로고침하면 되돌아왔다 (#284)
- 인증한 휴대폰 번호가 회원 정보에 저장된다. 그전에는 인증만 되고 아무것도 보내지 않아, 내 정보의 휴대폰 번호가 계속 `등록 전이에요`로 남았다. **서버가 저장 시점에 인증번호를 다시 검증한다** — 프론트가 "인증했다"고 주장하는 것만 믿지 않아서, 화면이 인증번호를 완료 버튼까지 들고 있는다 (#278)
- 이름과 생년월일을 고칠 수 있다. 시안이 따로 없어 닉네임 변경과 같은 골격으로 맞췄다. 생년월일은 치는 대로 `0000. 00. 00`으로 맞춰지고, 달력에 없는 날은 알리고 저장을 막는다 — 그대로 보내면 서버가 `LocalDate`로 읽지 못해 본문을 통째로 거절한다 (#278)
- 아이 정보 수정 세 화면(기본·체형·건강)이 저장된 값으로 열리고 실제로 저장된다. 수정 API가 없어 값만 채우면 "수정완료"가 아무것도 하지 않는 화면이 되기에 미뤄 뒀던 자리다(#230). **화면마다 자기가 고치는 필드만 보낸다** — 체형 화면이 이름·나이까지 실으면 고치지도 않은 값을 덮어쓴다 (#268)
- 아이 관리 카드의 화살표가 **어느 아이를 고치는지 함께 넘긴다.** 라우트가 `/mypage/pets/basic`처럼 아이를 가리지 않아 `?petId=`로 싣는다. 없으면 고칠 아이를 모른다고 알리고 저장을 막는다 (#268)
- 아이 정보 수정 화면이 저장된 값을 받는 동안 화면 골격으로 자리를 잡는다. 빈 화면이었다가 갑자기 차면 눌리는 자리가 밀린다 (#268 리뷰)
- 리뷰 필터의 품종·건강 관심사 선택하기 자리표시자를 좌우 분할 전체화면과 연결하고, 품종을 체구그룹(초소형·소형·중형·대형·믹스)으로 묶어 보여줬다. 체구 분류는 PD팀 확정 전이라 성견 평균 체중 기준의 임시 값이고, 골라도 목업 리뷰 필터링에는 아직 반영되지 않는다(리뷰 API 연동 시 처리) (#264)

### 수정

- **살 수 없는 상품이 왜 못 사는지 보인다.** 장바구니가 못 사는 까닭을 다섯 가지로 받는데 우리는 하나만 알아듣고 있었고, 그 하나마저 화면에 뜬 적이 없었다 — 이름이 오는 경우엔 이름이 까닭 자리를 덮어써서 **품절되거나 판매가 끝난 줄이 멀쩡한 상품처럼 보였다.** 이제 품절·판매 종료·타임딜 종료·없는 상품·일시적 확인 불가를 각각 알리고, 이름이 있으면 그 아래에 까닭을 둔다. "살 수 없으면 내용이 전부 비어 온다"고 잘못 적어 둔 문서 네 곳도 바로잡았다. 주문 상세의 배송 요청사항 타입도 서버가 주는 대로 비어 있을 수 있게 고쳤다 (#318)

- **배송지 저장이 조용히 실패하거나 통째로 거절되던 세 자리를 고쳤다.** 상세주소는 서버가 필수로 받는데 저장 버튼이 그 칸을 보지 않아, 비운 채 누르면 "입력한 내용을 다시 확인해 주세요"만 뜨고 어느 칸이 문제인지 알 수 없었다. 배송 요청사항은 지우려고 비워도 **저장에 성공했다고 하고 옛 문구가 그대로 남았다** — 서버가 빈 값을 "건드리지 마라"로 읽는다. 이미 기본인 배송지의 체크를 끄면 서버가 저장 전체를 거절해 **같이 고친 이름·연락처까지 무산됐는데**, 이제 그 체크를 잠그고 다른 배송지를 기본으로 지정하면 된다고 알린다 (#314)

- **결제 승인이 100% 실패하던 것을 고쳤다.** 주문 완료 라우트가 서버 컴포넌트인데 거기서 승인을 불렀다. 토큰은 브라우저에만 있어 서버 렌더에서는 인증이 실리지 않고, 서버용 API 주소도 설정돼 있지 않아 요청이 나가기 전에 끊겼다. **승인이 막히는 시점엔 이미 돈이 나간 뒤라** 모든 결제가 "결제 확인이 끝나지 않았어요" 화면으로 떨어지던 상태였다. 승인을 브라우저에서 부르되 같은 결제에 두 번 나가지 않게 막았다 (#308)
- **주문 완료 화면이 방금 산 주문을 보여준다.** 그전에는 결제를 마친 모두가 `상품명`·`천경진`·`테헤란로 123`이라는 목 데이터를 봤다. 합계만 실제 값이라 5만원을 결제해도 내역은 9,345+3,000으로 맞지 않았다. 주문 상세 조회가 열리고(#288) 숫자 주문 id가 복귀 주소에 실려 오면서(#301) 막고 있던 두 가지가 풀렸다 (#308)
- **결제창에 서버가 만든 주문의 금액이 실린다.** 그전에는 화면이 장바구니로 다시 센 값을 보냈다. 장바구니 캐시가 1분까지 낡을 수 있어, 그 사이 가격이 바뀌거나 타임딜이 끝나면 서버가 만든 주문과 금액이 갈린다. **그대로 결제창을 통과하면 승인에서 막히는데 그 시점엔 이미 돈이 나간 뒤**이고 주문은 결제 대기로 남는다. 화면에 보이는 합계는 그대로 둔다 — 그것은 장바구니를 보여주는 값이라 역할이 다르다 (#312)

- **서버가 내는 실패 문구가 화면에 제대로 뜬다.** 에러 코드 매핑을 백엔드 소스의 코드 75개와 전수 대조했더니 두 개가 **어디에도 없는 키**였다. 결제 금액이 어긋났을 때 백엔드가 자동 취소를 걸었다는 사실을 알리는 문구가 그중 하나라 한 번도 뜬 적이 없었고, 소셜 로그인 복귀 화면에는 입력칸이 하나도 없는데 "입력한 내용을 다시 확인해 주세요"가 떴다. 가입을 마치지 않은 사용자가 장바구니·주문·결제·배송지·리뷰 어디서든 받던 "권한이 없어요"도 "회원가입이 필요해요"로 바로잡았다. 주문 취소·구매확정이 이미 지난 단계라 막히는 경우, 마지막 기본 배송지를 해제하려는 경우도 무엇을 해야 하는지 알린다 (#310)

- **결제가 실서버에서 아예 되지 않던 것을 고쳤다.** 주문 생성이 장바구니와 같은 규격(`itemType`·`itemId`)을 보내고 있었는데 서버는 `productId`와 `dealItemId` 중 **하나만** 받는다. 둘 다 안 보내니 검증에 걸려 매번 거절됐고, 결제 버튼을 눌러도 결제창이 뜨기 전에 끊겼다. 배송지 목록도 서버가 최상위 배열로 주는데 `{ addresses }`로 꺼내고 있어, 붙는 순간 결제·배송지 관리·배송지 선택·내 정보 네 화면이 함께 죽는 상태였다. **테스트가 틀린 규격을 그대로 고정하고 있어 드러나지 않았다** — 목을 서버 기준으로 고쳤다 (#306)

- 결제를 마친 뒤 "주문 상세 보기"가 방금 산 주문을 연다. 그전에는 항상 `/mypage/orders/1`로 갔다 — 주문 상세 API가 열리기 전에 화면을 세우면서 둔 목 값이 그대로 남아 있었고, 주문 상세가 실제 API를 쓰게 되면서(#288) 남의 첫 주문을 열거나 조회에 실패하는 자리가 됐다. **결제는 리다이렉트로 돌아와 화면이 들고 있던 값이 사라진다** — 토스가 붙여 주는 `orderId`는 문자열 주문번호라 주문 상세로 갈 수 없어서, 주문을 만들 때 받은 숫자 id를 돌아올 주소에 실어 건너보낸다. 주소창으로 직접 들어와 그 값이 없으면 주문 내역으로 보내고 버튼 문구도 그에 맞춘다. **백엔드에 새로 요청한 것은 없다** (#301)

### 변경

- 리뷰 작성에서 **기호성과 급여 편의성이 필수**가 됐다. 시안이 문항마다 필수·선택 배지를 그렸고 그 둘만 필수다 — 다음 추천의 뼈대라 없으면 추천이 서지 않는다. 그전에는 "반응 문항 하나 이상"이라는 임시 규칙으로 막고 있었는데, 시안이 정한 필수 둘이 서버 조건을 함께 채워 그 임시 규칙이 필요 없어졌다. 섹션 제목의 오타("A 추천")도 "AI 추천"으로 바로잡고, 시안에 없는 섹션 배지를 걷었다 — 섹션에 "선택"을 달면 그 안의 기호성이 필수인 것과 어긋난다 (#302)

- 주문 상세와 주문 완료의 결제수단 자리에 토스페이 로고가 뜬다. 그전에는 서버가 준 결제수단 이름을 글자로 그렸다. PD팀이 두 화면을 시안 `paym_002`의 토스페이 로고로 통일하라고 확정해 그대로 맞췄고, 로고는 시안 `paym_001`의 결제 방법 자리에 있는 것을 그대로 가져왔다 (#304)
- 서버가 준 날짜·시각을 화면 어디서나 한국 기준으로 그린다. 그전에는 화면마다 `new Date(iso)`를 그대로 date-fns `format`에 넘겨 표시 기준이 브라우저 시간대를 따랐다. 국내 사용자만 쓰는 서비스라 대개 같지만 자정 근처 주문은 기기에 따라 하루 어긋나, 같은 주문이 사람마다 다른 날짜로 보일 수 있었다. **읽을 수 없는 값이면 라벨까지 줄을 통째로 비운다** — 그전에는 넷이 아무 방어 없이 `format`을 불러 서버가 이상한 문자열을 주면 화면이 통째로 깨졌다. 타임딜의 "오늘 오전 10시" 같은 문구도 같은 문제였다. `isToday`·`isTomorrow`가 브라우저 시간대로 판정해 자정 근처에 열리는 딜이 기기마다 다른 날로 보였다. 주문 목록·주문 상세·주문 완료·내 리뷰·타임딜 다섯이 같은 함수를 쓴다 (#295)
- 리뷰 작성의 사진 첨부가 시안대로 "사진 첨부하기" 시트를 거친다. 더하는 칸을 누르면 사진첩과 카메라 중 고르고, 사진첩은 여러 장을 골라도 시트가 남아 이어서 고르며 카메라는 한 장 찍으면 바로 닫힌다. 확인은 닫기만 한다 — 시안의 라벨이 비어 있어 PD와 정했다. 합쳐서 세 장이고 다 차면 두 줄이 잠긴다. 디자인 시스템에 새로 들어온 카메라·갤러리 아이콘 두 개를 공용 아이콘 세트(53종)에 더했고, 아이 사진 등록 자리가 따로 쓰던 react-icons 카메라를 세트 아이콘으로 바꿨다 (#296)
- 결제 직후 주문이 "배송준비중"으로 보인다. 서버는 결제완료와 상품 준비를 나누지만 시안에 "결제완료" 뱃지가 없어 무엇을 보여줄지 PD팀에 물었고, 두 단계를 묶어도 된다는 답을 받았다. 화면에만 있던 상태 하나를 걷었다 (#297)
- 주문 화면의 "상품 옵션" 자리에 수량을 넣는 것이 확정됐다. 백엔드에 옵션이라는 데이터가 없고 PD팀도 "옵션은 빼고 수량은 있어도 괜찮다"고 확인해 줬다. 임시로 넣어 두었던 표시가 그대로 맞는 것이어서 화면은 달라지지 않는다 (#297)
- 구매 확정 시트와 주문 취소 확인 창이 서버에 보내는 동안 닫히지 않는다. 바깥을 누르거나 Escape로 닫으면 어느 주문을 확정·취소하는지 잃은 채 요청만 남아, 끝났을 때 무엇이 처리됐는지 알 수 없었다 (#284 리뷰)
- 주문 상태 뱃지가 다섯 단계를 모두 보여준다. 그전에는 명세 Example에 나온 셋만 옮기고 배송준비중·배송중은 뱃지도 행동 버튼도 없이 지나갔다. 백엔드 `OrderStatus` enum에서 값을 확인해 채웠고, 시안에 자리가 없는 취소·환불은 그대로 두어 모르는 값으로 다룬다 (#288)
- 반품·교환 확인창이 접수 대신 신청 화면으로 보낸다. 그전에는 "반품 접수가 끝났어요" 토스트만 띄우고 실제로는 아무 일도 일어나지 않았다. 기능명세서의 접수에는 사유와 사진이 따라서 확인창에서 바로 서버를 부를 수 없다. **신청 화면은 시안이 아직 없어 접수까지 가지 못한다** — 개발용 문구가 그대로 보이던 자리를 왜 지금은 안 되는지 알리고 주문 상세로 돌아갈 수 있는 화면으로 바꿨다 (#288)
- 리뷰 작성 화면의 반응 문항 키와 보기 값을 백엔드 열거형(`ReviewQuestionType`·`ReviewAnswer`) 값으로 바꿨다. 화면은 시안 이름(`taste`·`good` 등)으로 들고 있었는데 등록 API는 열거형 이름을 `valueOf`로 읽어 그대로 보내면 거절된다. 화면 문구는 그대로다. 알러지 문항은 백엔드가 "보통"을 받지 않아 없었어요·있었어요 두 보기만 남겼다. 작성 화면 진입과 초안 저장 단위도 `orderItemId`에서 `productId`로 바꿨다 — 백엔드가 회원+상품당 리뷰를 한 건만 받아 구매 건을 가를 이유가 없다 (#281)
- 타임딜 화면을 UI 시안으로 교체했다. 목업 상품·가격을 시안 값으로 바꾸고, 가격 표기 순서(정가 취소선 위·할인율+최종가 아래, 할인율·최종가 세로 가운데 정렬)와 담기 버튼 모양(테두리 없는 6px 모서리 사각형, 재고 있음/담김은 배경 없음·품절만 회색 채움, 담기면 체크 아이콘)을 시안에 맞췄다. 머리말 오른쪽 아이콘을 공용 header 컴포넌트 방식(28px 아이콘+투명 터치 확장)으로 바로잡고, 탭도 전체를 가르는 회색 줄을 없애고 고른 탭 아래에만 1px 밑줄이 붙게 고쳤다. 담기 바텀시트(`ProductOptionSheet`)를 와이어프레임에서 시안대로 다시 그려 문구 크기·여백·불필요한 구분선을 정리했다. 남은시간과 상품 목록 사이 여백, 카드 좌우 여백도 시안 수치로 맞췄다. 오픈 예정 카드의 시안에 없던 장식용 아이콘을 지웠고, 빈 화면은 시안에 버튼이 없어 "상품 둘러보기" 버튼을 뺐다(버튼을 두는 편이 나을지는 프디팀 확인 중). 목록과 옵션 시트 가격이 같은 상품인데도 시안에서부터 서로 달라(24,000원/21,000원) 목록 값을 기준으로 삼았고, 어느 쪽이 맞는지는 프디팀 확인이 필요하다. QA 편의를 위해 화면 맨 아래에 빈 상태/상품 있는 상태를 오가는 개발용 버튼을 추가했다(시안에 없음) (#275)
- 타임딜 진행중 탭에서 상품 썸네일·이름을 누르면 상품 상세로 가도록 링크를 추가했다. 다른 상품 목록 화면과 같은 동작이다. 담기 버튼은 링크 밖에 겹쳐 둬 눌러도 상세로 새지 않는다 (#275)
- "오픈 알림 신청됨" 상태를 다시 누르면 신청이 취소되고 원래 버튼으로 돌아가도록 했다. 취소할 수 있게 되면서 "신청하면 초를 지켜볼 이유가 없다"는 전제가 깨져, 신청 후에도 카운트다운을 계속 보여주도록 함께 바꿨다 (#275)
- 좋아요 화면의 찜 탭을 UI 시안으로 교체했다. 뒤로가기 있는 `PageHeader`에 제목만 빼고, 빠져 있던 하단 네비게이션을 추가했다 (#274)
- PD 확인 결과에 맞춰 찜 탭 필터를 알림 상태 칩에서 상품 카테고리 칩(전체·사료·간식·영양제)으로 바꾸고, 찜 아이콘을 흰 원판+사진 오른쪽 위(4px)로 옮겼다. 찜 하트를 눌러 풀 때는 확인 모달 없이 바로 목록에서 빠지고, "최근에 봤어요"·"자주 샀어요" 탭은 이번 MVP 범위 밖이라 눌러도 반응하지 않는다 (#274)
- 메인 화면 "맞춤 추천" 섹션의 "더보기"로 들어가는 상품 추천 화면을 UI 시안으로 교체했다. 건강 고민 칩(관절·알러지·구강관리) 대신 상품 분류 탭(전체·사료·간식·영양제)으로 거르고, 정렬을 "추천순" 하나에서 4개(추천순·최신순·별점 높은순·별점 낮은순)로 늘려 URL에 남긴다. 안내 배너·별점 표기(별 1개+숫자+후기수)·찜 버튼·아이 선택 드롭다운도 시안에 맞췄다. 머리말 제목도 다른 화면 문구를 그대로 복사해 둔 "상품 둘러보기"에서 "맞춤 추천"으로 바로잡았다. 헤더·적합도 배지·하단 네비게이션 선택 탭은 시안과 다르게 유지했고 프디팀 확인이 필요하다 (#273)
- 검색 결과 화면이 목데이터 대신 실제 상품 검색 결과를 보여준다. 검색어·정렬을 서버에 그대로 넘겨 서버가 거르고 정렬한 결과를 받는다. 취소선 정가는 서버가 정가 값을 따로 주지 않아 이번엔 표시하지 않는다 (#282)
- 타임딜 화면의 목록·카운트다운이 실제 타임딜 데이터로 채워진다. 진행 중·오픈 예정 딜이 동시에 여러 개 있어도 전부 보여준다. 장바구니 담기·오픈 알림 신청은 아직 화면 안에서만 바뀌는 동작이다(백엔드 미착수) (#282)

## 2026-09-20

### 수정

- 타임딜 화면 1차 반영에서 놓친 것들을 다시 잡았다. 이미 담긴 딜을 다시 누르면 옵션 시트가 또 뜨던 것을 바로 빼지도록 고쳤고, 목록의 담기 아이콘이 진한 채움 도형이라 머리말 아이콘과 달라 보이던 것을 같은 선 아이콘으로 통일했다. 오픈 예정 문구를 "오전 10:00"에서 "오전 10시"로, 오타 "뵈요"를 "봬요"로 고쳤다. "오픈 알림 신청하기" 버튼의 종 아이콘 크기(20→24px)를 시안에 맞추고, 상품 목록 줄 간격이 늘어져 보이던 것을 시안대로 줄였다. 담기 바텀시트는 공용 `Drawer`의 손잡이가 시안보다 짧아 문구가 손잡이에 붙어 보이던 것을 위쪽 여백을 더해 보완했다 (#275)
- 타임딜 화면의 아이콘 색을 Figma 데브 모드 CSS 값으로 다시 맞췄다. 목록 담기 아이콘은 `icon/fill/secondary`, 담긴 상태 체크 아이콘은 `icon/fill/default`, "오픈 알림 신청하기"의 종 아이콘은 `icon/stroke/tertiary`로 각각 다른 값이었는데 전부 기본색 하나로 뭉뚱그려 놓았던 것을 바로잡았다. 빈 화면 제목도 `title/bold_18`이 아니라 `body/medium_18`(더 얇은 굵기)이 맞는 값이라 고쳤다 — 다른 화면들의 빈 상태 기본값과 다른 값이라 의도된 차이인지 프디팀 확인이 필요하다 (#275)
- 공용 스낵바(`shared/ui/snackbar`) 폭 관련 소동은 결국 코드가 아니라 확인 방법이 문제였다. 독립 컴포넌트만 내보낸 Figma 화면(1905-32431)이 문구 크기에 맞춰 잘려 보여 "문구 길이만큼만 차지하는 알약"으로 잘못 읽었는데, 실제 화면 전체(1905-32403)를 보면 좌우 20px 여백의 고정 폭·왼쪽 정렬 문구가 맞는 시안이었다. 그 사이 폭을 줄이거나 가운데 정렬을 넣는 시도를 몇 차례 했다가 전부 되돌렸다 — `showSnackbar`는 결국 처음 상태 그대로다 (#275)
- 타임딜 화면 리뷰로 눈에 보이는 문제 여럿을 다시 잡았다. 오픈 알림 신청 후에도 카운트다운을 계속 보여주도록 되돌리고, "신청됨" 버튼의 배경색·모서리·글자 굵기·여백을 신청 전 버튼과 완전히 같게 맞춰 상태를 오갈 때 버튼이 움찔거리던 것과 색이 다른 화면의 "선택됨" 표시와 달라 보이던 것을 없앴다. 진행중·오픈 예정 탭의 밑줄도 시안대로 1px로 얇게 고쳤다 (#275 리뷰)
- 오픈 예정 문구가 접속 시각에 따라 "오늘"·"내일"로 오락가락하던 것을 항상 "내일 오전 10시"로 고정했고, 개발용 QA 버튼이 운영 빌드에도 나가고 있던 것을 막았다 (#275 리뷰)
- PR 리뷰로 세 가지를 더 잡았다. 머리말 검색·장바구니 아이콘의 터치 영역이 44px 기준에 못 미쳐(37px) 넓혔다. 딜 자체의 id와 상세로 이동할 상품 id를 같은 값 하나로 쓰고 있던 것을, 백엔드가 이 둘을 별개 자원으로 두는 것에 맞춰 필드를 분리했다. 담긴 딜의 담기 버튼 문구가 "장바구니에 담김"으로 상태만 알려주고 있어, 눌렀을 때 실제로 일어나는 일(빼기)을 알리도록 고쳤다 (#275 리뷰)
- 맞춤 추천 화면의 정렬 드롭다운 터치 영역이 44px 기준에 2px 못 미쳤다(42px). 위아래로 넓히는 값을 늘려 46px을 채운다 (#273 리뷰)
- 좋아요 화면의 "최근에 봤어요"·"자주 샀어요"는 탭을 눌러도 반응하지 않게 막았지만, 주소로 `?tab=recent`를 직접 치면 비활성 탭에 들어가 지우기 동작을 쓸 수 있었다. 주소로도 못 들어가게 막고, 찜한 상품은 있는데 고른 카테고리에만 없는 경우와 찜 목록 자체가 빈 경우의 빈 상태 문구를 나눴다 (#274 리뷰)

## 2026-09-20

### 변경

- 상품 비교·검색 화면을 UI 시안으로 교체했다. 비교 카드에 적합도 우열(높은 쪽 강조)과 "장바구니 추가"·"맞춤 분석" 안내가 붙었고, 빈 자리의 "상품 추가하기" 버튼에 화살표가 붙었다. 검색에서 비교할 상품을 고를 때는 카드를 눌러 바로 이동하는 대신 체크하고 "선택 완료"로 확정한다 (#245)
- 그냥 검색할 때는 결과 카드에 적합도 배지 대신 찜하기를 넣었다. 어느 상품이 더 맞는지가 아니라 무엇을 담아 둘지가 그 화면의 목적이라서다. 검색 기본 화면의 제목·전체삭제 글자 크기와 최근 검색어 없음 안내도 시안에 맞췄다 (#245)

### 수정

- 화면 상단 공용 머리말(`PageHeader`)의 좌우 여백과 오른쪽 아이콘 사이 간격이 시안(20px·9px)보다 좁았다(`px-2`·`gap-1`). 상품비교뿐 아니라 이 컴포넌트를 쓰는 화면 전부에 있던 문제라 공용 컴포넌트에서 고쳤다 (#245)
- 비교 자리의 "상품 추가하기"·"장바구니 추가" 버튼 터치 영역이 44px 기준에 못 미쳤다. 보이는 높이(40px)는 시안대로 두고 누르는 자리만 위아래로 넓혔다 (#245 리뷰)
- 검색창에서 키보드 포커스 표시가 사라져 있었다. 클릭·입력 중 테두리가 생기지 않게 하면서 포커스 링까지 꺼 버려, 키보드로 접근하면 지금 어디 있는지 알 수 없었다. 테두리는 그대로 없애고 링으로만 표시한다 (#245 리뷰)
- 비교 화면 주소의 상품 id를 손으로 조작하면 두 자리가 같은 상품으로 겹칠 수 있었다. 정상 흐름은 만들지 않는 조합이지만 그대로 믿지 않고 반대쪽 자리를 비운다 (#245 리뷰)
- 아이 관리 사진 카드의 알레르기가 깨져 보이던 것을 고쳤다. 백엔드가 상세 조회의 `allergies`를 코드 배열에서 `{ code, displayName }` 배열로 바꿨는데 우리가 문자열로 읽고 있어, 알레르기를 고른 아이를 열면 `[object Object]`가 떴다. 표시명을 되찾으려고 선택지를 따로 받던 우회도 이제 필요 없어졌다 (#267)
- 상품 비교 화면에서 검색으로 자리를 채우고 돌아오면 반대쪽 자리가 사라지거나, 두 자리가 같은 상품으로 겹쳐 화면이 깨지는 경우가 있었다. 검색을 다녀오는 동안 반대쪽 자리 정보를 잃는 것이 원인이라 주소로 함께 들고 다니게 했다 (#245)
- 비교표에 상품을 반대 순서로 담으면 이름과 값이 서로 바뀌어 붙었다. 값의 두 열도 담긴 순서에 맞춰 뒤집게 했다 (#245)
- 비교 화면의 "맞춤 분석" 문구가 한쪽 상품의 적합도를 재지 못했을 때도 "비슷하다"고 표현했다. 미측정과 동점을 구분해 표현한다 (#245)
- 비교표가 어떤 상품을 골라도 특정 두 상품의 값을 그대로 보여줘, 고르지 않은 상품의 정보처럼 보일 수 있었다. 실제 값이 있는 조합에서만 표를 보여주고 나머지는 준비 중이라고 안내한다 (#245)
- 최근 검색어 칩의 삭제 버튼 탭 영역이 44px 기준에 못 미쳤다. 가로도 세로처럼 넓혔다 (#245)
- 영양 성분 이름과 막대 사이 여백이 시안(20px)보다 넓었다. 값 배지가 겹칠 자리를 미리 32px 비워 둔 것이 원인이라, 그 여백을 걷어내고 배지가 그 위에 그대로 겹쳐 뜨게 했다 (#229)
- 영양 성분의 부족/적정/과다 구간 표시에 어제 임의로 더했던 굵기 차이를 되돌렸다. 시안(1681-14278)을 다시 보니 세 글자 모두 굵기가 같고 색만 다르다 — 색 구분 자체가 부족한 것은 여전히 남은 문제라 디자인팀 확인이 필요한 항목으로 남긴다 (#229)
- 상세에서 비교하러 가면 항상 실패했다. 상세 상품(영양제)과 같은 종류를 검색에서 고를 수 없어 매번 "종류가 달라 비교할 수 없다"는 안내만 떴다. 영양제 후보와 전용 비교 항목을 추가했다 (#229)
- 상품 상세 곳곳의 터치 영역이 44px에 못 미쳤다. 옵션 시트 담기·상세설명 더보기/접기·비교하기 버튼(h-10, 40px)을 형제 버튼과 같은 44px로, 아이 전환 pill의 보이지 않는 여백도 42px에서 44px로 맞췄다 (#229)
- "배송 안내"·"교환/반품/환불 안내" 아코디언을 펼쳐도 내용이 없었다. 준비중 안내를 넣었다 (#229)
- 리뷰 사진 미리보기 4장이 360px 화면에서 폭을 넘었고, 눌러도 정확한 사진으로 가지 않았다 (#229)

## 2026-09-19

### 변경

- 상품 상세 메인 화면(제목·공유·배송정보·연관 상품·맨 위로 버튼)을 UI 시안으로 교체했다. 딜·품절 상태를 쿼리로 미리 볼 수 있게 했고, 맨 위로 버튼이 넓은 화면에서 본문 밖으로 벗어나던 것과 최하단 회색 여백이 빠져 있던 것을 함께 고쳤다 (#229)
- 적합도·영양성분·상품정보 패널을 UI 시안으로 교체했다. 영양 성분 막대의 2단 간격, 상세설명·제공고시 표의 여러 줄 정렬, 영양 성분 범례를 시안 값·자산으로 맞췄다 (#229)
- 상품·리뷰 엔티티(카드·리뷰 카드)를 상품 상세 시안에 맞춰 고쳤다. 상품 카드의 가격 크기와 찜 위치를 이미지 기준으로, 리뷰 카드의 "도움돼요" 아이콘을 등록된 세트로 바꿨다 (#229)
- 공용 `Switch`·`Slider` 컴포넌트를 상품 상세 리뷰 필터 시안에 맞췄다. 스위치의 좌우 여백을 대칭으로, 슬라이더의 구간(체중) 표시와 눈금 위치를 값 비례로 바로잡았다 (#229)
- 상품 상세에서 비교 화면으로 이어지는 흐름을 연결했다. 비교하러 오면 상세 상품을 첫 자리에 고정하고, 두 번째 상품을 검색으로 고르러 갔다 돌아와도 첫 자리가 유지된다 (#229)

### 수정

- 주문 완료 화면이 **승인이 돌려준 결제일시**를 보인다. 그전에는 `26.08.28 15:43`이 고정으로 떠서 오늘 결제해도 지난 날짜가 나왔다. 서버가 주지 않는 도착 예정일은 결제하기와 마찬가지로 걷어냈다 (#262)
- 결제 승인이 막히면 **주문번호와 함께 그 사실을 알린다.** 그전에는 라우트 오류 화면이 나서 결제가 어떻게 됐는지도, 무엇으로 문의해야 하는지도 알 수 없었다. 결제창에서는 이미 성공한 뒤라 돈이 빠져나갔을 수 있는 자리다. 다시 결제하러 가는 길은 주지 않는다 (#260)
- 상품 상세 기본 상태가 품절로 고정돼 있었다. 정상·타임딜·품절 세 상태를 하나씩 눈으로 확인하려고 잠깐 품절로 둔 값이 QA가 끝난 뒤에도 그대로 남아, 기본 화면에 들어가면 장바구니·바로 구매 버튼 대신 재입고 알림만 보였다 (#229)
- 영양 성분의 부족/적정/과다 구간이 화면 낭독기에 전혀 닿지 않았다. 그 줄이 `aria-hidden`인데 값 배지에도 구간 이름이 없었다. 배지의 접근성 이름에 구간을 함께 실었다(`"12%, 과다"`) — 색으로만 구분하는 시안 자체는 그대로 두고, 화면 낭독기가 놓치던 정보만 채웠다 (#229)

---

### 개발 환경

- 결제창 복귀(`?code=`)와 장바구니가 넘긴 고른 줄(`?items=`)을 화면 테스트로 고정했다. 모킹이 빈 쿼리로 굳어 있어 두 경로를 한 번도 타지 않았다 (#262)
- 상품 상세 e2e·README가 확정 UI 시안을 따라가도록 정리했다. 사진 리뷰 헤더 제목, 영양 배지 표기 방식이 바뀐 뒤에도 옛 값을 그대로 검증하고 있었다 (#229)

## 2026-09-18

### 추가

- 마이페이지가 실제 내 정보를 보인다. 홈 프로필 카드의 닉네임·이메일과 내 정보 화면의 회원 정보 다섯 줄이 `GET /members/me`에서 오고, 같은 화면의 아이 이름과 배송지는 각각 `GET /members/me/pets`·`GET /members/me/addresses`에서 온다. 그전까지는 누가 로그인하든 `졸린고양이 17`과 남의 이메일이 떴다. 이름·생년월일은 서버가 주지만 받는 자리가 화면에 없어 늘 비어 오므로 `등록 전이에요`로 알린다 (#266)
- 닉네임을 고치면 서버에 남는다. **고칠 것만 보낸다** — `PATCH /members/me`가 전 필드를 선택으로 받아서, 이름·생년월일까지 실으면 이 화면이 고치지도 않은 값을 덮어쓴다 (#266)
- 설정의 회원탈퇴가 동작한다. 되돌릴 수 없어 한 번 묻고 보낸다. **실패하면 기기의 토큰을 지우지 않는다** — 로그아웃과 다른 점이다. 계정이 살아 있는데 토큰만 비우면 쫓겨난 채로 탈퇴됐는지도 알 수 없다 (#266)
- 결제 화면이 실제 장바구니와 배송지를 보여준다. 그전에는 배송지도 상품도 하드코딩이라 누가 무엇을 사든 같은 화면이 떴다. 결제하기를 누르면 **주문 생성 → 결제 요청 → 결제창**이 한 번에 이어진다 (#255)
- **장바구니에서 고른 것만 결제된다.** 고른 줄을 `?items=`로 넘긴다 — 그전에는 셋 중 하나만 골라도 결제 화면이 장바구니 전체를 세었다 (#255)
- 배송지는 **기본 배송지**를 쓴다. 회원가입·온보딩 어디에도 배송지를 받는 자리가 없고 시안도 배송지 설정에 확정 버튼 대신 `기본 배송지` 뱃지만 두었다. 등록된 곳이 없으면 등록하러 보낸다 (#255)
- 결제창이 열리기까지 두 왕복이 걸려 버튼이 대기 중임을 알린다. 결제는 되돌릴 수 없어 두 번 눌리면 안 된다 (#255)

- 결제 승인이 목을 떠나 실제 엔드포인트(`POST /payments/confirm`)로 나간다. 주문 생성과 결제 요청 API도 함께 만들어 다섯 단계가 코드로는 이어졌다. 이 단계에서는 화면이 아직 목데이터였고, 실제 장바구니·배송지에 잇는 것은 같은 날 이어서 했다(위 항목) (#255)
- 주문 생성에 `Idempotency-Key`를 실어 보낸다. 결제 버튼을 연타하거나 네트워크가 끊겨 재시도가 일어나도 주문이 두 건 생기지 않게 서버가 알아볼 수 있다 (#255)
- 설정에서 로그아웃하면 서버의 세션까지 끊는다. `POST /auths/logout`이 accessToken을 블랙리스트에 올리고 저장된 refreshToken을 지운다. **기기에서 토큰만 지우면 서버의 refreshToken이 살아 있어 그것을 쥔 쪽은 계속 재발급을 받는다.** 그전까지 "로그아웃" 줄은 눌러도 아무 일도 없는 자리였다. 요청이 실패해도 기기의 토큰은 지운다 — 서버 정리에 실패했다고 막으면 남의 기기에서 빠져나올 방법이 없어진다 (#247)
- 휴대폰 인증이 실제 API를 탄다. **문자는 여전히 가지 않는다** — 발송이 건당 과금이고 무료 지원은 사업자등록이 필요한데 우리는 없다. 서버도 같은 방침이라 인증번호가 고정값이어서, 받은 것처럼 채워 주되 발송·확인은 서버가 한다. 이제 만료 180초와 재시도 제한(발송 1시간 5회, 확인 5분 5회)이 실제로 걸리고, 넘으면 까닭을 알린다 (#247)
- 없는 주소로 들어가면 우리 화면이 난다. 그전에는 Next.js 기본 화면이 그대로 나서, 오타나 만료된 링크로 들어온 사람이 영문으로 된 낯선 화면을 마주쳤다. 무엇이 잘못됐는지 알리고 홈으로 돌아가는 길을 준다 (#243)

- 버튼을 눌러 응답을 기다리는 동안 대기 중임을 알린다. 버튼은 **안의 라벨만**, 페이지네이션은 **셰브론 아이콘 자체**를 스피너로 바꾸되 **자리(폭·높이)는 그대로 지킨다** — 내용을 걷어내면 버튼이 줄었다 늘어난다. 색은 부모의 글자색을 따라가서 `default`(흰색)·`outline`(본문색)·`destructive`(빨강) 어디에 넣어도 따로 지정할 것이 없다. 스크린 리더에는 "처리 중"으로 읽히고, 조회를 기다리는 자리는 문구를 바꿔 넣는다. 화면이 처음 그려질 때의 대기는 지금처럼 Skeleton이다 (#231)
- 마이페이지가 실제로 등록한 아이를 보인다. 아이 관리(`/mypage/pets`)의 사진 카드와 아이 전환 줄, 마이페이지 홈 프로필 카드의 원 줄이 `GET /members/me/pets`·`GET /members/me/pets/{petId}`에서 온다. 그전까지는 온보딩 여섯 단계를 채워 등록을 마쳐도 화면에는 "코코"·"보리"가 떴고, 아이를 바꿔도 카드 내용은 그대로였다 (#230)
- **기본 아이가 처음 고른 아이다.** 백엔드 목록에 `ORDER BY`가 없어 순서가 DB에 달렸다. 순서가 흔들리면 눌렀던 자리가 매번 달라지므로 `isDefault`를 앞으로 올려 조회가 정렬해 준다 (#230)
- 아이가 하나도 없으면 카드 대신 등록하러 가는 자리를 보인다. 빈 카드만 두면 고장으로 읽힌다. 불러오는 중과 실패도 문구로 알린다 (#230)
- 메인·리뷰 작성도 같은 목록을 쓰지만 이번에는 붙이지 않았다. `useQueryPets`·`useQueryPetDetail`만 내어 두고 각 화면 담당이 붙인다 (#230)
- 온보딩 마지막 단계의 "작성 완료"가 등록을 기다리는 동안 대기 중임을 알린다. 그전에는 `disabled`만 되어 왜 안 눌리는지 알 수 없었고, 등록 왕복이 길어 두 번 눌러 두 마리가 등록될 여지가 있었다 (#234)
- 주소 검색 페이지네이션에서 **누른 셰브론만** 스피너로 바뀐다. 양쪽을 다 돌리면 어느 방향으로 가는 중인지 알 수 없어 누른 쪽을 기억한다 (#234)

- 배송지가 서버에 저장된다. `/mypage/address/new`에서 넣으면 실제로 남고, 결제 흐름의 배송지 설정(`/payment/address`)이 저장해 둔 곳을 보여준다. 그전에는 다 채우고 눌러도 나가는 것이 없어 주문할 때마다 주소를 처음부터 다시 적어야 했다 (#237)
- 저장해 둔 배송지를 고쳐서 다시 저장할 수 있다. 목록에서 줄을 누르면 그 값이 채워진 채 열린다 (#237)
- **기본 배송지가 목록 맨 위에 온다.** 명세가 순서를 약속하지 않는데, 고르는 화면은 맨 위가 기본값처럼 읽혀서 순서가 흔들리면 눌렀던 자리가 매번 달라진다 (#237)
- 배송지를 아직 넣지 않았으면 빈 화면 대신 넣으라고 알린다. 불러오지 못했을 때도 그 사실을 화면에 남긴다 (#237)

### 변경

- PD팀이 아이콘 세트에 다섯을 더해 46종에서 **51종**이 됐다(`bag`·`404`·`reload`·`pencil`·`refresh`). 우리가 "세트에 없다"며 시안에서 따오거나 react-icons로 채워 쓰던 자리를 전부 세트로 바꿨다 — 장바구니 빈 상태의 가방, 404 화면, 오류 화면이 그렇다 (#257)
- 후기 "작성 가능" 탭의 빈 상태에 연필 그림이 붙었다. 세트에 없어 비워 뒀던 자리다 (#257)

- 토스트가 PD snackbar 시안을 따른다. 성공은 연녹색, 실패는 연분홍, 그 밖은 어두운 바탕으로 나뉘고 모서리도 시안대로 5px다. 그전에는 셋 다 같은 모양이라 성공인지 실패인지 색으로 구분되지 않았다. 색은 셋 다 우리 토큰과 값이 같아 변수만 이어 줬다 (#253)

- 화면이 통째로 실패했을 때 뜨는 오류 화면을 PD 새 시안으로 바꿨다. 404와 같은 레이아웃이고 버튼은 [다시 시도하기] 하나다 — 헤더와 하단 탭이 살아 있어 홈으로 나가는 길은 남는다. 처음 올라온 시안은 다이얼로그였는데 전체 화면으로 다시 나왔다 (#248)

- 빈 상태가 PD 공용 시안을 따른다. 그림이 48px에서 **72px**로 커지고 화면마다 다른 그림이 붙는다 — 장바구니는 가방, 주문은 트럭, 찜은 하트처럼 무엇이 비었는지 그림으로 먼저 읽힌다. 문구도 시안 문장으로 맞췄다. 그전에는 21개 화면이 우리가 임시로 정한 18가지 문구를 쓰고 있었고 그림은 7곳에만 있었다 (#242)
- 시안의 그림은 대부분 **디자인 시스템 `icon` 세트를 72px로 키운 것**이라 그대로 쓴다. 트럭 path를 뽑아 대조해 확인했다. 세트 밖은 장바구니의 가방 하나뿐이어서 그것만 따로 옮겼고, 색이 `currentColor`라 다크 모드도 토큰이 따라온다 (#242)
- 후기 "작성 가능" 탭의 연필은 세트에 없어 그림을 비워 두었다. PD팀에 추가를 요청한다 (#242)

- 대기 표시 규칙을 세웠다. **화면을 처음 그릴 때는 Skeleton, 이미 그려진 UI가 응답을 기다릴 때는 LoadingSwap, 낙관적 갱신을 쓰는 자리에는 아무것도** 넣지 않는다. `AGENTS.md` 5.8과 `component-convention.md`의 "대기 상태" 절에 적었고, `.tsx`에 대기 상태가 있는데 `LoadingSwap`이 없으면 알려 주는 훅을 뒀다 — 막지는 않는다. 낙관적 갱신처럼 일부러 넣지 않는 자리는 `대기 표시 없음 — 사유` 주석으로 끈다 (#234)

- 리뷰 필터 바텀시트를 와이어프레임에서 UI 시안으로 교체했다. 시트를 전체폭 서랍에서 좌우 8px 여백의 카드형(`BottomSheet`)으로 바꾸고, 탭을 밑줄 스타일로, 종·중성화 칩을 완전한 필 모양으로, 버튼을 시안 색으로 맞췄다. 사용 기한·나이 슬라이더는 손잡이 하나짜리 최솟값으로, 체중만 손잡이 둘짜리 구간으로 모델을 다시 나눴다 (#229)
- 리뷰 탭 본문(별점 요약·리뷰 사진·리뷰 카드)을 UI 시안 타이포·간격으로 교체했다. 별점 요약을 왼쪽 정렬 큰 글자로, 리뷰 사진을 고정 80px 정사각형으로, 리뷰 카드의 배지·본문·"도움돼요" 버튼을 시안 값으로 맞췄다 (#229)
- Q&A 탭을 UI 시안으로 교체했다. "상품 문의"·"배송 문의" 버튼을 채워진 스타일로, 답변 상태 문구 색을(답변완료는 초록색으로) 시안에 맞췄고, 문의가 없을 때 보여줄 안내도 시안의 그림·문구로 반영했다 (#229)
- 사진 리뷰 모음 화면과 상세 뷰어를 UI 시안으로 교체했다. 격자에 좌우 여백을 두고, 뷰어 하단 버튼을 찜·장바구니·바로구매 세 개로 늘렸다. 찜은 실제로 토글되고, 장바구니·바로구매는 상품 상세로 이동한다 (#229)

### 수정

- 그림자 토큰 XS·L·XL이 시안보다 진하던 것을 맞췄다. 지금 쓰는 자리가 `shadow-sm`·`shadow-md` 다섯 곳뿐이고 그 둘은 원래 맞아서 겉모습은 그대로지만, 나중에 나머지를 쓰기 시작하면 그대로 어긋난다 (#250)
- **인증한 번호와 다른 번호로 완료할 수 있던 것을 고쳤다.** 인증을 "했다/안 했다"로만 들고 있어서, `010-1111`로 인증한 뒤 `010-2222`로 고쳐도 완료 버튼이 켜진 채로 남았다. 어느 번호를 인증했는지 들고 지금 번호와 견준다 (#247 리뷰)
- 숫자가 아닌 인증번호를 그대로 보내던 것을 고쳤다. `inputMode="numeric"`은 붙여넣기를 막지 않아 `12ab`가 들어오면 `NaN`이 되고, JSON이 그것을 `null`로 적어 서버가 본문을 통째로 거절했다 (#247 리뷰)
- 인증번호가 맞지 않을 때 아무 말도 없던 것을 고쳤다. 서버가 200에 `verified: false`로 답해서 화면이 조용히 넘어가지 않기만 했다 — 왜 안 넘어가는지 알 수 없었다 (#247)
- 담당 화면 넷에 빠져 있던 대기 표시를 채웠다. 회원가입 제출 버튼은 `disabled`만 걸려 있어 "안 눌린다"만 말하고 "처리 중"은 말하지 않았다 — 가입은 토큰 교체까지 도는 왕복이라 왜 안 눌리는지 모른 채 다시 누르게 된다. 아이 관리 사진 카드·마이페이지 홈의 아이 원 줄·품종 목록 셋은 화면을 처음 그리는 자리라 `Skeleton`으로 자리를 잡는다. 문구만 두면 카드가 통째로 비었다가 채워지고, 원이 없다가 생겨 점선 `+`가 오른쪽으로 밀리고, 검색창 아래가 갑자기 수십 줄로 찬다 (#238)
- **세션이 끝나면 어느 화면에서든 로그인으로 보낸다.** `apiRequest`가 재발급까지 실패하면 토큰을 지우고 401을 던지는데, 그 뒤를 아무도 맡지 않아 목록이 조용히 비어 보였다. 보호자는 왜 안 되는지 알 수 없다. `shared/api/README.md`가 정해 둔 `subscribeTokensCleared` 계약을 `AppProviders`가 구독한다 (#230)
- 세션이 끊긴 것을 조회 실패로 알리던 것을 고쳤다. `accessToken`은 메모리에만 있어 새로고침하면 사라지고 재발급 엔드포인트(`POST /auths/token/refresh`)가 아직 없어 되살릴 수 없다. "아이 정보를 불러오지 못했어요"로 두면 다시 눌러 볼 수 없는 것을 다시 누르게 된다. 401이면 로그인으로 보낸다 — 가입 화면이 세션 없는 진입을 막는 것과 같은 판단이다 (#230)
- 주소 검색에서 **다음 쪽으로 한 번에 넘어가지 않던 것**을 고쳤다. 한 번 더 눌러야 넘어갔다. `keepPreviousData`로 앞 결과를 그대로 두는 동안 `result`가 앞 쪽의 것인데, 범위 밖 쪽을 바로잡는 effect가 그 쪽 번호를 근거로 읽어 방금 누른 이동을 도로 되돌렸다. 두 번째 클릭이 되던 것은 첫 클릭에서 받아 온 응답이 캐시에 남아서다. `역삼동`은 결과가 4,747건인데 첫 4개밖에 볼 수 없었다 (#235)

### 개발 환경

- 장바구니 조회가 `entities/cart`로 내려갔다. 결제 화면이 같은 것을 써야 하는데 화면끼리는 서로를 부를 수 없다 (#255)

## 2026-09-17

### 추가

- 온보딩이 입력만 받고 끝나지 않는다. 마지막 "작성 완료"에서 `POST /members/me/pets`로 아이 프로필이 서버에 등록된다. 그전까지는 초안이 기기에만 남아 다음에 들어오면 아무것도 없었다. 등록에 실패하면 초안을 지우지 않고 완료 화면으로도 보내지 않는다 — 지우면 여섯 단계를 처음부터 다시 채워야 하고, 보내면 등록되지 않았는데 됐다고 알리는 셈이다 (#226)
- 품종 목록을 `GET /pets/breeds`에서 받는다. **이름이 아니라 서버가 준 id로 다룬다** — 등록 API가 `breedId`를 필수로 받아 이름 문자열로는 요청을 만들 수 없다. 하드코딩 58종을 걷어냈고, 우리 목록의 오타(`베드리턴 테리어` → `베들링턴 테리어`)도 함께 정리됐다. API가 종을 필수로 받아 강아지·고양이를 따로 부른다 — 품종 화면은 두 종을 한 번에 보이고 검색도 양쪽을 훑기 때문이다 (#226)
- 건강 고민·알레르기를 `GET /pets/health-options`에서 받는다. 갈래도 항목도 종마다 다르다. **알레르기는 코드로 저장한다** — 서버가 `{ code, displayName }`을 주므로 화면에는 표시명을 보이고 저장은 `CHICKEN` 같은 코드로 한다. 표시명이 바뀌어도 저장된 값이 깨지지 않는다. #123이 기다리던 계약이 이것이다 (#226)
- **품종을 바꿔 종이 달라지면 앞서 고른 질환과 알레르기를 함께 비운다.** 알레르기는 종별로 갈리는 코드가 있어(고양이 전용 `BONITO`, 강아지 전용 `INSECT`) 그대로 두면 새 종에 없는 코드를 등록 요청에 실어 보낸다 (#226)
- **선택지를 못 받으면 넘어가지 못하게 막는다.** 조회가 실패했을 때 빈 목록으로 두면 고를 것이 없어 "해당 사항이 없어요" 둘을 켜고 그대로 등록된다 — 고민도 알레르기도 없다고 답한 셈이 된다. 왜 못 고르는지 알리고 제출을 잠근다 (#226)
- 나이·몸무게·생년월일 입력을 걸러 받는다. 나이는 등록에 필수라 비면 다음 단계로 가지 못한다. 숫자가 아닌 글자는 아예 들어가지 않고(몸무게는 소수점 하나까지), 생년월일은 치는 대로 `0000. 00. 00`으로 맞춰진다. `2003. 10. 92`처럼 달력에 없는 날은 입력칸 아래에 알리고 다음 단계로 못 가게 막는다. **잘못 적은 칸을 조용히 빼고 보내면 사용자는 적었으니 저장된 줄 안다** (#226)

### 변경

- 메인 화면(`/`)을 와이어프레임에서 UI 시안으로 교체했다. 헤더 아이콘·탭, 배너, 아이 전환 줄, "AI가 골라주는 맞춤 상품"·"최근에 구매한 상품"·"타임딜 특가" 세 가로 목록, 사료·간식·영양제 탭의 2열 격자와 정렬 드롭다운까지 색·타이포·간격을 시안 값으로 맞췄다. 정렬 드롭다운은 실제로 상품 목록을 다시 늘어놓게 연결했고, 아이 전환 줄의 "+" 버튼은 온보딩 기본 정보 단계로 잇는다 (#185)
- 가로 목록 세 개(맞춤 상품·최근 구매·타임딜)의 카드 폭을 시안 고정값(208px·322px·160px)으로 맞췄다. 셋 다 왼쪽 20px 여백만 두고 오른쪽은 화면 끝까지 카드가 삐져나오게 해 더 스크롤할 게 있다는 걸 알린다 (#185)
- 하단 네비게이션 바를 UI 시안으로 교체했다. 아이콘을 디자인 시스템 세트로 바꾸고 28px로 키웠고, 누르는 동안 배경이 살짝 채워지는 pressed 상태를 hover 대신 넣었다. 홈·상품비교·마이페이지는 채워진 아이콘이 세트에 없어 아웃라인으로 남겨 뒀다 (#185)
- 반응 시트(`ProductFeedbackSheet`)를 화면별로 다른 시안에 맞게 나눴다. `variant="full"`(메인 상태 체크)은 제목 16px Medium·버튼 44px·둘째 배지 아웃라인으로, 기본값 `variant="floating"`(마이페이지 반응 시트)은 기존 제목 18px Bold·버튼 40px·둘째 배지 채움 그대로 남겼다. 같은 컴포넌트를 공유하다 보니 한쪽 시안에 맞춘 값이 다른 쪽에도 번져 있던 걸 바로잡았다 (#185)
- `PetSwitcher`에 `variant="main"`(60px, 원 안쪽 테두리)을 더했다. 리뷰 작성·마이페이지 기준으로 정해진 `default`(48px, 원 밖 링) 값과 메인 화면 값이 서로 달라 하나의 기본값으로 못 묶었다 (#185, #203)
- `Badge`에 `variant="outline"`을 더했다. 색은 톤(tone)으로, 채움·테두리 모양은 variant로 나눠 시안의 state=weak/line 조합을 표현한다. 지금은 default 톤의 아웃라인 테두리색만 시안으로 확인됐다 (#185)

### 수정

- 검색·추천 결과에서 적합도를 재지 못한 상품에 "정보 확인 중"을 다시 보여준다. 메인 화면 시안에 맞춰 배지를 단일 스타일로 정리하면서 이 표시(#119)가 같이 빠졌던 걸 되살렸다 (#185)
- 반응 시트의 미선택 반응 아이콘 배경을 `bg/secondary`에서 `surface/secondary`로 바로잡았다. 두 변수는 라이트에서 거의 같은 값이라 구분이 안 됐지만, 다크에서는 `bg/secondary`가 시트 배경(`bg/default`)과 같은 값이라 배경이 없는 것처럼 보였다 (#185)
- 홈 헤더의 검색·알림·장바구니 아이콘 터치 영역을 겹치지 않게 고쳤다. 44px로 넓히는 보이지 않는 영역이 9px 간격보다 커서 옆 아이콘 자리까지 침범해 잘못 눌릴 수 있었다 (#185)
- 반응 시트(`variant="full"`) 닫기 버튼의 터치 영역을 40px에서 44px로 보이지 않게 넓혔다 (#185)
- 공용 `DrawerTitle`의 기본 글자 굵기를 `font-medium`으로 되돌렸다. 반응 시트는 이미 각자 굵기를 명시해 영향이 없었는데, 카드 관리 등 다른 화면의 제목만 의도치 않게 굵어져 있었다 (#185)
- 타임딜이 끝난 뒤 "오픈 알림 받기" 버튼이 눌러도 반응이 없었다. 타임딜 화면(`DealsView`)과 같은 방식으로 로컬 상태를 두어 누르면 "오픈 알림 신청됨"으로 바뀌게 했다 (#185)
- 정렬 드롭다운에서 고른 항목을 배경색만으로 구분하던 것에 글자 굵기 표시를 더했다 (#185)
- 생년월일에 달력에 없는 날을 적으면 등록이 통째로 막히던 것을 고쳤다. `2003-10-92`가 그대로 나가면 서버가 `LocalDate`로 읽지 못해 본문 전체를 거절한다(`Failed to read request`). 생일 한 칸 때문에 다른 값이 다 멀쩡해도 등록이 안 됐다. 입력 단계에서 알리고 막되, 초안이 기기에 남아 `?step=health`로 바로 들어오면 그 가드를 거치지 않으므로 등록 직전에 한 번 더 본다 — 적었는데 못 알아들은 생일은 빼고 보내지 않고 고칠 칸으로 돌려보낸다. 아예 안 적은 생일은 선택이라 그대로 뺀다 (#226)
- 고른 알레르기가 `CHICKEN`처럼 코드로 보이던 것을 표시명으로 되돌렸다. 저장값을 코드로 바꾸면서 되보이는 자리를 놓쳤다 (#226)
- 몸무게를 `.5`로 적으면 `5`로 읽히던 것을 고쳤다. 0.5kg 고양이가 5kg으로 저장된다 (#226)
- 몸무게에 소수점을 두 번 이상 치면 `4.2.3`이 `4.23`으로 이어 붙던 것을 고쳤다. 사용자가 적지 않은 몸무게가 된다 (#226)

## 2026-09-16

### 추가

- 소셜 로그인을 백엔드에 연결했다. 로그인 화면의 카카오·구글 버튼이 게이트웨이 인가 시작 주소로 나가고, 인증을 마치면 백엔드가 `/auth/callback`으로 일회용 code를 넘긴다. 새로 만든 복귀 화면이 그 code를 `POST /auths/token/exchange`로 바꿔 토큰을 보관한 뒤 다음 화면으로 보낸다. **리다이렉트로 토큰을 직접 받지 않는 이유는 주소창·리퍼러·브라우저 기록에 남기 때문이다** — code는 60초짜리 일회용이다 (#215)
- **가입 화면으로 보낼지는 신규 회원 여부가 아니라 `needsSignup`으로 가른다.** 첫 로그인에서 닉네임·약관을 끝내지 않고 이탈한 사람은 다시 로그인해도 더 이상 신규가 아니어서, 신규 여부로 가르면 가입 화면에 영영 닿지 못한다. 백엔드가 그래서 응답에서 `isNewUser`를 빼고 `needsSignup`을 넣었다 (#215)
- 회원가입 제출을 붙였다. 약관 다섯 종을 백엔드 enum(`AGE_OVER_14`·`SERVICE_TERMS`·`PRIVACY_COLLECTION`·`MARKETING_BENEFIT`·`THIRD_PARTY_PROVIDE`)으로 옮겨 보내고, **고르지 않은 항목도 `agreed: false`로 함께 싣는다** — 빠진 항목과 거절한 항목은 다른 사실이고 선택 약관은 거절 기록 자체가 남아야 한다 (#215)
- **가입을 마치면 토큰을 갈아끼운다.** `memberId`는 가입을 끝내야 생기는 값이라 로그인 직후 받은 JWT에는 들어 있지 않다. 백엔드가 가입 응답으로 `memberId`가 담긴 새 토큰 쌍을 주므로 그것으로 바꾼다. 바꾸지 않으면 이후 회원 API가 누구의 요청인지 알지 못한다 (#215)
- **소셜 인증 없이 회원가입 화면에 들어오면 로그인으로 돌려보낸다.** 가입 요청은 토큰의 `authId`로 누구의 가입인지 알기 때문에, 토큰 없이 들어오면 약관과 닉네임을 다 채운 뒤에야 401을 만난다. 들어오는 자리에서 막아 그 헛걸음을 없앴다 (#215)
- 닉네임 단계가 백엔드의 추천 닉네임으로 미리 채워진다. 복귀 화면이 교환 응답의 값을 `/signup?nickname=`으로 넘긴다. `GET /members/me`가 아직 없어 가입 화면이 스스로 조회할 수 없다 (#215)
- `NEXT_PUBLIC_OAUTH_BASE_URL`을 더했다. **소셜 로그인 시작 주소만 `/api/v1`이 아니다** — 게이트웨이가 일반 API와 별개 라우트로 `/api/auth/oauth2/authorization/**`를 받으므로 `NEXT_PUBLIC_API_BASE_URL`에서 파생할 수 없다. 비우면 same-origin을 쓴다 (#215)
- 장바구니(`/cart`)가 목 대신 서버가 준 것을 그린다. 담은 것을 `GET /carts`로 가져오고, 수량 변경과 빼기가 서버에 남아 **새로고침해도 유지된다**. **수량은 바뀐 값이 아니라 증감(`{ delta: ±n }`)을 보낸다** — 스테퍼는 바뀐 값을 들고 있어 화면이 이전 값과의 차를 계산한다. 증감이라 연달아 눌러 요청이 여러 번 나가도 서버에서 합쳐지고, 그래서 응답을 기다리지 않고 먼저 그린 뒤 실패하면 되돌린다 (#214)
- **살 수 없는 줄이 보인다.** `available: false`인 줄은 이름·사진·금액이 전부 비어 오고 못 사는 까닭만 온다. 고를 수 없게 막고 까닭을 이름 자리에 둔다 — **없는 이름을 지어내면 사용자가 그것을 상품명으로 읽는다.** 빼기는 남겨 둔다. 지울 길이 없으면 장바구니에 계속 걸린다. 이 상태는 시안(`cart_001`)에 없어 새로 그리지 않고 있는 것만 썼다 (#214)
- 결제하기(`/payment`)에 토스 결제위젯을 붙였다. 결제 방법 자리를 위젯이 그리고 결제하기를 누르면 실제 결제창이 뜬다. 우리가 만들던 결제수단 라디오는 걷어냈다 — 카드·간편결제 분기와 할부 개월은 위젯이 갖고 있고 카드 정보 입력창은 아예 토스가 띄우는 별도 창이다. **승인은 우리가 하지 않는다** — 승인 API는 시크릿 키로 인증하는데 그 키가 브라우저에 닿으면 누구나 결제를 승인할 수 있어 백엔드가 맡는다. 우리는 결제창을 띄우고 돌아온 값을 완료 화면으로 넘기는 데까지다. **어느 엔드포인트가 승인을 맡는지는 아직 정해지지 않았다** (#212, #217)
- Redirect 방식이라 결제가 끝나면 브라우저가 돌아온다. 성공은 `/payment/done`으로 `paymentKey`·`orderId`·`amount`를 달고 오고 완료 화면이 그 값으로 승인을 부른다. 실패·취소는 `/payment`로 `code`를 달고 와 토스트로 왜 돌아왔는지 알린다 — 시안에 실패 화면이 없다. **창을 띄우기도 전에 막히는 실패는 되돌아오지 않으므로** 결제 버튼에서 직접 받아 같은 토스트로 알린다. 놓치면 눌러도 아무 일이 없어 보인다 (#212)
- `NEXT_PUBLIC_TOSS_CLIENT_KEY`를 더했다. **백엔드가 쓰는 테스트 상점의 결제위젯 클라이언트 키(`test_gck_`)를 받아 넣는다.** 클라이언트 키와 승인용 시크릿 키는 한 상점의 세트라, 짝이 안 맞으면 결제창은 떠도 승인에서 `INVALID_API_KEY`가 난다. 값이 없으면 결제 방법 자리에 안내 문구만 남고 결제 버튼은 잠긴 채다 (#212)

### 변경

- 결제하기(`/payment`)를 와이어프레임에서 UI 시안(`paym_001` 계열)으로 교체했다. 좌우 여백이 20px로 넓어지고 섹션 제목이 18px로 커진다. 배송지 정보는 이름 칸 52px에 값이 바로 옆에 붙고, 배송 요청사항은 라벨 아래 44px 칸으로 내려온다. 상품 썸네일이 80px가 되고 주문 수량이 이름과 값으로 갈린다. 결제하기 버튼은 48px다. **결제 방법 자리는 시안이 라디오 다섯과 페이 로고 셋을 그렸지만 그리지 않는다** — 그 자리는 토스 결제위젯이 차지한다 (#222)
- 약관 동의 문구에 대괄호가 붙는다 — `[전체 동의]`. 필수·선택 줄은 이미 그랬다 (#222)
- 배송지 설정(`/payment/address`)과 주문 완료(`/payment/done`)를 와이어프레임에서 UI 시안(`paym_011`·`paym_002`)으로 교체했다. 배송지 설정은 아이콘이 디자인 시스템 것으로 바뀌고 집·회사와 사용자가 더한 장소를 구분선 하나로 가른다. 주문 완료는 흰 바닥에 요약 카드 하나만 떠 있고 결제상세·배송지는 카드 없이 놓인다 (#210)
- 결제상세·배송지 블록을 `entities/order`로 올렸다. 주문 상세(`mypa_161`)와 주문 완료(`paym_002`)가 타이포·간격까지 같은 블록을 쓴다. **카드 껍데기는 조각이 갖지 않고 쓰는 쪽이 얹는다** — 한쪽은 회색 바닥 위 흰 카드, 다른 쪽은 흰 바닥에 그대로다 (#210)
- 주문 완료의 요약 카드 그림자를 `shadow-sm`으로 옮겼다. 시안은 `0 0 11.35px` 한 겹인데 토큰 다섯 단계는 모두 두 겹에 y 오프셋이 있어 맞는 값이 없다. blur가 가장 가까운 것을 골랐고 PD팀 확인이 필요하다 (#210)
- 주문 상세(`/mypage/orders/[orderId]`)를 와이어프레임에서 UI 시안(`mypa_161`·`mypa_161_배송완료`)으로 교체했다. 회색 바닥 위에 흰 카드 셋이 8px 간격으로 놓이고, 제목이 20px로 커지며, 상품 줄이 목록과 같은 2단 구조가 된다. 반품·교환은 주문정보 카드 안쪽 구분선 아래에서 48px로 나란히 온다 (#205)
- **시안에서 빠진 것을 뺐다.** 결제상세의 "포인트 할인"과 화면 아래 링크 세 줄(`주문 취소`·`리뷰 작성`·`문의하기`)이다. 주문 취소는 주문 목록에 있고 나머지는 시안에 자리가 없다 (#205)
- 주문 상품 줄을 `entities/order`로 옮겼다. 목록과 상세가 같은 줄을 쓰는데 `views` 안에 두면 같은 레이어끼리 참조하게 되어 ESLint가 막는다 (#205)
- 주문 상세가 `shared/ui`의 `DetailCard`·`DefinitionRow`를 쓰지 않는다. `DetailCard`는 내용을 `<dl>`로 감싸는데 시안의 주문정보 카드에는 주문번호 쌍 옆에 상품 줄과 버튼이 함께 들어가고, `DefinitionRow`는 이름 칸이 고정폭에 타이포가 한 벌인데 시안은 자리마다 다르게 쓴다. 화면 폴더에 `DetailSection`·`DetailRow`를 두었다 (#205)
- **결제 완료(`paym_002`) 시안에는 카드가 없다는 것을 확인했다.** `DetailCard`가 그 화면을 근거로 달고 있었는데 시안에서는 흰 바닥에 내용이 그대로 놓인다. 주석을 고쳐 두었고 자리는 `/payment/done` 작업 때 다시 본다 (#205)
- 고객지원(`/mypage/support`)을 와이어프레임에서 UI 시안(`mypa_071` 기본 · 질문 펼침)으로 교체했다. 1:1 문의·공지사항 입구 줄은 44px에 초록 헤드셋·노란 확성기 아이콘이고, 많이 찾는 질문은 연빨간 물음표가 펼치면 빨갛게 바뀌며 파란 느낌표 답이 붙는다. shadcn 아코디언의 접근성과 키보드 조작은 그대로 두고 모양만 덮었다. 1:1 문의·공지사항 하위 화면은 시안이 없어 그대로다 (#207)
- 나의 상품 후기(`/mypage/reviews`)와 리뷰 작성(`/mypage/reviews/write`)을 와이어프레임에서 UI 시안(`mypa_041` 두 장, `리뷰작성` 여섯 장과 완료 화면)으로 교체했다. 후기 목록은 알약 세그먼트 탭 아래 구매일·사진·이름이 놓이고 작성한 후기에는 노란 별과 본문 두 줄이 붙는다. 리뷰 작성은 **두 단계**로 나뉘어 `?step=`이 URL에 남는다. 1단계는 반 개 단위 별점·사용 기간과 아이의 반응 5문항(기호성, 소화·배변, 피부·모질, 체중·활력, 알러지 — 전부 선택), 2단계는 1단계 요약 카드·아이 선택·급여 편의성·사진·후기이고 등록하면 완료 화면으로 간다. 필수는 별점·사용 기간·아이·후기 글이고, 작성 중인 값은 구매 항목별로 기기에 남아 새로고침해도 이어지고, 어느 구매의 후기인지(`orderItemId`) 없이 열면 작성 대신 나의 상품 후기로 안내한다. 2단계의 "이전"은 뒤로가기에 기대지 않고 화면 안에서 1단계로 간다. 공용으로는 `rating`이 디자인 시스템 별(노란색·반 개)이 되어 메인·상품 상세·리뷰 카드의 별도 함께 바뀌고, `pet-switcher` 기본형 원이 와이어프레임 값 44에서 시안 값 48로, `badge`에 필수 표시용 `brand` 톤이 더해졌다 (#203)

### 수정

- **결제 화면(`/payment`)에서 약관을 누르면 브라우저가 멈추던 것을 고쳤다.** 무한 렌더 루프였다 — 결제위젯이 `onReady` 콜백을 effect 의존성에 두고 있어, 부르는 쪽이 렌더마다 새 함수를 넘기면 effect가 다시 돌고 새 결제 함수를 넘겨 부모 상태가 바뀌며 되돌았다. 콜백을 ref로 들고 의존성에서 뺐다. **아이덴티티 안정을 부르는 쪽에 맡기지 않는 이유는 그것이 React Compiler의 메모이제이션에 기대는 일이기 때문이다** — 메모이제이션은 성능 최적화라 언제든 빠지고, `try/catch` 안의 옵셔널 체이닝 한 줄 때문에 실제로 빠져 있었다. 그 줄도 함께 걷어냈다 (#223)

- **shadcn 위에 올린 `bold` 타이포 토큰이 굵어지지 않던 것을 고쳤다.** 700으로 그려져야 할 글자가 500으로 나오던 자리가 19곳이었다. 토큰의 굵기가 `font-weight: var(--tw-font-weight, 700)`이고 shadcn이 달고 오는 `font-medium`이 그 변수를 채워서, **선언 순서와 무관하게** 토큰의 굵기가 적용되지 않았다. `cn`이 토큰을 글자 크기 그룹으로 알고 있어 굵기와 다른 그룹으로 보고 둘 다 남긴 것이 원인이다. 토큰을 전용 그룹으로 옮기고 굵기·행간과 충돌하게 했다. 호출부 19곳이 아니라 한 곳에서 막아 새 화면에서 다시 빠뜨리지 않는다. 굵기를 손으로 되돌리던 여섯 곳도 함께 걷어냈다 (#219)
- 장바구니 "전체선택"이 공용 `Label`을 다시 쓴다. shadcn 기본값이 타이포 토큰을 덮는다는 이유로 원시 `<label>`을 쓰고 있었는데, **#176에서 토큰을 `cn`에 알려 준 시점에 이미 해소돼 있었다.** 계산값을 재 보니 16px·24px·500으로 토큰 그대로다 (#219)
- 명세를 잘못 읽어 적은 것을 바로잡았다. **API 명세에 `POST /payments/confirm`이 없다** — 결제 섹션은 `POST /payments`(진행 중)와 `GET /payments/{paymentId}`(시작 전) 둘뿐이라, 승인을 어디가 맡는지는 아직 정해지지 않았다. 장바구니 수량 변경 응답도 `200 OK`만 약속하는데 `cartItemId`·`quantity`가 온다고 타입에 단언해 뒀었다. **둘 다 명세 행의 "작성되어 있던 내용" 토글에 접혀 있던 개정 전 내용을 현재 규격으로 읽은 것이다** (#217)
- **본문 없이 `200`으로 끝나는 응답이 실패로 뒤집히던 것을 고쳤다.** API 클라이언트가 204만 걸러 내고 나머지 성공 응답에는 곧장 `json()`을 불러, 빈 본문에서 던졌다. 장바구니 수량 변경이 바로 그런 엔드포인트라 성공한 요청이 실패로 읽히고 낙관적으로 그려 둔 수량이 되돌아갔다 (#217)
- 화면 스모크(`e2e/screens.spec.ts`)가 환경마다 다르게 돌던 것을 고쳤다. 토스 결제위젯은 클라이언트 키가 있는 환경에서만 바깥으로 요청을 내보내, 키를 넣어 둔 로컬에서만 `/payment`가 `networkidle`에 닿지 못했다. 장바구니도 백엔드 주소가 비어 있어 같은 오리진으로 요청이 가고 404가 콘솔에 찍혔다. 둘 다 응답을 세워 어디서 돌려도 같은 결과가 나오게 했다 (#214)
- `sr-only`로 숨긴 라디오가 16px 상자로 흐름에 남아 알림·좋아요·추천 화면의 필터칩과 온보딩 칩, 결제수단 고르기가 14px씩 밀려 있던 문제를 고쳤다. shadcn 라디오의 `relative`·`size-4`가 CSS 순서상 `sr-only`를 덮어서였고, 자리 값과 크기를 다시 덮어 흐름에서 뺐다 (#203)

## 2026-09-15

### 추가

- Figma 디자인 시스템 `icon` 페이지에 #165 이후 새로 추가된 3종(`clock`·`thumbs_up`·`thumbs_down`)을 `Icon` 컴포넌트에 들였다. 메인 화면 UI 교체(#185) 작업 중 "타임딜 없을 때" 화면에 쓸 시계 아이콘을 찾다가 발견했다. #165와 같은 절차(SVG 다운로드 → 24×24 정규화 → `currentColor`)로 반영했고, `/dev` 갤러리 안내 문구가 "43종"으로 고정돼 있던 것도 `ICON_NAMES.length`를 참조하도록 고쳤다 (#188)

### 변경

- 주문·배송 확인(`/mypage/orders`)을 와이어프레임에서 UI 시안(`mypa_061` 목록·구매확정·주문취소)으로 교체했다. 카드가 2단이 됐다 — 썸네일 96 오른쪽 위에 상품명·상태 뱃지·옵션이 오고 결제 금액이 아래로 내려간다. 여백과 버튼도 시안 값(좌우 20·카드 사이 16·버튼 36)으로 맞췄다. **시안의 색 네 가지가 `03. semantic` 토큰 값과 하나도 어긋나지 않아** 그대로 옮겼다 — `mypa_*`는 변수가 물려 있지 않아 손그림인 줄 알았는데 값은 토큰 그대로였다 (#201)
- **`배송 위치 보기`가 눌린다.** 택배사 연동이 없어 잠가 두었지만 눌리지 않는 버튼은 고장으로 읽힌다. 이제 준비중임을 알리는 모달이 열리고, 갈 곳이 생기면 그 자리만 잇는다 (#201)
- 구매 확정 시트가 바닥에 붙는 모양에서 양옆 8px 띄운 카드로 바뀌었다. 시안의 카드에는 손잡이가 없어 `BottomSheet`에 손잡이를 감추는 `showHandle`을 더했고, 이 시트는 `false`로 연다. 문구도 시안대로 "구매 확정을 할 수 있어요!" / "확정하기"가 되고, 무엇을 확정하는지 알 수 있게 결제 금액이 함께 나온다. 그 전에는 200P를 준다고 적어 두었는데 시안에서 빠졌다 (#201)
- 주문 상태 뱃지 다섯 가지가 모두 같은 색이 됐다. 시안이 그렇게 정했고, 어느 단계인지는 색이 아니라 문구가 알린다. 공용 컴포넌트라 주문 상세도 함께 바뀐다 (#201)
- **`bold` 타이포 토큰이 shadcn 위에서 굵기를 잃는 것을 발견했다.** `DrawerTitle`·`DialogTitle`·`Button`이 저마다 `font-medium`을 들고 있는데, `cn`이 크기(`text-*`)와 굵기(`font-*`)를 다른 그룹으로 보아 둘 다 살아남고 나중 선언이 이긴다. 700이어야 할 글자가 500으로 그려지면서 크기·행간은 맞아 눈에 잘 띄지 않는다. 이 화면은 `font-bold`를 함께 붙여 맞췄고, 같은 방식으로 쓴 다른 12곳은 따로 다룬다 (#201)
- 설정(`/mypage/settings`)을 와이어프레임에서 UI 시안(`mypa_081`)으로 교체했다. 줄은 높이 40에 아이콘 24·굵은 14 글자이고 알림설정(노랑 종)·테마설정·로그아웃(보라 열쇠)·회원탈퇴가 12px 간격으로 놓인다. 시안은 네 줄 다 화살표이지만 갈 곳이 있는 줄이 없어 내 정보(#194) 선례대로 화살표 없는 정적 줄로 두었고, 알림설정은 하위 화면 시안이 없어 스위치를 그대로 둔다. 로그아웃·회원탈퇴 동작은 화면만 먼저 맞추고 나중에 붙인다. 이 줄 규격이 마이페이지 홈(44 · 28 · 굵은 16)과 달라 `list-row`에 `size="sm"`을 더했다 (#199)
- 알림 목록(`/mypage/notifications`)과 재입고 알림(`/mypage/restock`)을 와이어프레임에서 UI 시안(`noti_011` 목록·공지 모달·알림 모달, `mypa_031` 목록·선택·취소 확인)으로 교체했다. 알림 목록은 배지(공지 회색·알림 파랑)·제목·날짜·읽지 않음 점·본문 미리보기 한 줄이고, **와이어프레임 때 뺐던(#137) 전체·새 알림·확인한 알림 거르기 칩이 시안에 돌아와** 되살렸다. 값은 `?filter`로 URL에 둔다. 재입고 알림은 **"수정하기" 모드가 시안에서 빠져** 언제나 고를 수 있고, 고른 것이 있을 때만 "알림 취소하기"가 나와 확인 창을 거쳐 목록에서 지운다. 상품 줄은 사진 64·이름·가격에 오른쪽 원형 체크(`CheckboxRow`를 뒤집어 씀)이고 안내 문구도 시안대로 바뀌었다. `badge`에 안내용 파랑 `info` 톤을 더했다. 모달·확인 창의 버튼 줄은 shadcn 푸터의 회색 띠 없이 맨 줄로 두었고, 확인 창의 빨간 버튼은 `AlertDialogAction`이 클래스를 병합하지 않아 `Button`으로 그렸다 (#196)
- **주소 검색이 진짜 주소를 찾는다.** 행정안전부 도로명주소 API를 붙였다. 그동안 무엇을 치든 같은 목데이터 9건이 나왔다. 백엔드팀이 "직접 해주시면 따르겠다"고 회신해(2026-09-15) 프론트에서 붙였다 (#193)
- 승인키가 브라우저로 나가지 않도록 Route Handler(`/api/juso`)를 거친다. 행안부는 CORS를 열어 두어 직접 불러도 되지만 그러면 키가 클라이언트 번들에 박힌다. 이 저장소의 첫 Route Handler다 (#193)
- 행안부가 **실패도 HTTP 200으로 보내는 것**을 Route Handler가 걸러 우리 규격으로 옮긴다. 검색어가 너무 넓거나(`서울`) 숫자만 넣으면(`123`) 무엇을 고쳐야 하는지 알려 준다 — 그냥 "실패"라고만 하면 사용자가 같은 말을 다시 친다 (#193)
- 쪽을 넘길 때 앞 결과를 그대로 두고 새 결과를 기다린다. 넘길 때마다 목록이 사라지고 뼈대가 번쩍여 화면이 들썩이던 것을 막았다 (#193)
- 주소 검색 통로가 행안부의 뜻밖의 응답을 버틴다. 점검 페이지가 200으로 와도 화면이 터지지 않고, 답하지 않는 요청은 5초에 끊는다 (#193)
- 찾는 동안 결과 자리에 뼈대를 보여 준다. 빈 화면을 두면 결과가 도착할 때 아래 버튼까지 밀려 올라간다. 공용 로딩 시안은 PD팀에 요청해 두었다 (#193)
- **배송지 입력과 주소 검색이 이어졌다.** 그동안 주소를 골라도 배송지 폼에 들어가지 않았다 — 폼이 주소를 setter 없이 들고 있었고 검색 화면은 고른 뒤 뒤로가기만 했다. 이제 고른 주소를 `?zipNo=&roadAddr=`에 실어 넘긴다. 컴포넌트 상태가 아니라 주소창에 둔 것은 검색 화면으로 넘어가는 순간 사라지고 새로고침에서도 살아남지 못하기 때문이다 (#187)
- 배송지 입력(`mypa_311`)과 주소 검색(`mypa_312`)을 와이어프레임에서 UI 시안으로 교체했다. 주소 줄 오른쪽이 화살표에서 돋보기로 바뀌고, 검색창은 테두리 상자에서 밑줄 한 줄이 되며 검색 버튼(28px)이 그 줄 안으로 들어간다. 검색 결과는 우편번호·도로명·구주소 세 줄로 그린다. 글자는 전부 타이포 토큰을 쓴다 (#187)
- **시안과 한 곳을 다르게 갔다.** `mypa_312`는 검색어만 넣어도 `입력 완료`가 활성인데, 주소를 고르지 않으면 넘길 값이 없어 화면이 멈춘다. 주소를 골랐을 때만 활성으로 뒀다 (#187)
- 화면 골격(`SingleInputScreen`)의 좌우 여백을 16px에서 20px로 고쳤다. 그 안의 버튼 줄이 20px이라 입력칸이 버튼보다 안쪽으로 들어가 있었다. 공용 컴포넌트라 회원가입·휴대폰 인증·별명 수정 화면도 함께 맞춰진다 (#187)
- 주소 검색 결과에 페이지네이션을 붙였다. **한 페이지에 4개**이고 목록 바로 아래에 넘기는 버튼이 온다 — PD팀이 393×852에서 스크롤이 생기지 않는 수로 정했다. 쪽 번호를 늘어놓지 않고 `1 / 3`처럼 몇 쪽인지만 보여 준다. `역삼동` 4,747건처럼 흔한 검색어가 쪽 천 개를 넘겨 번호가 의미를 잃기 때문이다 (#187)
- 주소창의 쪽이 범위를 벗어나도 화면이 깨지지 않게 했다. `page=0`은 결과가 있는데도 빈 목록을, 음수는 엉뚱한 구간을, `page=999`는 빈 화면을 만들었다. 이제 있는 쪽으로 보정하고 주소창도 조용히 맞춘다 (#187)
- 뒤로가기로 주소창의 검색어가 바뀌면 입력칸도 따라가게 했다. 그 전에는 결과만 바뀌고 입력칸에 앞서 친 말이 남아 둘이 어긋났다 (#187)
- 주소 검색의 검색어와 쪽을 주소창에 두었다. 그 전에는 새로고침하면 결과가 통째로 사라졌다. 이제 `?query=테헤란로&page=2` 링크만으로 같은 화면이 나오고, 뒤로가기가 앞 쪽으로 돌아간다 (#187)
- 주소 검색의 입력칸과 쪽 넘기는 버튼을 44×44로 키웠다. 검색 버튼(28px)은 시안이 지정한 값이라 그대로 둔다 — 컨벤션이 "시안이 지정한 값을 그대로 쓰되 시안에 없는 자리는 44×44를 기본으로 한다"고 정하고 있고, 쪽 버튼은 시안에 없던 자리다 (#187)
- 배송지를 고치는 중에 주소를 검색하면 고치던 대상이 사라지던 것을 막았다. 검색 화면으로 갈 때 `place`를 들고 가고 돌아올 때 그대로 돌려준다. 그 전에는 "집 주소를 고칠까요?"가 "어디로 보내드릴까요?"로 바뀌면서 먼저 적어 둔 이름·연락처·상세주소가 전부 날아갔다 (#187)
- 주소 검색어를 두 글자 이상 받는다. 행안부 API가 한 글자를 `E0008`로 거절하는 것을 실제 호출로 확인했다 (#187)
- 주소 조회를 화면에서 `views/search-address/api`로 옮겼다. 페이지도 거기서 자른다 — 행안부 API가 `currentPage`·`countPerPage`로 서버에서 잘라 주므로 지금부터 같은 모양으로 맞춰 두면 연동할 때 그 파일 안만 바뀐다. 목 데이터도 지어낸 값이 아니라 실제 응답을 옮겼다 (#187)

- 온보딩 6단계(소개·기본 정보·품종 선택·상세·건강·완료)를 와이어프레임에서 UI 시안(`onbo_001`~`onbo_005`, `onbo_011`)으로 교체했다. 화면 아이콘은 `Icon`으로, 글자는 타이포 토큰으로, 모서리는 고정 토큰으로 바꿔 임의 값이 남지 않는다. 품종 선택은 검색창 아래에 전체 목록을 먼저 보여 주고 줄을 누르면 바로 확정되며, 완료 화면의 "프로필 추가"는 초안을 지우고 기본 정보 단계로 돌아간다 (#163)
- 온보딩이 쓰는 공용 컴포넌트 6종(`step-progress`·`avatar-uploader`·`chip-select`·`checkbox-row`·`bottom-action-bar`·`form-field`)을 시안 모양으로 맞췄다. 시안이 40px인 버튼·칩도 탭 영역은 44px을 유지하고, 시안에 없는 고른 칩의 체크 아이콘은 색만으로 알리지 않도록 남겼다 (#163)
- 체구 안내를 툴팁이 아니라 말풍선(Popover)으로, 체형(BCS)은 다섯 칸 트랙 위를 움직이는 슬라이더로 바꿨다. Radix 슬라이더는 값을 트랙 폭에 선형으로 놓아 가운데 값만 칸 중앙에 오므로, 트랙과 눈금은 직접 그리고 슬라이더 폭을 첫 칸 중앙부터 마지막 칸 중앙까지로 잡아 손잡이가 늘 칸 한가운데에 선다. 눈금 문구는 화면용이라 스크린 리더에는 손잡이 값이 "보통"처럼 체형 이름으로 읽힌다 (#163)
- 바텀시트를 시안의 떠 있는 카드 모양(양옆 8px·아래 32px 띄우고 모서리 넷 다 둥글게, 40px 손잡이 영역)으로 그리는 `shared/ui/bottom-sheet`를 만들고 건강 선택 시트와 체형 설명 시트에 적용했다. shadcn `drawer.tsx`는 CLI가 덮어쓰므로 고치지 않고 그 조각으로 조립한다. 다른 화면의 시트 7곳은 아직 와이어프레임 기준이라 각 화면을 시안으로 옮길 때 함께 바꾼다 (#163)
- 파일 헤더 주석 규칙에 UI 시안 단계를 반영했다. 시안을 근거로 만든 파일은 어느 화면(ID)을 따랐는지 남긴다 (#163)
- 로그인·회원가입을 와이어프레임에서 UI 시안(`sign_001` 6장)으로 교체했다. 로그인은 제목·서브카피·입력 간격을 시안대로 맞추고 소셜을 44px 원형 아이콘으로 바꿨다. 시안에는 넷(카카오·네이버·애플·구글)이 그려져 있으나 인증 정책이 카카오·구글이라 둘만 둔다. 약관 동의는 원형 체크(전체 동의 24px·항목 16px, 고르면 브랜드색)에 필수 표시 점과 설명 문구가 붙고, 닉네임은 밑줄 하나짜리 입력이다. 시안의 아이디·비밀번호 폼 유지 여부, "골라주개냥" 강조색, 약관 변형 화면(`1117-5644`), 선택 약관의 본문 화살표는 PD·PM 확인 항목으로 이슈에 남겼다 (#184)
- 마이페이지 홈·내 정보·닉네임·휴대폰 인증을 와이어프레임에서 UI 시안(`mypa_001`·`mypa_011`·`mypa_111`·`mypa_212`)으로 교체했다. 홈은 회색 바탕에 흰 카드 넷(프로필·나의 쇼핑·혜택과 결제·고객지원)이고 메뉴 아이콘은 항목마다 색이 다르다(재입고 빨강, 후기 노랑, 결제·배송 파랑, 고객지원 초록). **시안에서 "최근 본 상품" 메뉴가 빠져** 진입점만 뺐고 화면은 남겼다. 내 정보는 상단 사진 없이 닉네임·이름·생년월일·휴대폰·내 아이들 다섯 줄과 배송 받을 곳 목록이며, 닉네임 변경은 현재 닉네임을 회색 자리 표시로만 보인다. 휴대폰 인증은 입력칸 안에 "인증" 칩이 들어가고 인증 번호를 보내면 토스트로 알린다 (#194)
- 마이페이지 전체가 쓰는 목록 줄(`list-row`)과 설정 묶음(`setting-group`)이 시안 규격이 됐다. 줄은 높이 44에 아이콘 28·제목 굵은 16·화살표 28이고, 묶음은 제목을 안에 둔 흰 카드(모서리 12)에 줄 사이 선 없이 12px 간격이다. 설정·고객지원·결제수단 화면의 줄도 함께 바뀐다. `badge`에 검정 바탕 `strong`, `form-field`에 입력칸 안 오른쪽 동작 슬롯(`trailing`)을 더했다 (#194)
- 마이페이지 반려동물 화면을 와이어프레임에서 UI 시안(`mypa_021` 아이 관리 2탭 · 반응 시트 · 정보 수정 3장)으로 교체했다. 내 아이 관리 탭은 사진 카드 위에 이름·몸무게·질환을 세 줄로 얹고 줄마다 수정 화면으로 가며, 사진 아래 절반은 같은 사진을 흐리게 한 장 더 얹고 마스크로 위쪽을 지워 어둠과 흐림이 함께 사라진다. `backdrop-filter`는 Chrome에서 마스크를 따르지 않아 경계가 선으로 보이기 때문이다. 아이 제품 관리 탭은 필터칩과 제품 카드에 "반응 남기기"(미입력은 브랜드 채움, 입력은 테두리)이고 남길 반응이 있으면 탭 이름에 점이 찍힌다. **새 아이 추가는 온보딩 기본 정보 단계로 간다.** 시안에 별도 등록 화면이 없어 도입 화면(`/mypage/pets/new`)과 후기 시트(`ProductReviewSheet`)는 걷어냈다. 사진 카드 아래 모서리는 시안 `radius/20`이 토큰에 없어 16을 쓴다 (#189)
- 배지·필터칩·아이 전환 줄·반응 시트가 시안 모양이 됐다. "구매 후 6일"·"3번째 구매"·알러지 이름 같은 짧은 표시는 뜻에 따라 회색·초록·빨강으로 갈리는 공용 배지(`badge`)로 통일했고, 목록을 거르는 칩(`filter-chips`)은 36px 알약이 되어 좋아요·추천 화면도 함께 바뀐다. 아이 관리의 아이 전환 줄은 고른 아이만 90px로 크게 보이고(메인의 줄은 그대로), 제품 반응을 남기는 시트는 온보딩과 같은 떠 있는 카드에 잘 맞았어요·그냥 그랬어요·안 맞았어요 아이콘으로 묻는다. 메인의 상태 체크 시트도 같은 것이라 함께 바뀐다 (#189)
- 공용 `checkbox-row`가 원형 체크의 크기(m 24 / s 16)와 골랐을 때 색(primary / brand), 레이블 밖 설명, 오른쪽 슬롯을 받게 넓혔다. 회원가입 약관 줄이 그 위에 규칙만 얹어 원형 체크를 그리는 코드가 한 곳이 된다. `form-field`에는 밑줄형 `variant="underline"`을 더했고, `page-header`는 시안 공용 header(높이 48·`Icon` 뒤로가기)로, `single-input-screen` 질문은 `title/bold_20`으로 맞췄다. 머리말과 단일 입력 골격을 쓰는 다른 화면에도 함께 적용된다 (#184)

## 2026-09-14

### 추가

- 실패했을 때 사용자에게 무엇을 보일지를 네 층으로 나눠 정하고 구현했다. 변경(`useMutation`) 실패는 `MutationCache`의 전역 `onError`가 토스트로 알리고, 섹션 렌더 오류는 기존 `ErrorBoundary`가, 화면 하나가 통째로 실패하면 `error.tsx`가, 루트 레이아웃까지 깨지면 `global-error.tsx`가 맡는다. 조회 실패는 전역에서 토스트를 띄우지 않는다 — 목록이 비면 빈 상태를 보여주는 편이 낫고 배경 refetch까지 알리면 사용자가 하지도 않은 일로 알림이 뜬다 (#173)
- 사용자 문구를 `shared/config/app-message.ts`로 모으고 토스트는 `toastAppSuccess`·`toastAppError`로만 띄우게 했다. 호출부는 문구가 아니라 메시지 코드만 넘긴다. 백엔드 실패 응답은 `errorCode`를 키로 우리 문구에 매핑하고, 모르는 코드는 HTTP 상태 기준 기본 문구로 떨어진다. 백엔드가 새 코드를 추가해도 화면이 도는 구조다 (#173)
- 오류 원인을 남기는 통로를 `shared/lib/report-error.ts` 하나로 모았다. ESLint `no-console`을 여기서만 열고, 상태 코드와 `errorCode`만 뽑아 남긴다. 규칙의 근거인 시큐어 코딩 가이드 SC-G-02가 우려하는 상황(응답 객체를 통째로 찍어 토큰이 로그에 남는 것)을 막기 위해서다. 관측 도구를 붙이면 이 함수 안이 보고 지점이 된다 (#173)
- 디자인 시스템 아이콘 43종을 `shared/ui/icon`의 `Icon` 컴포넌트로 들였다. 화면 아이콘을 react-icons(Ionicons)로 그려 온 탓에 물음표·화살표·검색처럼 어디에나 있는 아이콘부터 시안과 모양이 달랐다. Figma `icon` 페이지의 SVG를 스크립트로 받아 24×24 상자 안 위치를 되살리고 색을 `currentColor`로 바꿔, `text-icon-fill-*`·`text-icon-stroke-*` 토큰과 다크 모드가 그대로 먹는다. 시안 44종 중 `cart_noti`는 아이콘이 아니라 숫자 배지 조합이라 뺐다 (#165)
- 아이콘 규칙을 바꿨다. 화면 아이콘은 `Icon`을 먼저 쓰고, 세트에 없는 글리프만 react-icons로 보충하며 그 파일 헤더에 이유를 남긴다. 기존 화면의 아이콘은 화면을 UI 시안으로 옮길 때 그 화면 단위로 바꾼다 (#165)

### 변경

- `cn()`이 #167의 타이포 토큰(`text-title-bold-20` 등)을 글자색으로 오인해, `text-foreground` 같은 색 클래스와 함께 쓰면 타이포를 지우던 것을 고쳤다. tailwind-merge는 CSS를 읽지 않아 `@theme`의 토큰을 모르고 낯선 `text-*`를 색으로 분류한다. 토큰 이름 목록(`shared/lib/typo/typo-tokens.ts`)을 글자 크기 그룹에 등록했고, 목록이 `globals.css`와 어긋나면 테스트가 잡는다. 공용 컴포넌트가 전부 `cn(base, className)` 구조라 이 수정 전에는 타이포 토큰을 공용 컴포넌트에 넘기는 순간 사라졌다 (#176)
- 장바구니를 와이어프레임에서 UI 시안(`cart_001`)으로 교체했다. 체크박스가 목록 왼쪽이 아니라 상품 사진 위로 올라가고, 상품명은 옵션 줄 없이 한 줄로 잘린다. 전체선택이 고른 개수를 함께 보여주고(`전체선택 (0/3)`), **아무것도 고르지 않으면 금액 줄을 아예 감춘다** — 0원만 늘어놓아도 알려주는 것이 없고 고르라는 신호가 흐려진다. 와이어프레임 때 `상품 옵션`으로 적고 PD팀에 확인을 요청해 둔 줄은 시안에서 `판매가격`으로 확정됐다 (#172)
- **탭 대상 크기 규칙을 디자인 시스템 기준으로 바꿨다.** 지금까지 "최소 44×44"를 고정값으로 두었는데, 디자인 시스템이 `button/m`(32px)·`button/xl`(44px)처럼 크기 토큰을 갖고 있어 시안이 지정한 값을 그대로 쓰기로 했다. 접근성을 이유로 시안보다 키우면 화면마다 크기가 달라져 그게 더 큰 문제가 된다. 시안에 없는 자리는 44×44가 기본이고, 시안 값이 작아 보이면 임의로 키우지 말고 디자인팀에 확인한다 (#172)
- 수량 스테퍼를 시안대로 32×32 칸 세 개로 바꾸고 칸 사이를 12px짜리 짧은 선으로 나눴다. 머리말 제목도 공용 헤더가 쓰는 `title/bold_18`로 맞췄다. 둘 다 공용 컴포넌트라 다른 화면에도 함께 적용된다 (#172)
- 장바구니 목데이터를 화면 컴포넌트에서 `views/cart/api`로 옮겼다. API가 생기면 그 함수 안이 `apiRequest` 호출로 바뀌고 화면 코드는 그대로 둘 수 있다. 백엔드 `order-service`에 아직 컨트롤러가 없고 장바구니는 서비스 자체가 없어 연동은 하지 않았다 (#172)
- 모서리 토큰을 배율 파생에서 명시값으로 바꿨다. shadcn 기본은 `--radius` 하나에서 `rounded-sm·md·lg·xl·2xl`을 0.6·0.8·1·1.4·1.8배로 만들어 어느 단계도 Figma `radius/4·6·8·12·16`과 맞지 않았고, 화면마다 `rounded-[8px]` 같은 임의 값을 적게 되던 것을 4·6·8·12·16px로 고정했다. 와이어프레임 기준 화면의 모서리가 1~2px 달라지지만, UI 시안을 적용할 때 화면 단위로 다시 손대므로 여기서 맞추지 않는다 (#169)
- Figma foundation의 그림자 다섯 단계(`shadow_xs·s·m·l·xl`)를 `shadow-xs·sm·md·lg·xl`로 넣었다. 각 두 겹이고 색은 `surface/primary` 라이트값에 투명도만 다르다. 다크 모드 값은 시안에 없어 같은 값을 쓴다 (#169)
- PR 템플릿의 검증 체크리스트에 `format:check`와 `lint`를 더해 컨벤션과 같은 5단계로 맞췄다. AGENTS.md 2.9절은 다섯을 요구하는데 템플릿에는 `typecheck`·`test`·`build` 셋만 있어, 템플릿대로 채우면 누가 올려도 둘이 빠졌다 (#170)
- 소셜 로그인 복귀 방식이 토큰을 쿼리로 넘기던 것에서 일회용 code를 넘기고 프론트가 교환하는 것으로 바뀐 것을 인증 설명에 반영했다. 토큰 보관소 주석과 API 클라이언트 문서가 구 방식을 전제로 남아 있어, 콜백 화면을 만들 때 쿼리에서 토큰을 읽게 될 수 있었다. 라우팅 문서에는 `/auth/callback?code=`를 예정 라우트로 올리고 신규 회원이면 `/signup`, 아니면 `/`로 보내는 분기를 적었다 (#154)
- `bg/*`(화면 프레임 배경)와 `surface/*`(컴포넌트 표면)가 리네임이 아니라 별개 카테고리로 분리된 것임을 PD팀에게 재확인해, `docs/conventions/design-convention.md`와 `globals.css` 주석의 잘못된 설명을 정정했다 (#136)
- `--bg-default`·`--bg-secondary`를 새로 반영하고 `--background`를 `surface/default`가 아니라 `bg/default`에 연결했다. `bg/secondary` 다크 값은 `bg/default` 다크와 같은데, 이는 Figma가 지정한 값 그대로다 (#136)
- Figma `04. component`의 `card/bg`가 `surface/default`가 아니라 `surface/default_react`를 참조하는 것을 확인해 `--card`를 재연결했다. `dialog`·`sheet` 컴포넌트도 같은 `card/bg`를 쓰므로 `dialog.tsx`·`alert-dialog.tsx`·`drawer.tsx`를 `bg-popover`·`text-popover-foreground`에서 `bg-card`·`text-card-foreground`로 바꿨다. `dropdown`(`select.tsx`)은 `surface/default`를 직접 참조해 `bg-popover`를 그대로 유지했다 (#136)
- `surface/default_sheet`가 `surface/default_react`로, `surface/dimmed`가 `surface/overlay_dimmed`로 다시 리네임된 것을 반영하고, 흰색 기반의 신규 토큰 `surface/overlay_static`을 추가했다 (#136)
- 본문 폰트를 `Noto_Sans`에서 **Pretendard**로 바꿨다. Figma 타이포 토큰이 모두 `typo/pretendard`를 참조하는데 기존 폰트는 latin subset이라 한글 글리프가 없어, 화면의 한글이 보는 사람의 OS에 따라 다른 모양으로 그려지고 있었다. Google Fonts에 없는 폰트라 `next/font/local`로 self-host하고, weight는 토큰이 실제로 쓰는 400·500·700 세 벌만 subset으로 받았다(합계 787KB). CDN에서 불러오지 않은 것은 `next/font`가 만들어 주는 `size-adjust` fallback을 써야 폰트 교체 순간에 레이아웃이 밀리지 않기 때문이다. 이로써 AGENTS.md 6절의 "한글 본문 폰트" 보류가 풀렸다 (#162)
- 폰트 교체에 맞춰 `font-semibold`(600) 14곳을 `font-bold`로 바꿨다. 디자인 시스템의 Text Style이 `regular`(400)·`medium`(500)·`bold`(700) 셋뿐이라 600은 애초에 없는 굵기이고, CSS 폰트 매칭 규칙상 500을 넘는 요청은 더 굵은 쪽을 먼저 찾으므로 600은 이미 700으로 그려지고 있었다. 각 자리가 시안상 어떤 Text Style인지는 화면별 UI 적용 작업에서 다시 잡는다 (#162)
- Figma `골라주개냥 Design System`의 Text Style **25종**을 타이포 토큰으로 반영했다. 색상과 달리 타이포는 크기·행간·굵기가 한 벌로 묶여야 의미가 있어, Tailwind v4의 `--text-*` 하위 속성으로 셋을 함께 걸었다 — `text-label-bold-14` 하나면 14px·행간 22·weight 700이다. 계열은 `title`·`body`·`caption`·`label` 넷이고, Figma가 px로 주는 값은 rem으로 환산해 넣었다. px로 고정하면 사용자가 브라우저 기본 글자 크기를 키워도 화면이 따라 커지지 않기 때문이다 (#167)
- `body/*`의 Figma 이름이 `refular`지만 코드에는 `regular`로 넣었다. 정의표의 샘플 글자가 `body/regular_18`로 적혀 있어 스타일 이름만 오타로 봤다. `label/regular_13`·`_14`·`_16` 셋은 정의표에 없고 변수도 안 물려 있어 PD팀에 확인을 요청했고, 행간을 각각 20·22·24로 고치고 변수를 연결해 주어 함께 반영했다 (#167)
- 개발용 갤러리(`/dev`)에 Typography 절을 더해 25종을 실제 렌더로 비교할 수 있게 했다. Tailwind는 소스에 쓰인 클래스만 만들어 내므로, 토큰을 정의만 하고 아무 데서도 쓰지 않으면 제대로 걸렸는지 확인할 방법이 없다 (#167)

### 개발 환경

- Dockerfile을 추가하고 `next.config.ts`에 `output: "standalone"`을 켰다. CI(코드 품질 검증)만 있고 배포 파이프라인은 없던 상태에서, sever 레포와 같은 방식(Jenkins·kaniko·ECR·GitOps)으로 컨테이너 배포를 연결하기 위한 첫 단계다. 로컬에서 이미지 빌드와 기동을 확인했다 (#178)
- Jenkinsfile을 추가했다. GitHub Actions가 이미 코드 품질을 검증하므로 이 파이프라인은 빌드·Trivy CRITICAL 스캔·ECR push·GitOps 갱신만 맡는다. sever와 달리 서비스가 하나뿐이라 다중 서비스 분기 없이 단순한 구조다 (#178)
- 첫 web-ci 빌드가 kaniko ephemeral-storage 3Gi 초과로 Evicted됐다. Dockerfile 안에서 npm ci(패키지 852개) + build를 kaniko가 통째로 떠안은 게 원인이라, node 컨테이너에서 standalone 산출물(약 80MB)을 미리 만들고 kaniko는 결과물만 COPY하도록 Dockerfile·Jenkinsfile을 고쳤다 (#178)
- 두 번째 web-ci 빌드에서 Trivy CRITICAL 3건이 걸렸다. next 16.3.0의 인증 우회 원격 코드 실행 취약점 2건(CVE-2026-75604, GHSA-2xp9-vwfh-vxw4)은 16.3.5로 올려 해결했고, node:22-alpine에 기본 포함된 npm CLI 내부 tar 패키지의 CVE-2026-59873은 런타임에 npm/npx/corepack/yarn이 아예 필요 없어서 Dockerfile에서 통째로 지워 해결했다. 로컬 Trivy 스캔으로 CRITICAL 0건 재확인했다 (#178)
- #178 머지 직후 dev 첫 실배포 빌드가 crane push에서 `petflow/web` ECR 레포 없음으로 실패했다. 머지가 infra의 `tapply.sh` 실행보다 먼저 끝나서 생긴 순서 문제였고, Trivy 스캔 자체는 CRITICAL 0건으로 정상 통과했다. 레포 생성 후 재트리거용 커밋으로 다시 검증한다 (#181)

---

## 2026-09-11

### 변경

- 목록 화면들이 목데이터 상수를 직접 보던 것을 조회 결과로 받도록 바꿨다. 상수는 늘 차 있어 "후기가 없어요" 같은 빈 상태가 화면에서 한 번도 닿지 않았고, 테스트도 그 자리를 덮지 못했다. 나의 후기·간편결제 카드·사진 리뷰 모음이 대상이다 (#159)
- PD팀이 갱신한 회색조 배경 스케일(`bg/weak`·`bg/secondary`·`bg/tertiary`·`bg/disable`)과 positive 계열, danger 다크 모드 값을 반영했다. danger 다크 대비는 2.76:1→3.90:1로, positive 라이트 대비는 2.83:1→3.03:1로 개선됐다 (#136)
- `bg/control_disabled`가 다른 disable 계열 토큰과 값이 겹쳐 Figma에서 정리돼, `--bg-control-disabled`를 코드에서도 제거했다 (#136)
- brand·success·destructive의 흰 글자 대비를 PD팀이 APCA 기준으로 재평가해 유지하기로 확인했다. WCAG 비율 공식 기준으로는 여전히 미달이라 Lighthouse 감사에서는 미달로 잡힐 수 있다 (#136)
- `--muted`·`--muted-foreground`는 `bg/tertiary`와 역할이 비슷해 보였을 뿐 Figma·PD팀이 실제로 지정한 대응이 아니었음을 확인해, 전역 연결 없이 shadcn 기본값을 유지하기로 정리했다. `bg/tertiary`를 직접 쓰는 화면은 그 토큰을 바로 쓰고 글자는 용도에 따라 고른다 (#136)
- Figma가 기존 `bg/*`를 `surface/*`(컴포넌트 표면)로 옮기고, `bg/*`는 화면 프레임 배경 전용으로 새로 분리했다. `surface/dimmed`(투명도 보존)를 새로 반영하고, `icon/fill/rating`을 `icon/fill/accent`로 리네임했으며, `icon/fill`에 색상 계열 10개(연한/진한 초록·빨강·파랑·브랜드·보라)를 추가해 공용 시맨틱 토큰이 총 85개가 됐다 (#136)

### 추가

- 상품 상세 리뷰 탭에 후기 목록을 붙였다. 각 후기에 **품종·나이·체중**이 함께 붙는다. 같은 사료라도 4kg 말티즈와 28kg 리트리버의 후기는 다른 이야기라, 별점만 나열하면 보호자가 자기 아이에게 적용할 수 있는지 판단할 수 없다 (#148)
- `내 반려동물 맞춤보기` 토글로 지금 고른 아이와 같은 품종의 후기만 볼 수 있다. 시안 구조도가 필터를 자동(토글)과 수동(바텀시트)으로 나눠 적은 것 중 자동 쪽이다 (#148)
- 리뷰를 조건으로 거를 수 있다. 사용 기간·재구매 여부·종·나이·중성화·체중을 바텀시트에서 고른다. 128개를 다 읽을 수는 없으니 우리 아이와 비슷한 조건의 후기만 추려 보는 길을 냈다 (#149)
- 고른 구간에 뜻을 함께 적는다. `8세 이상 (노령견)`처럼 숫자만으로는 무엇을 고른 것인지 읽히지 않기 때문이다 (#149)
- 상품 상세 Q&A 탭에 문의 목록을 붙였다. 전에는 "등록된 문의가 없어요"만 있어 다른 사람이 이미 물어본 것을 볼 수 없었다. 급여량·병행 급여처럼 같은 질문이 반복되는 자리라, 목록이 보이면 묻기 전에 답을 찾는다 (#153)
- 후기에 달린 사진만 모아 보는 화면을 붙였다. 사료 후기에서는 알갱이 크기나 변 상태처럼 글로 옮기기 어려운 것을 사진이 말한다. 격자에서 고르면 그 사진을 남긴 후기가 함께 열린다 (#151)

### 제거

- 자리 표시만 하던 `/products/[productId]/reviews` 화면을 걷어냈다. 시안에서 리뷰가 탭 안으로 들어오면서 갈 곳이 없어졌다 (#148)

---

## 2026-09-10

### 추가

- 결제 화면에 약관 동의를 넣었다. 전에는 동의 항목이 하나도 없어 아무것도 확인받지 않고 결제할 수 있었다. **필수 셋을 다 켜야 결제하기가 풀린다** (#138)
- 배송 요청사항 보기를 넷에서 여섯으로 늘리고 `직접 입력`을 더했다. 고르면 100자 칸이 열린다 (#138)
- 주문 완료에 주문번호와 `주문 상세 보기`를 넣었다. 문의할 때 대는 유일한 식별자인데 보여주지 않아, 주문 내역을 다시 찾아 들어가야 했다 (#138)
- 배송이 끝난 주문에 반품·교환 접수를 붙였다. 누르면 바로 접수되지 않고 확인창이 한 번 막는다. 접수하면 기사가 상품을 가지러 오고 그 뒤에야 환불이나 교환이 진행되므로, 무슨 일이 일어나는지 알린 뒤 확인받아야 한다 (#139)
- 반품과 교환은 수거 뒤에 오는 것이 달라 설명도 나눴다. 반품은 "바로 환불해 드릴게요", 교환은 "새 상품을 보내드릴게요"다 (#139)
- 비교 화면에서 종류가 다른 둘을 담으면 표 대신 안내가 나온다. 사료와 간식은 10g당 가격도 칼로리도 기준이 달라, 나란히 놓으면 숫자가 큰 쪽이 나빠 보이는 착시가 생긴다 (#144)
- 배송지에 연락처를 받는다. 기사가 부재 시 연락할 곳인데 없었다. 받는 사람이 나와 다른 경우가 배송지를 따로 만드는 이유라 가입 때 받은 번호로 대신할 수 없다 (#145)

### 변경

- 비교할 상품을 검색 화면에서 고른다. 와이어프레임 2026-09-09 수정에서 `comp_011`이 빠지고 "검색 페이지 통일"이 적혔다. 상품 상세의 `비교하기`도 보던 상품을 첫 자리에 담아 비교로 보낸다 (#144)
- 알림 목록에서 거르기 칩을 뺐다. 와이어프레임 2026-09-09 수정에서 `notice` 섹션에 "필터칩 삭제"가 붙었다. 함께 쓰던 `?filter` 쿼리도 없앴다 (#137)
- 장바구니에서 옵션변경 버튼과 옵션 바꾸기 시트를 걷어냈다. `cart_001_옵션변경` 프레임이 삭제되고 "페이지 삭제 및 옵션변경 버튼 삭제" 메모가 붙었다. 남겨 두면 시안에도 API 계약에도 없는 동작을 붙이게 된다 (#137)
- Figma `골라주개냥 Design System`의 공용 시맨틱 컬러 토큰 74개를 `globals.css`에 추가하고, `primary`·`input` 등 의미가 대응되는 shadcn 슬롯을 Figma 토큰에 연결했다. 함께 사용할 토큰이 확정되지 않은 `muted`와 의미가 다른 `accent`는 기존 값을 유지했다 (#136)
- `--brand`·`--success`에 있던 디자인 확정 전 임시값을 Figma 확정값으로 바꿨다 (#136)
- brand·danger 글자색은 PD팀 결정에 따라 흰색으로 유지하고, success 배지 글자색은 현재 Figma 연결에 맞춰 흰색으로 반영했다. danger 다크 모드의 색상 조정과 success 배지의 대비 기준 적용 여부는 PD팀과 후속 확인한다 (#136)
- 디자인 컨벤션의 "디자인 토큰은 확정 전이다" 안내를 지웠다. 코드는 확정값을 쓰는데 문서만 미확정이라고 하면 서로 어긋난다. 대신 Figma 원본과 CSS 변수의 매핑 규칙을 적었다 (#136)

---

## 2026-09-09

### 변경

- API 클라이언트를 백엔드 인증 정책에 맞췄다. 토큰 재발급 요청에 만료된 accessToken을 붙이지 않는다. 게이트웨이가 JWT를 먼저 검증하는 구조라, 붙이면 재발급 자체가 401로 끊길 수 있었다 (#133)
- 로그인 없이 보는 엔드포인트를 위한 `auth: false` 옵션을 더했다. 토큰이 있어도 붙이지 않고 401에도 재발급하지 않는다 (#133)
- 쿼리 스트링 조립과 `FormData` 본문 전달을 클라이언트가 맡는다. 명세 GET 대부분이 `petId`·`sort`·`page` 같은 쿼리를 받고, 이미지 업로드는 multipart라 슬라이스마다 따로 만들 뻔했다 (#133)
- 4xx 응답은 다시 보내지 않는다. 전에는 401·404까지 한 번 더 보내서 에러 화면이 늦게 떴다. 5xx와 네트워크 오류만 1회 재시도한다 (#133)
- 토큰 보관소에 로그인 여부(`hasSession`)와 세션 종료 알림(`subscribeTokensCleared`)을 더했다. 재발급 실패로 세션이 끝났을 때 로그인 이동·캐시 비우기를 붙일 자리다 (#133)
- Query Key를 명세 파라미터에 맞췄다. 상품 목록에 없는 `species`를 빼고, 아이(`petId`)에 따라 응답이 달라지는 상세·검색·장바구니·리뷰 목록 키에 `petId`를 넣었다. 빠뜨리면 아이를 바꿔도 같은 캐시가 나왔다. 카테고리·주소 검색·리뷰 사진·반려동물 상세와 정적 목록(`catalog`) 키를 더했다 (#133)

### 수정

- 로그인 전에 인증이 필요한 요청을 한 번이라도 보냈으면 그 뒤 로그인해도 토큰 재발급이 영영 건너뛰어지던 문제를 고쳤다. refreshToken 없이 끝난 재발급 Promise가 지워지지 않고 남아 있었다 (#133)

---

## 2026-09-08

### 추가

- 상품 상세 화면을 만들었다(`/products/[productId]`). 전에는 `productId`와 리뷰 링크만 있는 자리 표시였다. 이미지 자리·가격·배송 표, 적합도, 함께 보면 좋은 상품, 상품 정보·리뷰·Q&A 탭, 찜·장바구니·바로 구매를 담았다 (#130)
- 적합도와 근거를 가격 바로 아래 둔다. 스펙을 다 읽고 나서야 판단하게 하지 않고, 살지 말지를 정하는 자리에서 근거를 먼저 보인다 (#130)
- 근거에는 지켜볼 점도 함께 적는다. 도움되는 것만 늘어놓으면 광고와 구별되지 않는다 (#130)
- 기준이 되는 아이를 바꾸면 점수·근거·영양 분석이 통째로 바뀐다. 잴 수 없는 아이에게는 점수를 채우지 않고 "아직 재지 못했어요"로 보인다 (#130)
- 영양 성분을 부족–적정–과다 막대로 보인다. 시안이 색으로만 가르는 것을 값 옆에 구간 이름을 붙여 색을 구분하기 어려워도 읽히게 했다. 절대 기준치가 없는 오메가3에는 눈금 대신 그 사실을 적는다 (#130)
- 값이 기준 안에 들었다는 뜻의 `success` 색 토큰을 더했다. `destructive`의 짝인데 shadcn 기본 팔레트에 없었다. 브랜드 색과 같이 디자인 확정 전 임시값이다 (#130)
- 영양 구간 이름을 숨은 글자가 아니라 값 배지에 눈에 보이게 적는다. 숨겨 두면 화면 낭독기 사용자에게만 닿고, 화면은 보이지만 색을 가리기 어려운 사람에게는 색이 여전히 유일한 단서로 남는다 (#130)
- 색 위에 얹는 글자색을 `destructive-foreground`·`success-foreground`로 나눠 정의했다. 어두운 화면의 `destructive`에 흰 글자를 얹으면 대비가 2.89:1, `success`는 3.26:1로 작은 글씨 기준(4.5:1)에 못 미쳤다 (#130)
- 공유 버튼이 실제로 주소를 복사한다. 전에는 복사하지 않고 성공 알림만 띄워, 붙여넣을 것이 없는데 복사된 줄 알게 했다 (#130)
- 종합 점수 카드를 점수와 한 줄이 다 있을 때만 그린다. 성분은 받았는데 점수를 못 받으면 "종합 점 — "처럼 글자가 빠진 문장이 화면에 남았다 (#130)
- 아이 고르기 목록을 적합도에서 파생시켰다. 목록을 따로 두면 적합도가 없는 아이를 고른 순간 이름은 그 아이인데 근거는 다른 아이 것이 붙어, **한 화면에 두 아이 이름이 섞인 거짓 근거**가 됐다 (#130)
- 함께 보면 좋은 상품 카드를 누를 수 없게 했다. 목데이터가 한 상품뿐이라 어느 카드를 눌러도 같은 화면이 나와서, 링크를 살려 두면 화면이 거짓말을 했다 (#130)
- `brand` 바탕 위 글자색도 어두운 색으로 바꿨다. 흰 글자면 3.11:1이다. 타임딜 품절임박 배지와 메인 알림 배지가 함께 고쳐졌다 (#130)

### 수정

- 온보딩에서 새로고침해도 입력하던 값이 남게 했다. 단계는 주소에 있는데 입력값이 화면 안 상태여서, `?step=health`로 새로고침하면 단계만 남고 종이 강아지로 돌아가 **고양이 보호자가 강아지 질환 갈래를 만났다** (#126)
- 사진은 남기지 않는다. 파일은 저장소에 담기지 않고, 다시 고르는 것이 한 번의 탭이다 (#126)
- 등록을 마치면 남겨 둔 초안을 지운다. 남기면 다음에 들어올 때 앞 사람의 값이 채워져 보인다 (#126)
- 저장된 값이 보기에 없는 것이면 안 고른 것으로 되돌린다. 성별·중성화·체구와 체형 단계가 그렇다. 체형은 배열 자리를 가리키는 숫자여서 범위를 벗어나면 설명이 비어 나온다 (#126)

### 개발 환경

- 화면에서 요소를 지우거나 이름을 바꿨으면 e2e를 통째로 돌리도록 검증 규칙에 적었다. 지운 요소를 다른 spec이 어떤 이름으로 붙잡고 있는지는 검색으로 다 찾지 못한다 (#124)
- 효과 안에서 setState를 부르지 못할 때 무엇을 하면 되는지 세 갈래로 나눠 적었다. 전에는 lint를 끄는 예외만 있어서, 시트 상태를 되돌리거나 저장소를 읽는 경우에 답이 없었다 (#124)
- 기기 저장소를 만들 때의 골격도 함께 적었다. 캐시 한 칸·서버 스냅숏·저장값을 한 칸씩 확인해 옮기기까지 같은 모양이 되풀이된다 (#124)

---

## 2026-09-07

### 추가

- 적합도를 재지 못한 상품에 "정보 확인 중"을 보인다. 영양 정보가 등록되지 않아 계산이 안 되는 상품이 생기는데, 0점으로 내려보내면 "확인이 필요해요"로 읽혀 궁합이 나쁜 상품처럼 보였다. 재 봤더니 안 맞는 것과 아직 재지 않은 것은 다른 이야기다 (#119)
- 그런 상품은 검색 결과에서 어떤 정렬을 골랐든 마지막에 온다. 점수를 모르는 상품이 가격순 첫 줄에 오면 무엇을 기준으로 고르는지가 흐려진다 (#119)
- 검색 결과 화면을 만들었다(`/search/result`). 검색어에 걸린 상품을 2열로 보이고, 카드마다 우리 아이 적합도와 하루 예상 급여비를 함께 둔다. 정렬은 IA대로 추천순을 기본으로 인기순·낮은 가격순·높은 가격순·리뷰 많은순 다섯이다 (#113)
- 결과 화면의 검색바는 입력창처럼 보이지만 버튼이다. 누르면 검색 화면으로 되돌아간다. 결과를 보다 검색어를 고치는 자리라 여기서 다시 치게 하지 않았다 (#113)
- 걸리는 상품이 없으면 "검색 결과가 없어요"를 보인다. 셀 것이 없으므로 개수와 정렬은 함께 감춘다 (#113)
- 적합도가 계산되지 않은 상품에 "정보 확인 중"을 붙이는 것은 넣지 않았다. 공용 적합도 뱃지를 함께 고쳐야 해서 별도로 뺀다 (#113)

### 변경

- 걱정되는 질환 목록을 기능명세서 v0.4의 확정 데이터로 갈아끼우고 **종별로 갈랐다.** 강아지 11갈래·고양이 12갈래다. 전에는 종 구분 없는 6갈래여서, 고양이 보호자에게 `십자인대 질환`을 강아지 보호자에게 `헤어볼`을 보이고 있었다 (#121)
- 품종을 바꿔 종이 달라지면 앞서 고른 질환을 비운다. 강아지로 고른 `슬개골 탈구`가 고양이 프로필에 남으면 추천 근거가 거짓이 된다 (#121)
- 품종 목록을 기능명세서 v0.4의 확정 데이터로 갈아끼웠다. 강아지 23 → 35, 고양이 15 → 23이고, `말티푸`처럼 흔한데 고를 수 없던 품종이 들어왔다 (#122)
- 표기도 함께 맞췄다. `코카 스파니엘` → `코커 스패니얼`, `프렌치 불독` → `프렌치 불도그`, `뱅갈` → `벵갈` 같은 것들이다. 표기가 다르면 서버가 내려주는 값과 대조가 안 된다 (#122)
- `푸들`이 `토이 푸들`·`스탠다드 푸들`·`미니어처 푸들` 셋으로 갈렸다. 체구가 다르면 급여량과 적합도 계산이 달라진다 (#122)
- `기타`를 골라도 종이 뒤집히지 않게 했다. 확정 데이터에서 `기타`가 강아지·고양이 양쪽에 있어 이름만으로는 가를 수 없다. 프로필이 든 종을 함께 넘긴다 (#122)
- 마이페이지 건강 정보 수정을 온보딩과 같은 선택형으로 바꿨다. 온보딩만 고쳐 두어, "슬개골 탈구"를 골라 저장한 사람이 수정 화면에서는 자유 입력창을 만났다. 한쪽만 자유 입력이면 같은 질환이 여러 표기로 쌓여 추천에 쓸 수 없다 (#117)
- 해당 없음을 켠 자리는 시안대로 "해당 사항 없음"으로 바뀐다 (#117)
- 고르는 자리를 `entities/pet`의 `HealthPickerField`로 올려 온보딩과 수정 화면이 같은 것을 쓴다. 겉모습도 동작도 한 곳에서만 바뀐다 (#117)
- 온보딩 알러지 자리에도 시안의 예시 문구(`ex) 복숭아, 닭, 연어, 밀가루 등`)를 넣었다. `onbo_004`에 있는데 빠져 있었다 (#117)
- **아이 정보 수정의 머리말은 `mypa_321`을 따르지 않고 `정보 수정`을 유지했다.** 같은 흐름인 `mypa_121`·`mypa_221`은 `< 정보 수정`인데 `mypa_321`만 온보딩의 진행 표시 세 칸이 붙어 있다. 마이페이지에서 들어온 화면에 온보딩 진행률이 뜨는 것은 말이 되지 않는다. PD 확인이 필요하다 (#117)
- 온보딩에서 건너뛰기와 닫기 버튼을 뺐다. 시안 `onboarding` 섹션이 완료로 바뀌면서 두 버튼이 사라졌고, 섹션 메모도 "건너뛰기, 닫기 버튼 삭제"라고 적고 있다. 도입부는 "프로필 입력하기" 하나만 전체 폭으로 놓인다 (#115)
- 닫기와 함께 이탈 확인 모달도 걷어냈다. 여는 곳이 없어져 쓰이지 않는 코드가 된다 (#115)
- **이제 화면 안에서 온보딩을 떠나는 길이 없다.** 회원가입 직후 반드시 거치는 단계라는 뜻으로 읽히지만, 기기 뒤로가기 말고 나갈 방법이 없다는 점은 PD 확인이 필요하다 (#115)
- 검색 화면에서 검색어를 넣으면 이제 결과 화면으로 간다. 전에는 최근 검색어에 기록만 남기고 입력이 비어 갈 곳이 없었다 (#113)
- 최근 검색어를 기기에 남긴다. 검색하면 결과 화면으로 떠나게 되어, 돌아왔을 때 방금 검색한 말이 사라지면 최근 검색어가 동작하지 않는다. 서버에 둘지 기기에 둘지는 API 계약과 함께 정한다 (#113)
- 최근 검색어를 다섯 개까지만 남긴다. IA가 최대 5개로 못 박고 있는데 상한이 없었다 (#113)

### 개발 환경

- `views/README.md`의 슬라이스 표를 실제 구현 상태에 맞췄다. 타임딜·검색·맞춤 추천·로그인·회원가입·좋아요·리뷰 작성·알림 여덟 곳이 만들어 두고도 "(자리 표시)"로 남아 있어, 표만 보면 아직 없는 화면으로 읽혔다 (#111)
- 검색 슬라이스 설명이 "검색 결과"라고 적혀 있던 것을 "검색 입력"으로 바로잡았다. 만든 것은 최근 검색어와 추천어를 보이는 화면이다 (#111)

---

## 2026-09-04

### 추가

- 로그인 화면을 만들었다(`/login`). 아이디·비밀번호와 자동로그인, 소셜 두 갈래(카카오·구글)를 둔다. 시안에는 소셜이 넷 그려져 있으나 카카오·구글로 확정된 것에 맞췄다 (#107)
- 회원가입 화면을 만들었다(`/signup`). 약관 동의와 닉네임을 차례로 받는다. 전체 동의를 누르면 그 묶음이 한꺼번에 켜지고, 필수를 다 채우기 전에는 다음으로 갈 수 없다 (#107)
- 약관 줄에서 설명 문구를 눌러도 체크가 바뀌지 않게 했다. 한 줄에 체크와 본문 이동 두 동작이 붙어 있어, 설명을 읽으려다 동의가 풀리면 다시 채워야 한다 (#107)
- 아이디·비밀번호 찾기는 링크를 걸지 않았다. 갈 화면이 시안에 아직 없어 링크를 걸면 404가 된다 (#107)
- **두 화면 모두 실제 인증은 붙지 않았다.** 아이디·비밀번호도 소셜도 눌러서 들어가지지 않는다. 백엔드 인증 방식이 정해져야 이어진다 (#107)
- 필수 약관을 채우지 않으면 주소로 닉네임 단계에 들어갈 수 없게 했다. 그대로 두면 `?step=nickname`으로 열어 동의 없이 가입을 끝낼 수 있었다 (#107)

### 변경

- 온보딩에서 건강 관심사와 알러지 성분을 목록에서 고르게 바꿨다. 전에는 글자를 직접 쳤는데, 보호자마다 다르게 적으면 같은 질환이 여러 표기로 쌓여 추천에 쓸 수 없다. 시안대로 탭 여섯(관절·알러지·체중·장 건강·구강 관리·노화)으로 나눠 여러 개를 고른다 (#109)
- 고른 것을 입력창 자리에 칩으로 되보인다. 시트를 다시 열지 않아도 무엇을 골랐는지 알 수 있다 (#109)
- 질환·성분 목록은 아직 더미다. 시안의 칩이 목 데이터여서 — 관절 탭 다섯 중 넷이 같은 이름으로 복붙돼 있었다 — 통용되는 분류로 채웠다. 실제 목록이 오면 `entities/pet/model/health.ts` 한 파일만 갈아끼운다 (#109)
- 시트의 탭과 고르는 칩이 최소 탭 영역(44×44)을 채우게 했다. 탭이 31px 높이에 두 글자짜리는 28px 폭이라 손가락으로 정확히 누르기 어려웠다. 같은 문제가 있던 타임딜 탭도 함께 고쳤다 (#109)
- 고른 칩에 체크 표시를 더했다. 색만으로 알리면 색을 구분하기 어려운 사람이 무엇을 골랐는지 알 수 없다 (#109)
- 체형(BCS) 안내를 모달에서 아래에서 올라오는 시트로 옮겼다. 시안 메모가 "물음표를 클릭하면 액션시트로 뜬다"고 적고 있다. 낮은 화면에서 잘리지 않는지 보던 회귀 테스트는 시트 기준으로 이어 간다 (#109)

### 수정

- 탭이나 단계를 옮긴 뒤 뒤로가기를 누르면 이전 상태로 돌아오게 했다. nuqs 기본값이 replace라 히스토리에 쌓이지 않아 화면을 통째로 떠났다. 좋아요·나의 후기·아이 제품 관리의 탭, 메인의 종류, 온보딩의 단계 다섯 곳이다 (#105)
- 온보딩에서 이름·품종까지 입력하다 뒤로가기를 눌러도 입력값이 남게 했다. 전에는 온보딩을 통째로 떠나 처음부터 다시 써야 했다. WebView 앱으로 감쌀 예정이라 기기 뒤로가기가 실제 사용 경로다 (#105)
- 필터·정렬은 히스토리에 쌓지 않는 쪽을 그대로 뒀다. 같은 목록을 좁히는 것이라 쌓이면 뒤로가기를 여러 번 눌러야 화면을 떠난다 (#105)
- 아이 제품 관리의 후기 필터가 주소로 아무 값이나 받던 것을 보기 안에서만 받게 했다 (#105)
- 맞춤 추천에서 주소에 없는 아이 id가 오면 고르는 자리가 비어 보이던 것을 고쳤다. 본문은 첫 아이를 보여주는데 고르는 자리만 빈 값이라 서로 달라 보였다 (#105)

---

## 2026-09-03

### 추가

- 에러 응답 타입에 백엔드 공통 구현의 확장 필드를 반영했다. `errorCode`는 사용자 문구·동작 분기의 기준이 되고, `fieldErrors`는 폼 검증 실패를 필드별로 표시할 때 그대로 쓴다. 백엔드 common-core의 GlobalExceptionHandler가 내려주는 실제 응답과 대조해 맞췄다 (#102)
- 검색 화면을 만들었다(`/search`). 최근 검색어와 카테고리 바로가기로 시작하고, 글자를 넣으면 추천어가 그 자리를 넘겨받는다. 들어오면 입력창에 초점이 바로 가서 한 번 더 누르지 않아도 칠 수 있다 (#101)
- 최근 검색어를 하나씩 지우거나 한 번에 지울 수 있게 했다. 다 지우면 무엇을 하면 되는지 안내로 바뀐다. 같은 말을 다시 검색하면 목록 맨 앞으로 올라오고 중복은 남지 않는다 (#101)
- 추천어에서 입력한 부분을 굵은 브랜드 색으로 강조한다. 색만으로 알리면 구분하기 어려운 사람이 왜 이 말이 떴는지 알 수 없어 굵기를 함께 준다 (#101)
- 리뷰 작성 화면을 만들었다(`/mypage/reviews/write`). 별점만 받지 않는다 — 기호성·소화 반응·급여 편의성을 함께 묻는다. 보호자가 그동안 혼자 추측하던 신호이고, 여기서 모인 것이 다음 추천의 근거가 된다 (#97)
- 어느 아이가 먹었는지를 필수로 받는다. 같은 사료라도 아이마다 반응이 달라, 아이를 모르면 그 답을 다음 추천에 쓸 수 없다 (#97)
- 별을 눌러 점수를 매기는 것을 만들었다. `shared/ui/rating`은 보여주기만 해서 매기는 쪽이 없었다. 시안의 반쪽 별은 쓰지 않았다 — 별 하나를 좌우로 가르면 탭 영역이 22px로 줄어 최소 44px 기준을 못 맞춘다 (#97)
- 사진을 최대 세 장 붙이고 뺄 수 있게 했다. 고른 파일은 화면이 들고만 있는다 — API를 연동하면 등록 요청에 함께 실을 자리다. 미리 올려 두면 쓰다 말았을 때 서버에 사진만 남는다 (#97)
- 칩 고르기에서 아직 안 고른 상태를 넘겼을 때 나던 controlled 경고를 없앴다. Radix가 처음 `undefined`를 받으면 uncontrolled로 보고, 고르는 순간 바뀌었다고 알렸다 (#97)
- 타임딜 화면을 만들었다(`/deals`). 진행 중인 딜은 종료까지 남은 시간과 함께 보여주고, 시간이 다 되면 목록도 같이 사라진다. 세는 것만 멈추면 살 수 없는 상품이 그대로 남는다 (#95)
- 타임딜 목록에서 상세로 가지 않고 바로 구성과 수량을 골라 담게 했다. 시간에 쫓기는 화면이라 단계를 줄였다. 담고 나면 그 자리에서 담긴 것으로 바뀐다 (#95)
- 오픈 예정 딜에 알림 신청을 붙였다. 신청하고 나면 남은 시간 대신 몇 시에 열리는지를 남긴다 — 알림을 받기로 한 이상 초를 지켜볼 이유가 없다 (#95)
- 상품 행(`shared/ui/product-summary`)에 이미지 위 뱃지와 오른쪽 액션 자리를 열었다. 품절임박 표시와 담기 버튼이 붙는 자리다 (#95)
- 타임딜 탭을 뒤로가기로 되돌아올 수 있게 했다. nuqs 기본값이 replace라 뒤로가기가 탭 전환을 건너뛰고 화면을 떠났다 (#95)
- 메인 화면을 만들었다(`/`). 전체 탭은 프로모션 배너·아이 고르기·맞춤 상품·상태 체크·타임딜을 모은 큐레이션이고, 종류 탭은 정렬이 붙은 2열 상품 목록이다. 같은 화면의 필터가 아니라 구성이 통째로 다르다 (#93)
- 최근에 산 제품이 아이에게 맞았는지 묻는 시트를 만들었다. 잘 맞았어요·그냥 그랬어요·안 맞았어요 셋과 "아직 판단하기에는 일러요"를 받고, 등록하면 그 반응이 다음 추천 적합도에 반영된다고 알린다. 이 서비스가 근거를 모으는 자리라 그 약속을 화면에 적었다 (#93)
- 손으로 밀어 넘기는 가로 목록(`shared/ui/scroll-row`)과 남은 시간을 세는 표시(`shared/ui/countdown`)를 만들었다. 가로 목록은 캐러셀이 아니다 — 저절로 넘어가면 읽는 도중에 내용이 바뀌어 방해가 된다 (#93)
- 시안의 주황을 `--brand` 토큰 한 곳에 담았다. 브랜드 컬러가 아니라 임시값이라, 색이 정해지면 `globals.css`의 두 줄만 갈아끼우면 된다 (#93)
- 아이 고르기 줄이 이름을 함께 보일 수 있게 했다(`withNames`). 메인처럼 처음 보는 화면에서는 사진만으로 어느 아이인지 알 수 없다 (#93)
- 아이 이름 뒤 조사를 받침에 맞춰 고르는 유틸을 만들었다(`shared/lib/josa`). 이름이 서버에서 오므로 "햇살는 어때요?" 같은 비문이 그대로 화면에 나간다 (#93)
- 타임딜 시간이 다 되면 화면이 함께 바뀌게 했다. 세는 것만 멈추고 목록과 "종료까지 남은 시간"이 그대로 남아 있었다 (#93)
- 반응을 고르면 "아직 판단하기에는 일러요"가 함께 풀리게 했다. 둘 다 켜진 채로 남으면 무엇을 답한 것인지 알 수 없다 (#93)
- 종류 고르기 줄에서 탭 역할을 걷어내고 `aria-current`로 알리게 했다. 고르면 화면 구성이 통째로 바뀌고 주소도 달라져 연결할 패널이 없다 (#93)
- 만들어 둔 화면 목록을 `/dev/screens`로 옮겼다. 메인이 `/`를 차지했고, 자리 표시 화면이 남아 있는 동안은 그 목록이 여전히 쓸모 있다 (#93)
- 맞춤 추천 화면을 만들었다(`/recommendations`). 고른 아이의 건강 고민에 맞는 상품을 격자로 보여주고, 카드마다 적합도와 하루 예상 급여비를 함께 읽힌다. 무엇을 근거로 골랐는지를 화면에 적는다 — 별점·인기순 나열과 갈라지는 지점이라 근거가 보이지 않으면 이 화면의 의미가 없다 (#89)
- 좋아요 화면을 만들었다(`/likes`). 찜한 상품·최근에 본 상품·자주 산 상품을 탭으로 나눈다. 자주 산 상품에는 구매 횟수와 장바구니·구매하기가 붙고, 빼기는 되돌릴 수 없어 확인창으로 막는다 (#90)
- 아이 제품 관리 탭에 후기 작성 여부로 거르는 칩을 넣었다. 시안에 수정 요청으로 표시된 자리다 (#91)
- 거르는 칩이 주소로 아무 값이나 받던 것을 보기 안에서만 받게 했다. 목록에 없는 값이 오면 걸러 낸 결과가 비어 화면이 통째로 사라진다 (#89 #90)
- 찜을 풀면 목록에서도 빠지게 했다. 하트만 비우고 목록에 남기면 푼 것이 아니다. 되돌릴 수 없는 동작이라 다른 탭과 같이 확인창을 거친다 (#90)
- 목록을 거르는 칩을 `shared/ui/filter-chips`로 올렸다. 알림 화면에서 만들었던 것인데 아이 제품 관리·좋아요·맞춤 추천에서도 같은 모양이 나왔다 (#91)
- 상품 격자 카드가 화면마다 다른 것을 얹을 수 있게 됐다. 적합도·구매 횟수는 이미지 안에, 찜 하트나 지우기는 이미지 위에, 장바구니·구매하기는 카드 아래에 자리로 받는다. 화면 수만큼 boolean을 늘리는 대신 무엇을 놓을지 넘긴다 (#89 #90)
- 체형(BCS)이 무엇인지 알려주는 모달을 붙였다(`onbo_003_bcs툴팁`). 만져서 판단하는 기준이라 다섯 단계 이름만으로는 고를 수 없어, 물음표를 누르면 각 단계가 무엇을 뜻하는지 나온다. 온보딩과 아이 정보 수정 두 곳에서 쓴다 (#87)

### 개발 환경

- 콘솔 출력과 `dangerouslySetInnerHTML`을 lint로 막았다. 사이버 보안팀 시큐어 코딩 가이드 v1.0의 SC-G-02(로그에 민감정보 금지)와 SC-B-01(XSS) 중 기계가 판정할 수 있는 두 가지다. 켜기 전에는 둘 다 경고 없이 통과했다 (#99)
- `console.error`·`console.warn`도 함께 막았다. 가이드가 로그 레벨로 구분하지 않고, `console.error(err)`에 응답 객체가 통째로 들어가면 토큰이 찍힌다. 오류는 `react-error-boundary`가 잡고 사용자에게는 토스트로 알린다 (#99)

## 2026-09-02

### 추가

- API 클라이언트에 인증을 붙였다. 토큰이 있으면 모든 요청에 Bearer 헤더가 붙고, 401이면 재발급 후 원 요청을 한 번 다시 보낸다. 백엔드가 재발급에 rotation을 적용해 중복 호출이 탈취로 간주되므로, 동시에 여러 요청이 만료를 만나도 재발급은 한 번만 나가게 묶었다. accessToken은 메모리에, refreshToken은 localStorage에 둔다 — httpOnly 쿠키는 WebView 구글 로그인 차단 정책으로 백엔드가 리다이렉트 쿼리 방식을 확정하면서 제외됐다 (#83)
- 알림 화면을 만들었다(`/mypage/notifications`). 공지와 배송 알림을 한 목록에서 보고, 전체·새 알림·확인한 알림으로 거른다. 누르면 상세가 모달로 열리고 배송 알림에서는 주문 내역으로 이어진다 (#79)
- 읽지 않은 알림을 점으로 표시하면서 "읽지 않음"을 문장으로도 읽히게 했다. 점만 찍으면 스크린 리더가 아무것도 알리지 못한다 (#79)
- shadcn `Dialog`를 새로 받았다. `AlertDialog`는 사용자의 응답을 요구하는 경고용이라 스크린 리더가 즉시 알리는데, 항목을 눌러 여는 정보 전달 모달에는 `dialog`가 맞다 (#79)
- 아이 정보 수정에서 품종을 고를 수 있게 했다(`?picking=breed`). 목록이 길어 별도 단계에서 골라야 하는데 갈 곳이 없어 그 줄을 잠가 뒀던 자리다 (#81)
- 반려동물 프로필의 제품 후기에서 "수정하기"를 이었다. 리뷰 상세 경로가 생겨 갈 곳이 정해졌다 (#81)
- 품종 고르기 화면(`BreedPickerStep`)과 종을 되찾는 `findSpecies`를 `entities/pet`으로 올렸다. 온보딩 안에만 있던 것을 정보 수정도 쓰게 됐다 (#81)
- **품종 고르기를 별도 라우트로 만들었다가 되물렸다.** 라우트를 나누면 정보 수정 화면이 언마운트되어 입력하던 이름·나이·성별이 저장값으로 되돌아간다. 리뷰에서 지적받아 실제로 재현해 보고 같은 화면 안의 단계로 바꿨다 (#81)
- 휴대폰 인증을 목업으로 채웠다. 인증을 누르면 인증번호가 채워지고 실제 문자는 가지 않는다. 문자 발송은 건당 요금이고 무료 지원은 사업자등록이 있어야 하는데 우리는 없다. 화면과 흐름은 그대로 두라는 PM 방침이라 기능 명세에는 남기고 구현만 덜어냈다 (#85)

### 수정

- 새로 받은 `Dialog`의 오버레이가 `bg-black/10`이어서 `/50`으로 맞췄다. `drawer`·`alert-dialog`가 이미 `/50`이고, 옅으면 뒤 화면이 그대로 읽혀 무엇이 잠긴 상태인지 알기 어렵다 (#79)

## 2026-09-01

### 추가

- PRD v0.3·IA v0.6·UCS v0.1의 확정 범위를 기준으로 로그인·회원가입·검색·추천·상품과 마이페이지 하위 화면의 진입점을 만들었다. 실제 화면이 없는 곳은 자리 표시 화면으로 구성하고 목적지가 확정된 기존 메뉴와 링크를 이었다 (#74)
- 화면별 경로·접근 정책·URL 상태와 미확정 결정을 `docs/routing.md`에 정리했다. 작성 중인 IA v0.7은 확정 기준으로 적용하지 않고 후속 변경 가능성을 확인하는 참고 자료로 구분했다 (#74)
- 반려동물 프로필 관리 화면을 만들었다. 아이의 정보와 그 아이가 먹은 제품을 탭으로 나눠 보고, 제품을 누르면 좋았던 점과 아쉬운 점을 나눠 보여준다 (#69)
- 아이 정보를 항목별로 고칠 수 있다. 이름·품종·나이·성별을 한 화면에서, 체구와 몸무게를 다른 화면에서, 염려질환과 알러지를 또 다른 화면에서 고친다 (#69)
- 여러 아이를 키우면 화면 아래 줄에서 오갈 수 있다. 고른 아이를 테두리로도 알린다 — 색만으로는 어느 것이 골라졌는지 알 수 없다 (#69)
- 새 아이를 들이는 화면을 만들었다(`/mypage/pets/new`). 아이 고르기 줄의 "+"에서 들어간다. 등록 뒤에 이어지는 화면이 시안에 없어 "아이 추가하기"는 아직 잠겨 있다 (#69)
- 마이페이지와 내 정보에서 막아 두었던 반려동물 진입점 두 곳을 되살렸다 (#69)
- 상품 비교 5화면을 만들었다. 사료 두 개를 담아 10g당 가격·주원료·핵심 기능성 같은 아홉 항목으로 견준다. 시안 `compare` 섹션이 확정돼 마이페이지보다 먼저 착수했다 (#67)
- 비교표를 `<table>`로 짰다. 화면에서는 항목 이름이 가운데 있어 눈으로 좌우를 견주지만, 스크린 리더는 "어느 상품의 어느 항목"인지 읽어야 값의 의미가 산다. 항목 이름을 그 줄의 머리글로 두고 표 이름에 두 상품을 넣었다 (#67)
- 화면 하단 이동 줄을 만들었다(`widgets/bottom-nav`). 홈·상품비교·좋아요·마이페이지 네 항목이고 `widgets` 레이어의 첫 슬라이스다. 홈과 좋아요는 갈 화면이 없어 표시만 한다 (#67)
- 주문 상세 화면을 만들었다. 주문정보·결제상세·배송지 정보를 카드로 나눈다. 주문 목록의 "자세히 보기"가 갈 곳이 없어 잠가 뒀던 것을 이제 이었다 (#67)
- 결제 3화면을 만들었다. 결제하기에서 배송지·결제 정보·결제 방법을 한 화면에 두고, 배송지 설정과 주문 완료로 이어진다. 결제 수단에서 페이결제를 고르면 어느 페이인지 한 번 더 묻는다 (#71)
- 실제 결제 승인은 붙이지 않았다. 시크릿 키를 쥔 백엔드가 승인과 금액 위변조 검증을 맡고, 사업자등록이 없어 실결제 자체가 아직 불가하다. 일반카드결제는 카드 등록 화면(`mypa_151`)과 맞물리는데 그 화면이 카드번호·CVC를 직접 받는 형태라 PCI DSS 확인 대기 중이다 (#71)
- 항목-값 줄을 제목 아래로 묶는 `DetailCard`를 공용으로 올렸다. 주문 상세에서 만든 것을 결제 화면이 그대로 쓰게 됐다 (#71)
- 장바구니 화면을 만들었다(`/cart`). 고른 상품만 합계에 넣고, 하나도 고르지 않으면 배송비도 물리지 않고 결제하기가 잠긴다. 옵션 변경은 바텀시트로, 빼기는 되돌릴 수 없어 확인창으로 막는다 (#72)
- 수량을 올리고 내리는 `QuantityStepper`를 공용으로 올렸다. 값이 바뀌면 스크린 리더가 알리도록 라이브 영역으로 뒀다 — 숫자만 바뀌는 자리라 알리지 않으면 눌러도 아무 반응이 없는 것으로 읽힌다 (#72)
- API 연동의 공통 기반을 만들었다. 백엔드 명세의 GET 엔드포인트를 따라 도메인별 Query Key factory를 두고, 모든 요청이 지나는 fetch 래퍼는 헤더 조립을 한 곳으로 모아 JWT 방식이 확정되면 그 지점만 고치면 된다. 실패 응답은 Spring 표준 ProblemDetail(RFC 9457)로 파싱하고, 요청·응답 타입은 Swagger 확정 전이라 호출부 제네릭으로 비워 뒀다 (#65)

### 수정

- 주문 상세의 리뷰 작성 링크가 주문 ID에 `-1`을 붙여 주문상품 ID를 임의로 만들던 것을 고쳤다. 둘의 관계는 API 계약 확정 전이라 목값을 따로 뒀다. 1:1 문의 화면도 새 문의 작성이 외부 페이지로 연결된다고 단정하던 문구를 정책 확정 전 상태로 맞췄다 (#74)
- 자리 표시 화면 네 곳(상품 상세·배송지 관리·주문 상세·서비스 안내)의 밑줄 텍스트 링크가 최소 탭 영역(44px)에 못 미쳐 넓혔다 (#74)
- 배송지 화면에 "받는 분 이름"이 빠져 있던 것을 채웠다 — 시안(`mypa_311`)에는 배송지 이름 다음에 받는 분 이름이 있는데 옮기면서 놓쳤다. 마이페이지 시안이 "완료"로 확정된 뒤 상태 변화 프레임까지 대조하다 찾았다 (#67)
- 주소 검색 결과의 항목 이름을 시안에 맞췄다 — 우편번호에 이름이 없었고 "지번"이라 부르던 것이 시안에서는 "구주소"다. 건물명은 도로명 주소에 이미 들어 있어 줄을 나누지 않는다 (#67)
- 고른 상품이 비교 화면에 담기지 않던 것을 이었다 — 선택 화면이 아무것도 넘기지 않고 뒤로만 가서, 고르고 완료해도 결과가 사라졌다. 어느 자리에 무엇을 담았는지를 주소창으로 넘긴다. 새로고침·뒤로가기에서 남아야 하는 값이고 라우터 구조가 바뀌어도 이 규약은 그대로 쓸 수 있다 (#67)
- 주문 상세가 어느 주문을 눌러도 같은 내용을 보여주던 것을 고쳤다 — 라우트가 `[id]`인데 그 값을 읽지 않았다 (#67)
- 하단 이동 줄의 홈이 잠겨 있던 것을 이었다. 루트는 정확히 같을 때만 현재 화면으로 본다 — `startsWith`로 판정하면 모든 경로에서 홈이 켜진다 (#67)
- 좁은 자리에서 금액이 "26,000 / 원"처럼 잘려 읽히던 것을 고쳤다. 금액 한 덩이는 붙여 두고, 자리가 모자라면 원가만 아래 줄로 내린다 (#72)
- 마이페이지 머리말의 장바구니 아이콘을 이었다. 갈 화면이 없어 표시만 하던 자리다 (#72)
- 손가락으로 누르기 어려운 자리 세 곳을 넓혔다. 결제 수단 라벨과 장바구니 전체선택은 라디오·체크박스가 작아 라벨까지가 누르는 자리인데 글자 높이만큼만 눌렸고, 배송지 수정하기 링크도 같았다 (#72)
- 화면 목록 문서에서 빠져 있던 슬라이스 세 개(상품 비교, 비교할 상품 고르기, 주문 상세)를 채웠다. 앞선 작업에서 화면만 만들고 목록에 올리지 않았다 (#72)
- 리뷰 반영 — base URL 환경 변수를 빈 값으로 두면 "비우면 /api/v1" 규칙이 적용되지 않고 same-origin 루트로 요청되던 문제를 고쳤다 (#65)

### 개발 환경

- 파일 여러 개를 커밋하면 pre-commit 훅이 끝나지 않던 문제의 원인을 좁히고 lint-staged를 걷어냈다. Windows에서 lint-staged가 `node_modules/.bin` 래퍼를 실행하는 태스크가 시작만 하고 끝나지 않는다. `eslint`뿐 아니라 `prettier`도 같고 16.x로 낮춰도 같아서, 훅이 파일 목록을 직접 넘기는 지금 방식을 유지하기로 했다. `.ts` 아홉 개를 스테이징한 커밋이 23초에 끝나는 것을 확인했다 (#53)
- 처음 적었던 진단(파일 목록이 넘어가지 않아 저장소 전체를 돈다)은 틀린 것으로 확인했다. `--debug` 로그의 `args: [ '--fix' ]`는 설정 문자열의 인자만 찍은 것이고 파일은 그 뒤에 붙는다. 인자를 파일로 받아 세어 아홉 개가 정상으로 넘어가는 것을 확인했고, 저장소 전체 린트도 50초라 멈춤의 이유가 되지 못한다 (#53)

- 개발용 갤러리(`/dev`)에 빠져 있던 공용 컴포넌트 열두 개를 채웠다. 화면 작업을 하면서 컴포넌트만 만들고 갤러리를 갱신하지 않은 것이 쌓여 있었다. 이 화면이 "무엇이 이미 있는가"의 목록이기도 해서, 비어 있으면 같은 것을 다시 만들게 된다 (#75)
- 화면 하나를 통째로 차지하는 골격(`SingleInputScreen`·`BreedPicker`·`BottomNav`)은 갤러리에 넣지 않고 어느 화면에서 볼 수 있는지 적었다. 중첩하면 머리말과 하단 버튼 줄이 두 개씩 생긴다 (#75)
- 공용 컴포넌트를 만들 때 갤러리도 함께 갱신하도록 컴포넌트 컨벤션에 절과 체크리스트 항목을 더했다 (#75)

- 시안 변경을 픽셀 비교로 확인하는 방식을 썼다. 이전 스냅샷과 새 이미지를 겹쳐 달라진 영역만 추리니 `mypage` 섹션에서 바뀐 것은 헤더 라벨 한 곳뿐이었다. 38개 프레임을 눈으로 다시 훑지 않고도 화면이 그대로임을 확인했다 (#67)

## 2026-08-31

### 추가

- 마이페이지 후기·결제수단·주문·고객지원·설정 14화면을 만들었다. 시안이 하루 사이에 14화면에서 31화면으로 늘어 이슈를 나눠 진행했다 (#59)
- 주문 상태 뱃지와 별점 표시를 만들었다. 둘 다 색이나 모양만으로는 값을 알 수 없어 문구를 함께 읽히게 했다 (#59)
- 재입고 알림 3화면을 만들었다. IA 화면 목록에는 없던 화면으로 와이어프레임에서 처음 나왔다. 목록과 고르는 모드를 오가며, 고르는 모드에서는 안내와 검색을 숨겨 목록에 집중하게 했다 (#58)
- 마이페이지 회원 정보와 배송지 14화면을 만들었다. 라우트로는 6개이고 나머지는 같은 화면의 상태 변화다. 네 화면이 "헤더 → 질문 → 입력 → 하단 완료"로 골격이 같아 `SingleInputScreen`으로 뽑았다 (#57)
- 주소 검색을 우리 화면으로 그린다. 디자인팀이 커스텀을 요청해 다음 우편번호 위젯을 쓰지 않고, 행정안전부 도로명주소 API 응답 필드에 맞춘 `AddressResultList`를 만들었다. 승인키가 노출되면 안 되므로 백엔드 프록시를 요청해 둔 상태다 (#57)
- 온보딩 프로필 등록 9화면을 만들었다. 반려동물 프로필은 RICE 1위이고 FE 공수가 9 MD로 가장 크다. 단계는 URL 쿼리로 다루고 입력값은 컴포넌트 상태로 든다 — 시안의 이탈 모달이 "저장되지 않아요"라고 알리기 때문이다 (#56)
- 온보딩 단계 관리에 `@use-funnel`을 쓰지 않기로 했다. `@use-funnel/next`의 `useNextRouter`가 `next/router`를 반환해 **Pages Router 전용**이며 App Router에서 동작하지 않는다. 이미 도입한 `nuqs`로 같은 일이 되고 "새로고침에 남아야 하는 상태는 URL"이라는 규칙과도 맞는다 (#56)

- 화면을 만들 때 되풀이되는 공용 컴포넌트 16종을 만들었다. IA 46개 화면의 단위기능을 기계로 집계해 반복 횟수를 세고, PD팀 와이어프레임(온보딩 8·마이페이지 14·재입고 3화면)에서 형태가 확정된 것부터 골랐다. 화면 상단 머리말은 8개 화면에 명시돼 있고 하단 버튼 줄은 시안 21화면에 나온다 (#49 #50 #51)
- 공용 컴포넌트를 한 화면에서 눈으로 확인하는 `/dev` 화면을 두었다. Storybook을 도입하지 않기로 한 대신이며 프로덕션 빌드에서는 열리지 않는다 (#49)

### 수정

- 설정 화면의 알림 행에서 하이드레이션이 깨지던 문제 해결 — 행 전체를 버튼으로 만들고 그 안에 스위치를 넣었는데 스위치도 버튼이라 HTML이 허용하지 않는 중첩이 됐다. 행을 레이블로 바꿔 어디를 눌러도 스위치가 눌리게 했다 (#59)
- 주소 검색 결과에서 같은 항목이 여러 개로 보이던 문제 해결 — 화면 확인용 목 데이터를 같은 값 네 개로 만들어 두어 검색 결과가 한 건씩 다르게 보이지 않았다. 서로 다른 주소로 바꾸고, 실제 API도 같은 도로명의 다른 건물을 돌려줄 수 있어 목록 키에 순번을 함께 쓴다 (#57)
- 화면이 없는 곳으로 이어지던 링크 11개를 걷어냈다 — 시안의 행과 화살표는 그대로 두되 누르면 404가 되던 이동만 뗐다. 배송지 두 곳은 시안상 이미 만든 화면과 같아 그쪽으로 이었다. 아직 갈 곳이 없는 줄을 위해 `ListRowStatic`을 두었다 (#59)
- 시안 프레임 번호가 한 칸씩 밀려 있던 것을 바로잡았다 — `mypa_021`은 재입고 알림이 아니라 반려동물 프로필 관리다. 그 화면을 빼고 세어 뒤가 모두 어긋났고, PD팀과 화면을 짚을 때 쓰는 번호라 파일 18개를 함께 고쳤다 (#59)
- 마이페이지 반려동물 줄에 시안의 아이 추가 자리를 넣었다 (#59)
- 화면을 시안과 하나씩 맞춰 보고 여덟 곳을 고쳤다 — 주소 검색의 입력 예시 안내(도로명·동주소·건물명)가 통째로 빠져 있었고, 주문 상태 뱃지가 상품명 옆이 아니라 카드 우상단에 있었다. 배송 시작 전 주문을 취소하는 길이 결제완료 상태에만 열려 있어 배송준비중에도 열었다. 재입고 안내에 시안에 없는 제목이 붙어 있던 것과 검색창 돋보기, 카드사 표시, 상품 이미지 자리, 배송지 문구도 함께 맞췄다 (#59)
- 바텀시트나 확인창을 열면 뒤에 깔린 화면이 한 줄로 접히던 문제 해결 — 화면 폭 제한을 `body`에 걸었는데, 시트는 포털로 `body` 바로 아래에 붙는다. 폭을 제한하는 flex 컨테이너에 포털이 들어오니 뒤 화면이 420px에서 32px로 짜부라졌다. 제한을 안쪽 `div`로 옮겨 포털과 갈라 놓았다 (#59)
- 시트가 열렸을 때 뒤 글씨가 뭉개져 보이던 문제 해결 — 오버레이가 10%로 너무 옅어 흐림 효과만 도드라졌다. 50%로 올려 뒤 화면이 자연스럽게 가라앉게 했다. 넓은 화면에서 시트가 화면 폭을 넘어 글자가 왼쪽 끝에 붙던 것도 함께 맞췄다 (#59)
- 눌러도 아무 일이 없던 컨트롤을 잠그거나 표시용 줄로 바꿨다 — 주문의 배송 조회·자세히 보기, 결제수단의 카드 등록·별명 바꾸기·카드 지우기, 설정의 테마·로그아웃·회원탈퇴가 모두 눌리는 모양만 하고 있었다. 링크는 앞서 뗐는데 버튼은 그대로 둬서 생긴 어긋남이다 (#59)
- 구매 확정과 주문 취소가 목록에 반영되지 않던 문제 해결 — 알림만 띄우고 상태를 그대로 둬서 같은 주문을 몇 번이고 확정할 수 있었다. 목 목록을 화면 상태로 들어 확정하면 상태가 바뀌고 취소하면 목록에서 빠진다 (#59)
- 기존 배송지를 눌러도 빈 입력 화면이 열리던 문제 해결 — 404를 피하려고 새 배송지 화면으로 보냈는데 어느 곳인지 넘기지 않아 값이 채워지지 않았다. 주소창에 어느 곳인지 담아 이미 저장된 값을 채운다 (#59)
- 별점이 만점을 바꿔도 스크린 리더에는 5점 만점으로 읽히던 문제 해결 (#59)
- 검색창 앞 돋보기가 클릭을 먹어 입력칸에 포커스가 가지 않던 문제 해결 (#59)
- 탭 터치 영역이 25px이라 컨벤션의 44px에 못 미치던 것을 맞췄다 (#59)
- 결제수단 안내가 카드 정보를 암호화해 보관한다고 단정하던 것을 고쳤다. 보관 주체와 계약이 정해지지 않았는데 사용자에게 약속하는 문장이었다 (#59)

### 개발 환경

- 만든 화면을 모두 열어 확인하는 검증을 붙였다(`e2e/screens.spec.ts`). 화면별 콘솔 오류, 넓은 화면에서 시트를 열었을 때 뒤 화면 폭, 화면에 걸린 링크가 실제로 열리는지를 본다. 눈으로 훑다가 하이드레이션 오류와 레이아웃 붕괴를 잇달아 놓쳐 기계가 보게 했다 (#59)
- 홈 화면을 임시 입구로 바꾸면서 기존 E2E가 옛 문구를 찾아 CI가 깨졌다. 새 화면에 맞춰 고쳤고 그룹 제목과 실제 이동까지 함께 본다. 화면을 바꿀 때는 그 화면을 보는 기존 테스트가 있는지 먼저 봐야 한다 (#59)

- 컴포넌트를 만드는 규칙을 `component-convention`으로 정하고 `/new-component` 커맨드를 붙였다. `shared/ui`와 `entities/*/ui`의 경계, 와이어프레임 근거를 파일 주석에 남기는 형식, Props·상태·접근성 규칙을 담았다 (#52)
- 라이브러리를 고르는 절차를 `library-convention`으로 정하고 `/check-library` 커맨드를 붙였다. 최근 며칠 사이 Webpack 전용 플러그인을 Turbopack 프로젝트에 넣거나 3년 방치된 패키지를 후보에 올리는 일이 반복돼, 무엇을 확인하는지 정해 두었다 (#54)
- `shared/ui` 전체가 prettier·ESLint 검사에서 빠져 있던 것을 shadcn CLI 생성물(`shared/ui/*.tsx`)로 좁혔다. 우리가 만든 컴포넌트는 하위 폴더에 두고 접근성 검사를 그대로 받는다 (#49)
- `.ts`·`.tsx` 파일 여러 개를 커밋하면 pre-commit 훅이 끝나지 않던 문제를 우회했다. lint-staged가 eslint에 파일 목록을 넘기지 못해 저장소 전체를 `--fix`로 돌리고 있었다. 스테이징된 파일을 직접 넘기는 방식으로 바꿨고 원인 규명은 별도로 남겼다 (#53)
- 접근성 린트 규칙을 6개에서 31개로 늘렸다. `eslint-config-next`가 켜 주는 것은 `alt-text`와 `aria-*` 계열뿐이라 레이블 없는 입력이나 키보드로 누를 수 없는 요소는 그냥 통과했다. Lighthouse 접근성 95점이 이 프로젝트의 차별화 축이고, 화면을 만들기 전에 켜야 위반이 쌓이지 않는다. 플러그인을 다시 등록하지 않고 규칙만 펼쳤다 — flat config에서 같은 이름을 다시 선언하면 설정 로드 자체가 실패한다 (#47)
- 번들 구성을 확인하는 `npm run analyze`를 추가했다. 차트처럼 무거운 의존성이 첫 화면에 딸려 들어갔는지 볼 수단이 없었다. `@next/bundle-analyzer`를 붙였다가 걷어냈다 — Webpack 전용이라 Turbopack 빌드에서는 리포트가 나오지 않았고, Next 16.1부터 들어온 `next experimental-analyze`가 같은 일을 한다 (#47)
- TanStack Query devtools를 개발 빌드에 넣었다. 캐시 상태를 눈으로 확인할 수 있고 프로덕션 번들에는 포함되지 않는다 (#47)
- 필터·정렬 상태를 URL 쿼리로 다루기로 하고 `nuqs`를 넣었다. 목록에서 상세로 갔다 돌아오면 고른 조건이 사라지는 것이 경쟁사 앱 리뷰에서 가장 많이 나온 불만이다. 컴포넌트 상태로 들면 목록 화면이 통째로 클라이언트 컴포넌트가 되어 서버 컴포넌트 우선 방침과도 어긋난다. `AGENTS.md`의 상태 관리 표가 필터를 Zustand 담당으로 적고 있어 함께 고쳤다. `useQueryState`는 기본이 `shallow: true`라 URL만 바뀌고 서버는 모르므로, 서버가 다시 조회해야 하는 값에는 `shallow: false`가 필요하다는 경계도 컨벤션에 적었다 (#46)

## 2026-08-24

### 개발 환경

- 커밋 메시지 어투를 명사형 종결로 정했다. 커밋 로그는 사람에게 말을 거는 글이 아니라 무엇을 했는지 훑는 목록이라, 종결어미를 떼면 같은 폭에 내용이 더 들어간다. 문서 어투는 그대로다 (#43)
- 문서와 커밋 메시지의 문체 규칙을 컨벤션에 적었다. 커밋 메시지와 README는 존댓말로, 변경 이력과 컨벤션 문서는 평서체로 쓴다. 지금까지 관습으로만 존재해 새로 합류한 사람이 알 방법이 없었고, 실제로 mobile 저장소 README가 다른 문체로 작성됐다가 뒤늦게 고쳐졌다. 이미 올라간 커밋은 이력을 되감아야 하므로 고치지 않는다 (#41)

## 2026-08-21

### 수정

- PR 제목을 유효한 형식에서 `WIP` 같은 무효 형식으로 고쳐도 이전 유형 라벨이 그대로 남던 문제 해결 — 라벨은 릴리스 노트의 분류 기준이라, 남은 라벨이 곧 내용과 무관한 분류가 됐다. 제목에서 유형을 찾지 못하면 이 워크플로우가 관리하는 라벨을 모두 뗀 뒤 끝낸다 (#39)
- `feat:`처럼 본문이 없거나 공백뿐인 PR 제목에 유형 라벨이 붙던 문제 해결 — PR 제목 검사가 콜론까지만 봐서 커밋 훅과 서로 다른 답을 냈다. 이제 양쪽 다 콜론 뒤에 공백 하나와 비공백 본문을 요구한다 (#39)
- 실재하지 않는 `#0`을 손으로 적으면 커밋 훅과 PR 제목 검사를 그대로 통과하던 문제 해결 — 브랜치명에서 번호를 채우는 훅은 이미 `#0`을 막고 있었지만 자동 삽입 경로에만 적용돼 수동 입력을 걸러내지 못했다. 이슈 번호 없는 `유형: 내용` 형식은 컨벤션이 허용하는 형태이므로 그대로 통과시킨다 (#39)
- WebView 앱에서 화면은 보이는데 버튼이 하나도 눌리지 않던 문제 해결 — dev 서버가 `localhost` 밖에서 오는 개발용 청크 요청을 403으로 막아 하이드레이션이 조용히 실패하고 있었다. 콘솔에 에러가 남지 않아 증상만으로는 원인을 알기 어려웠다. 에뮬레이터가 보는 호스트 주소를 허용 출처에 넣었고, dev 전용 설정이라 프로덕션 빌드는 그대로다 (#37)

### 개발 환경

- 디자인 시안의 기준 크기를 컨벤션에 못박음 — 모바일 393×852, 태블릿 768×1024, 웹 1920×1080. 시안 폭이 곧 전환점이 아니라는 점(`md`는 768px, `lg`는 1024px 진입)과 대응 시안이 없는 1024~1919px 구간의 처리 방침을 함께 적었다. 폰트·여백 값은 디자인팀이 프레임에 명시하므로 프론트는 옮기는 방식만 판단한다 (#35)

## 2026-08-18

### 수정

- Dependabot이 여는 PR마다 CI가 깨지던 문제 해결 — 두 패키지가 요구하는 버전 범위가 어긋나 중첩 설치되던 `@swc/helpers`를 `overrides`로 하나로 합쳤다. 지울 중첩 엔트리가 없으면 의존성 갱신기가 lockfile을 다시 써도 `npm ci`가 어긋나지 않는다 (#29)
- CI의 E2E가 끝나지 않던 문제 해결 — 브라우저 캐시가 적중하면 실행되던 `playwright install-deps`가 apt 프롬프트에서 입력을 기다리며 멈춰 있었다. 캐시 적중 여부로 나누던 분기를 없애 `install --with-deps` 하나로 합쳤고, 두 잡에 타임아웃을 걸어 다음에 멈추면 몇 분 안에 드러나게 했다 (#33)

### 개발 환경

- 정기 의존성 갱신(Dependabot version updates)을 껐다. 첫 실행에서 열린 3건이 전부 머지 불가였고, `eslint-config-next`가 딸고 오는 플러그인들이 major를 막고 있어 이 스택의 major는 Next.js와 함께 가야 한다. 취약점 알림은 저장소 설정에 있어 그대로 남는다 (#31)

## 2026-08-14

### 변경

- 서비스명을 `골라주개냥`으로 바꾸고 슬로건도 함께 맞춤 (#8)
- 다크 모드 토글의 탭 영역을 44×44px로 넓혀 터치 기준을 맞춤 (#10)

### 수정

- `/figma-watch`의 백로그 정합성 결함 수정 — 페이지 목록에서 페이지가 일시 누락되면 프레임이 대량 삭제로 오탐되던 것을 관찰 불가 처리로 막고, gh 없이 첫 실행하면 구현된 화면까지 미구현으로 박제되던 공백과 번호 발급·보고 시점의 모순을 정리 (#22)

### 개발 환경

- Figma 파일에 새로 올라오거나 바뀐 프레임을 감지해 알리는 `/figma-watch` 커맨드 — 스냅샷 비교 방식이고 `/loop`로 감싸면 주기적으로 확인한다 (#6)
- Figma 프레임을 구현으로 잇는 `/from-figma` 커맨드 — 디자인 분석과 FSD 배치 계획을 승인 게이트로 확인받은 뒤에만 이슈·구현으로 진행하고, `/figma-watch`가 새 프레임을 감지하면 이 분석·게이트까지 자동으로 이어진다 (#11)
- `/figma-watch`가 프레임 안쪽 요소의 추가·삭제·크기 변경까지 감지하도록 하위 트리 해시 비교 추가 (#12)
- `/figma-watch`가 감지한 프레임의 구현 여부를 이슈·PR의 프레임 URL과 대조해, 구현된 화면의 디자인 변경은 코드 갱신 신호로 알리고 중복 착수는 막는다 (#15)
- `/figma-watch` 첫 실행 보고를 현황판으로 확장 — 기준선 프레임 전체를 구현 여부와 함께 보여주고, 미구현 백로그에 번호를 붙여 `/from-figma <번호>`로 바로 착수할 수 있다 (#17)
- 백로그 번호를 평상시 감지된 새 프레임에도 이어서 부여하고, 재기준선 때 번호가 바뀔 수 있다는 경고와 다중 페이지 실측에서 확인된 한계(페이지 이동 시 ID 변경, 새 페이지 목록 미반영)를 명시 (#19)
- 초기 세팅 점검에서 확인된 하네스 결함 수정 — 리뷰 집계가 첫 30건만 세던 것, 머지된 PR을 열린 PR로 알리던 것, jq 없는 환경에서 훅이 에러를 뱉던 것, `git -C`·환경변수 접두 명령을 놓치던 것, ESLint 서브패스 우회, fork PR 라벨 부착 실패, 리뷰 조회 실패가 정상 결과로 위장되던 것 (#10)
- 문서가 서술한 사실을 실제와 맞춤 — 없는 파일 참조, 검증 순서 불일치, 커맨드 절차 불일치, lint가 잡지 못하는 규칙 범위 명시 (#10)
- 손으로 시키던 절차 자동화 — PR을 올리면 리뷰 완료까지 감시해 그대로 처리하고, 낡은 베이스에서 브랜치를 파면 알리고, 커밋 메시지의 이슈 번호는 브랜치명에서 채운다 (#24)
- 테스트 작성을 유도하는 장치를 `/new-slice`·PR 템플릿·`code-reviewer`·Write 훅 네 곳에 배선 — 지금까지 CI는 실행만 강제했고 작성을 요구하는 장치는 없었다 (#24)
- Dependabot 주 1회 의존성 갱신과 CI 캐시(Playwright 브라우저·Next 빌드) 도입 (#24)

## 2026-08-13

### 추가

- 초기 세팅 안내 홈 화면과 다크 모드 데모 토글
- TanStack Query Provider 배선 (`shared/providers`)

### 변경

- 홈 화면을 create-next-app 템플릿에서 서비스 소개 자리 표시 화면으로 교체

### 제거

- PR 동작 확인용으로 넣었던 임시 주석 (#1)

### 개발 환경

- 프론트엔드 스택 설치 — Next.js 16, React 19, Tailwind v4, shadcn/ui, TanStack Query, Zustand, react-hook-form, zod, date-fns, Recharts
- Feature-Sliced Design 폴더 구조와 레이어·슬라이스별 README
- 컨벤션 문서 7종 (`docs/conventions`)
- Prettier·LF 줄바꿈·에디터 공용 설정
- ESLint 컨벤션 강제 — FSD 레이어 경계, Tailwind 클래스, 파일·폴더 네이밍, import 위치
- 테스트 환경 — Vitest 단위 테스트, Playwright E2E
- GitHub Actions CI — typecheck·format·lint·test·build·E2E
- husky 로컬 훅 — pre-commit 린트·포맷, commit-msg 형식 검사, pre-push 이력 보호
- GitHub 이슈·PR 템플릿, CodeRabbit 리뷰 설정
- Claude Code 하네스 (`.claude`) — 슬래시 커맨드, code-reviewer 에이전트, 알림 훅
- 작업 기록 자동화 — PR 제목으로 라벨 부착, 라벨 기반 릴리스 노트 분류, 커밋에서 변경 이력 문장 생성 (#3)
- `main`·`dev` 브랜치 보호 Ruleset — PR 필수, CI 통과 필수, rebase 전용, 이력 되감기 차단
- 원격에 올릴 때 변경 이력 반영과 미응답 리뷰를 확인하는 훅 — PR 생성과 그 이후의 push를 모두 보므로 리뷰 대응 커밋도 기록에서 빠지지 않는다 (#4)
- 변경 이력을 날짜 단위로 바꾸면서 남아 있던 `[Unreleased]` 참조 정정 (#4)
